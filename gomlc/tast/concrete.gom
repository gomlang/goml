package tast;

pub fn type_is_concrete(value: Type) -> bool {
    match prune(value) {
        Var(_) | Param { name: _ } => false,
        value => !any_child_type(value, |child| !type_is_concrete(child)),
    }
}
