package api;

pub trait Render {
    fn render(self: Self) -> string;
}

pub struct Value {
    pub text: string,
}

impl Render for Value {
    fn render(self: Value) -> string {
        self.text
    }
}
