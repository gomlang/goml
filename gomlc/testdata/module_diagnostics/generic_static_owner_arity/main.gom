package main;

struct Box[T] {
    value: T,
}

impl Box[f64] {
    fn from_float(value: f64) -> Box[f64] {
        Box { value }
    }
}

fn main() -> () {
    let _ = Box::[f64, isize]::from_float(1.0);
}
