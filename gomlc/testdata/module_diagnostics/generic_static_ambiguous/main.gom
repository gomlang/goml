package main;

struct Box[T] {
    value: T,
}

impl Box[f64] {
    fn empty() -> Box[f64] {
        Box { value: 0.0 }
    }
}

impl Box[isize] {
    fn empty() -> Box[isize] {
        Box { value: 0 }
    }
}

fn main() -> () {
    let _ = Box::empty();
}
