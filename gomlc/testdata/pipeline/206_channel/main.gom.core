fn decimal_string(value/0: u64) -> string {
  let _wild0 =
    if (value/0 == 0) {
        {
          return "0"
        }
    } else {
        ()
    } in
  let reversed/0 = @intrinsic(vec.with_capacity)(20) in
  let remaining/0 = value/0 in
  let _wild1 =
    while (remaining/0 > 0) {
        {
          let _wild2 = inherent#Vec#Vec[u8]#push(reversed/0, (((remaining/0 % 10) as u8) + 48)) in
          let _wild3 =
            {
              let compound_old0 = remaining/0 in
              let compound_value0 = 10 in
              remaining/0 = (compound_old0 / compound_value0)
            } in
        }
    } in
  let bytes/0 = @intrinsic(vec.with_capacity)(inherent#Vec#Vec[u8]#len(reversed/0)) in
  let offset/0 = 0 in
  let _wild4 =
    while (offset/0 < inherent#Vec#Vec[u8]#len(reversed/0)) {
        {
          let _wild5 =
            inherent#Vec#Vec[u8]#push(
              bytes/0,
              @intrinsic(vec.get)(
                reversed/0,
                ((inherent#Vec#Vec[u8]#len(reversed/0) - offset/0) - 1)
              )
            ) in
          let _wild6 =
            {
              let compound_old1 = offset/0 in
              let compound_value1 = 1 in
              offset/0 = (compound_old1 + compound_value1)
            } in
        }
    } in
  let mtmp0 = @runtime(core.string_from_utf8)(bytes/0) in
  {
    let x0 = mtmp0.1 in
    {
      let result/0 = x0 in
      {
        result/0
      }
    }
  }
}

fn signed_decimal_string(value/0: i64) -> string {
  if (value/0 < 0) {
      {
        ("-" + decimal_string((0 - (value/0 as u64))))
      }
  } else {
      {
        decimal_string((value/0 as u64))
      }
  }
}

fn __goml_builtin_int_to_string(value/0: isize) -> string {
  signed_decimal_string((value/0 as i64))
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn trait_impl#ToString#isize#to_string(self/0: isize) -> string {
  __goml_builtin_int_to_string(self/0)
}

fn inherent#Vec#Vec[T]#push[T](self/0: Vec[T], elem/0: T) -> () {
  @intrinsic(vec.push)(self/0, elem/0)
}

fn inherent#Vec#Vec[T]#len[T](self/0: Vec[T]) -> isize {
  @intrinsic(vec.len)(self/0)
}

fn inherent#Channel#Channel[T]#new[T](capacity/0: isize) -> Channel[T] {
  @intrinsic(channel.new)(capacity/0)
}

fn inherent#Channel#Channel[T]#send[T](self/0: Channel[T], value/0: T) -> () {
  @intrinsic(channel.send)(self/0, value/0)
}

fn inherent#Channel#Channel[T]#recv[T](self/0: Channel[T]) -> Option[T] {
  let mtmp0 = @intrinsic(channel.recv)(self/0) in
  {
    let x0 = mtmp0.0 in
    let x1 = mtmp0.1 in
    {
      let ok/0 = x1 in
      {
        let value/0 = x0 in
        {
          if ok/0 {
              {
                Option::Some(value/0)
              }
          } else {
              {
                Option::None
              }
          }
        }
      }
    }
  }
}

fn inherent#Channel#Channel[T]#close[T](self/0: Channel[T]) -> () {
  @intrinsic(channel.close)(self/0)
}

fn println[T](value/0: T) -> () {
  @runtime(core.string_println)(trait_call[ToString::to_string](value/0))
}

fn make_string_channel() -> Channel[string] {
  inherent#Channel#Channel[string]#new(0)
}

fn roundtrip[T](channel/0: Channel[T], value/0: T) -> Option[T] {
  let _wild0 = go || => inherent#Channel#Channel[T]#send(channel/0, value/0) in
  inherent#Channel#Channel[T]#recv(channel/0)
}

fn main() -> () {
  let buffered/0 = inherent#Channel#Channel[isize]#new(2) in
  let _wild0 = inherent#Channel#Channel[isize]#send(buffered/0, 10) in
  let _wild1 = inherent#Channel#Channel[isize]#send(buffered/0, 20) in
  let _wild2 = inherent#Channel#Channel[isize]#close(buffered/0) in
  let _wild3 =
    println(
      {
        let mtmp0 = inherent#Channel#Channel[isize]#recv(buffered/0) in
        match mtmp0 {
          Option::None => {
            -1
          },
          Option::Some(x0) => {
            {
              let x0 = Option::Some._0(mtmp0) in
              {
                let value/0 = x0 in
                value/0
              }
            }
          },
        }
      }
    ) in
  let _wild4 =
    println(
      {
        let mtmp1 = inherent#Channel#Channel[isize]#recv(buffered/0) in
        match mtmp1 {
          Option::None => {
            -1
          },
          Option::Some(x1) => {
            {
              let x1 = Option::Some._0(mtmp1) in
              {
                let value/1 = x1 in
                value/1
              }
            }
          },
        }
      }
    ) in
  let _wild5 =
    println(
      {
        let mtmp2 = inherent#Channel#Channel[isize]#recv(buffered/0) in
        match mtmp2 {
          Option::None => {
            "closed"
          },
          Option::Some(x2) => {
            {
              "open"
            }
          },
        }
      }
    ) in
  let unbuffered/0 = make_string_channel() in
  let _wild6 =
    println(
      {
        let mtmp3 = roundtrip(unbuffered/0, "ready") in
        match mtmp3 {
          Option::None => {
            "closed"
          },
          Option::Some(x3) => {
            {
              let x3 = Option::Some._0(mtmp3) in
              {
                let value/2 = x3 in
                value/2
              }
            }
          },
        }
      }
    ) in
  let _wild7 = inherent#Channel#Channel[string]#close(unbuffered/0) in
}