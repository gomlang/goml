fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  @runtime(core.bool_to_string)(self/0)
}

fn trait_impl#ToString#string#to_string(self/0: string) -> string {
  self/0
}

fn println[T](value/0: T) -> () {
  @runtime(core.string_println)(trait_call[ToString::to_string](value/0))
}

fn main() -> () {
  let _wild0 = println(@go_ffi(strings, ToUpper)("goml")) in
  let mtmp0 = @go_ffi(strings, Cut)("left:right", ":") in
  {
    let x0 = mtmp0.0 in
    let x1 = mtmp0.1 in
    let x2 = mtmp0.2 in
    {
      let found/0 = x2 in
      {
        let after/0 = x1 in
        {
          let before/0 = x0 in
          {
            let _wild1 = println(before/0) in
            let _wild2 = println(after/0) in
            let _wild3 = println(found/0) in
            @go_ffi(runtime, Gosched)()
          }
        }
      }
    }
  }
}