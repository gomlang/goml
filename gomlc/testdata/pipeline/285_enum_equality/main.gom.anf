fn same(a/0: E, b/0: E) -> bool {
  match a/0 {
    Tag_0 => {
      match b/0 {
        Tag_0 => {
          true
        },
        _ => {
          false
        },
      }
    },
    Tag_1 => {
      match b/0 {
        Tag_1 => {
          true
        },
        _ => {
          false
        },
      }
    },
    Tag_2 => {
      match b/0 {
        Tag_2 => {
          true
        },
        _ => {
          false
        },
      }
    },
  }
}

fn same_reversed(a/0: E, b/0: E) -> bool {
  match b/0 {
    Tag_0 => {
      match a/0 {
        Tag_0 => {
          true
        },
        _ => {
          false
        },
      }
    },
    Tag_1 => {
      match a/0 {
        Tag_1 => {
          true
        },
        _ => {
          false
        },
      }
    },
    Tag_2 => {
      match a/0 {
        Tag_2 => {
          true
        },
        _ => {
          false
        },
      }
    },
  }
}

fn main() -> () {
  let t0 = [Tag_0, Tag_1, Tag_2] in
  let values/0 = @intrinsic(vec.from_array)(t0) in
  let t1 = [same, same_reversed] in
  let functions/0 = @intrinsic(vec.from_array)(t1) in
  let for_limit0 = @intrinsic(vec.len)(values/0) in
  let for_index0 = 0 in
  join exit0() -> () {
    ()
  } in
  joinrec {
    join loop0() -> () {
      let t2 = (for_index0 < for_limit0) in
      if t2 {
        let for_item0 = @intrinsic(vec.get)(values/0, for_index0) in
        let t3 = (for_index0 + 1) in
        let _wild0 = for_index0 = t3 in
        let for_limit1 = @intrinsic(vec.len)(values/0) in
        let for_index1 = 0 in
        join exit1() -> () {
          jump loop0()
        } in
        joinrec {
          join loop1() -> () {
            let t4 = (for_index1 < for_limit1) in
            if t4 {
              let for_item1 = @intrinsic(vec.get)(values/0, for_index1) in
              let t5 = (for_index1 + 1) in
              let _wild1 = for_index1 = t5 in
              let for_limit2 = @intrinsic(vec.len)(functions/0) in
              let for_index2 = 0 in
              join exit2() -> () {
                jump loop1()
              } in
              joinrec {
                join loop2() -> () {
                  let t6 = (for_index2 < for_limit2) in
                  if t6 {
                    let for_item2 = @intrinsic(vec.get)(functions/0, for_index2) in
                    let t7 = (for_index2 + 1) in
                    let _wild2 = for_index2 = t7 in
                    let t8 = for_item2(for_item0, for_item1) in
                    let inline0 = trait_impl#ToString#bool#to_string(t8) in
                    let inline1 = @runtime(core.string_println)(inline0) in
                    jump loop2()
                  } else {
                    jump exit2()
                  }
                } in
              } in
              jump loop2()
            } else {
              jump exit1()
            }
          } in
        } in
        jump loop1()
      } else {
        jump exit0()
      }
    } in
  } in
  jump loop0()
}

fn println__T_bool(value/0: bool) -> () {
  join inline_cont0(t0: string) -> () {
    let t1 = @runtime(core.string_println)(t0) in
    ()
  } in
  let inline0 = @runtime(core.bool_to_string)(value/0) in
  jump inline_cont0(inline0)
}

fn trait_impl#ToString#bool#to_string(self/0: bool) -> string {
  let t0 = @runtime(core.bool_to_string)(self/0) in
  t0
}