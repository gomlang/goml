package std;

use module::ascii;
use module::bincode;
use module::bytes;
use module::c;
use module::cmp;
use module::collections;
use module::context;
use module::crypto;
use module::encoding;
use module::error;
use module::env;
use module::ffi;
use module::fs;
use module::hash;
use module::io;
use module::iter;
use module::json;
use module::math;
use module::num;
use module::os;
use module::path;
use module::process;
use module::rand;
use module::resource;
use module::serde;
use module::simd;
use module::text;
use module::toml;
use module::time;
use module::utf8;
use module::utf16;
use module::unicode;
