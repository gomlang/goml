package hash;

use module::bytes;

pub trait Hasher {
    fn update(self: Self, input: Slice[byte]) -> ();
    fn reset(self: Self) -> ();
    fn sum(self: Self) -> bytes::Bytes;
    fn size(self: Self) -> isize;
    fn block_size(self: Self) -> isize;
}
