package main;

use std::testing;

#[test]
fn llvm_builds_and_verifies_ir() -> () {
    let ir = build_ir();
    testing::assert(ir.contains("define i32 @answer()"));
    testing::assert(ir.contains("ret i32 42"));
}
