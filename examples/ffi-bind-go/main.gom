package main;

use module::bindings;
use std::ffi;

fn main() -> () {
    println(bindings::count(ffi::string_from_text("banana"), ffi::string_from_text("a")));
    let (value, error) = bindings::parse(ffi::string_from_text("42"));
    println(value);
    println(ffi::error_is_nil(error));
}
