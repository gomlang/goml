package tests;

use tools::markdown_entities as generator;
use std::testing;

#[test]
fn pinned_html_entities_reproduce_checked_in_lookup_functions() -> () {
    testing::assert_ok(generator::verify(".."));
}
