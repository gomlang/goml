package tests;

use tools::unicode_data as generator;
use std::testing;

#[test]
fn fresh_unicode_sources_match_tables_and_official_conformance() -> () {
    testing::assert_ok(generator::verify(".."));
}
