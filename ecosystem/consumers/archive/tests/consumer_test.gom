package tests;

use consumer::archive as example;
use ecosystem::archive;
use std::testing;

#[test]
fn versioned_archive_api_and_generic_streams() -> () {
    testing::assert_ok(example::exercise());
    testing::assert(archive::safe_path("../outside", 4096).is_err());
}
