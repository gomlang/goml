package tests;

use consumer::tui_markdown as app;

#[test]
fn public_markdown_viewer_composes_with_tui() -> () {
    app::exercise();
}

#[test]
fn public_optional_tables_and_plain_output() -> () {
    app::table();
}
