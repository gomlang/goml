package main;

use ecosystem::parser::arithmetic;
use std::testing;

fn main() -> () {
    testing::assert_eq(
        testing::expect_ok(arithmetic::expression().parse("6 * (3 + 4)"), "expression"),
        42,
    );
    println("parser consumer: ok")
}
