package tests;

use ecosystem::consumers::mime;
use std::testing;

#[test]
fn consumer_uses_public_mime_contract() -> () {
    testing::assert_ok(mime::exercise());
    testing::assert_ok(mime::exercise_quotedprintable());
    testing::assert_ok(mime::exercise_multipart());
}
