# Batch A acceptance evidence

This tracks the matrix in `plan.md`. Passing a related unit test is not treated as proof of a broader requirement. Batch A passed the task and runtime acceptance gates in the final current-tree test/bootstrap/CI sequence recorded in implementation-plan.md. No cross-target execution is claimed without a target runner.

| Matrix | Current evidence | Remaining scope |
| --- | --- | --- |
| A-I01–03 | Every numeric width and bool roundtrips through actual Go generic identity calls; public runtime test covers string, char, marker identity, read-only/mutable slices with shared mutation, and all channel directions; existing pipeline/module tests cover array, unit and flat multi-results | Covered by helper, TAST and end-to-end wrapper tests |
| A-I04–06 | `TestInvalidCalls`, `TestMissingPackageAndMain`, `TestSleepDefinedType`, alias/defined-type witness cases | Covered by helper tests and existing source diagnostics |
| A-I07 | Malformed-request tests, blocked-input cancellation, pre-canceled validation and missing-tool tests; `TestCancellationDuringPackageLoad` cancels/expires after observing an active Go list process | Covered by helper tests on the Linux host; shell-wrapper test skips Windows |
| A-I08 | Helper duplicate-witness/mixed-failure tests; two packages with the same Go target roundtrip interface and Core, merge to two distinct declaration identities and retain both source locations | Covered by helper and cross-artifact origin tests |
| A-I09 | Generic inference and variadic calls in witness and differential Go-build tests | Covered by existing helper tests |
| A-E01 | Complete fixture under `tools/release/testdata/go-export`, driver temporary-copy test and extracted-release smoke | Fixture runs check, export, Go test and prints `42 4 7 8` |
| A-E02 | Public CLI test assigns exact Go function types for every integer width, bool, f32/f64, nested fixed arrays, unit and mixed tuple results; executes integer limits and floating-point bit patterns | Covered on the host architecture; isize/usize use native Go int/uint limits |
| A-E03–04 | AST/TAST negative annotation and unsupported-value tests | Covered by frontend tests |
| A-E05–07 | Library-root artifact roundtrip and Core→ANF tests; external Go calls; unused imports and helpers removed | Covered by existing linking/backend tests |
| A-E08 | Public CLI exports a four-package dependency chain with three OnceCell initializers and shared trace state; fresh Go processes test root-first and leaf-first calls, ordering, repeated calls and exclusion of a panicking GoML main | Covered by dependency runtime fixture and existing backend tests |
| A-E09 | Duplicate export, generated-name/import collision tests; staged handwritten-Go collision test | Covered by current frontend/backend/overlay tests |
| A-B01–02 | Project cache-hit shim signature rejection, first-publication failure, shim body-change execution tests | Covered by ordinary project command tests |
| A-B03 | Helper target/file/hash tests plus public export regeneration switching Linux/Windows and build tags, removing/restoring selected files and correcting signatures | Covered by helper and public export tests; cross-target checks are compile/typecheck only |
| A-B04 | Complete Add/AbsPlusOne/Pair fixture emitted from source-compiled Core and separately via serialized Core CLI after source deletion; equal API JSON and exact Go signatures/results; existing dependency tests exercise interface inputs | Covered by complete-fixture comparison and interface/dependency tests |
| A-B05 | Public CLI rejects traversal, import mismatch, invalid package names, nested modules, unowned source and handwritten symbol collisions while preserving user files; internal tests cover symlinks, concurrent locks and rollback | Covered by public CLI and internal destination/publication tests |
| A-B06 | Deterministic CLI regeneration; complete fixture deletes GoML sources before Go test/run; extracted-toolchain smoke repeats export | Covered by fixture and distribution tests; latest CI evidence is in implementation-plan.md |
| A-B07 | Internal overlay tests plus public CLI direct/indirect cycle regressions; failed regeneration preserves prior source and manifest and releases the output lock | Covered by staged-check and public CLI tests |
| A-B08 | Existing stage0 compilation, fixed-point CI, helper packaging/race tests; cold bootstrap and expanded release smoke run for this change | Passed cold bootstrap and subsequent CI; exact results are recorded in implementation-plan.md |

The tests are in `tools/goml-go-meta/internal/validate`, `gomlc/cmd/gomlc`, `gomlc/tast`, `gomlc/go_backend`, `goml/project` and `goml/cmd/goml`. The release smoke uses only copied fixture files and the extracted toolchain for GoML operations, then removes GoML source/manifest and invokes ordinary Go tools with no GoML binaries on PATH.

## Task acceptance audit

The task requirements in plan sections A0–A9 were checked against the implementation as well as the runtime matrix. Final command results are recorded in implementation-plan.md; the current-tree test, clean-bootstrap and CI sequence exited successfully.

| Task | Implementation and verification evidence |
| --- | --- |
| A0 | implementation-plan.md records the initially clean HEAD, tool versions/environment, actual recipes, compiler responsibility mapping and passing baseline test/CI logs. Existing goldens remain generated by the repository harness. |
| A1 | `gomlc/ffi` models and codecs plus helper `internal/protocol` cover version, context, caller, binding identity, structured types, diagnostics and limits. Requests use argument-array subprocesses; helper stdout is JSON only. Build context contains selected executable/toolchain, module directory, target, cgo, flags/tags and explicit workspace/network policies. |
| A2 | Helper validation constructs Go AST witnesses in one packages.Load world, observes generic inference and fixed variadic arguments, and rejects invalid symbols/imports/signatures. `build.sh` uses GOHOSTOS/GOHOSTARCH for the helper; target settings apply to analyzed packages. Missing tools, malformed requests, active-load cancellation, deadlines, target changes and source changes have tests. |
| A3 | Package publication and ordinary project cache paths require current validation before successful output. Private/unused/interface/Core bindings are collected; declaration identities and available source spans are preserved. No-binding and explicit Off paths avoid helper work; Off is observable. TAST retains pure bridge checks; driver code owns host queries. |
| A4 | AST/TAST validate export attachment, visibility, generic/comptime/test/extern exclusions, names and ABI shapes. Interface/Core/compiler-world formats encode export identity/type/origin with explicit version bumps and semantic hashes excluding origin. Roundtrip, renamed-export and unsupported-annotation tests cover the contract. |
| A5 | Library linking selects reachable Core units, sets explicit selected-package function roots before Mono and preserves private/generic/dependency calls. The backend retains export wrappers as roots; existing executable/test entry handling remains separate. No synthetic GoML main or global DCE disabling is used. |
| A6 | `lower_library` and `encode_package` emit ordinary Go packages and real multiple results. Namespace transformation hides internal declarations and checks export/import/type/helper collisions. Statics use Go package globals and existing lazy OnceCell contents; dependency runtime tests prove shared initialization and no main invocation. Panic behavior is tested without a recovery wrapper. |
| A7 | Public export orchestration validates source/destination/module identity and owns only two checksummed files. Output and artifact locks serialize publication/build work. Candidate Go overlays include handwritten files, reject cycles/collisions and prevent old output masking. Tests cover ownership, rollback, symlinks, invalid paths and module-file preservation. |
| A8 | The shared complete fixture runs in temporary modules. Source/Core API and result comparisons, cache-hit signature changes, body changes, tags/platform/file changes and the full runtime matrix pass. No persistent Verified cache exists. Cross-target checks are explicitly build/typecheck-only. |
| A9 | `.justfile` builds/installs/tests the helper before compiler use and packages it with the toolchain. Stage0 builds the current sources; stage3 fixed-point comparison and extracted-archive helper/export smoke tests pass. The language guide documents ABI, commands, modes, failures, initialization/panic and limitations. Final current-tree test/bootstrap/CI logs are the completion gate. |

Limits remain explicit: Windows target checks do not claim execution; active-load shell tests run on the Linux host. Publication handles reported errors but does not claim two-file crash atomicity. A does not add the external types, pointers, error/text/container adapters or callback capabilities assigned to B and C.

The final whole-plan audit expanded TestDifferentialGoBuild to 24 native-build comparisons, adding argument/result counts, private/missing symbols, missing/main packages, directional any/defined-type assignment and preserved discarded returns. The complete helper race suite passed after this test-only change (`/tmp/goml-plan-final-audit-checks-0907.log`). The complete implementation had just passed CI with 895 compiler and 154 driver tests (`/tmp/goml-bind-overlay-ci-retry-0907.log`); no implementation code changed between these checks. See [plan-audit.md](plan-audit.md) for the final evidence boundary.
