# Go interoperability implementation status

The scope remains all of `plan.md`: checked imports and Go libraries (A), explicit boundaries (B), callbacks (C), and interfaces/binding generation (D). All batches have passed the plan-wide acceptance audit in [plan-audit.md](plan-audit.md). Full CI passed with 895 compiler and 154 driver tests, helper race checks, stage3 fixed point and extracted-release/LSP smoke. A subsequent test-only expansion passed the complete helper race suite and module format checks. Individual acceptance documents retain revision-specific evidence; acceptance-d1.md and acceptance-d2.md now include the final full CI. Entries below are chronological evidence, not current limitations or a replacement task scope.

## Baseline: 2026-09-06

- Starting HEAD: `7f2a64b23` (`fix`). `git status --short` was empty.
- Host: `go version go1.25.8 linux/amd64`.
- `go env GOOS GOARCH CGO_ENABLED GOWORK GOTOOLCHAIN`: `linux`, `amd64`, `1`, empty, `auto`.
- Read repository `AGENTS.md`, `plan.md`, the Go FFI section in `docs/goml.md`, the actual `.justfile`, and existing FFI wrappers and fixtures.
- `just --list` confirms `make`, `test`, `bootstrap`, `ci`, `update-golden`, and `verify-golden` exist. `test` depends on `make-tools`, which depends on `make`; `make` builds stage2 using the pinned stage0. Fixed-point comparison builds stage3 and stage3-fixed from their own stage tools.
- Ran `just test`, exit 0: compiler 747 passed, driver 78 passed, zero failures or ignored tests. Full local log: `/tmp/goml-ffi-baseline.log` (ephemeral evidence, not a portable build input).
- Ran `just ci`, exit 0, after adding the protocol package: the existing compiler/driver suites, stage3/stage3-fixed artifact comparison, script checks, extension build, and release smoke gates passed. Full local log: `/tmp/goml-ffi-ci.log`. The new independent Go module is tested separately; it is not yet wired into CI.
- No baseline failure was observed. No golden file was regenerated or edited.

## Verified implementation entry points

| Source / symbol | Current responsibility and integration implication |
| --- | --- |
| `gomlc/tast/env.gom`: `go_ffi_signature_diagnostic`, `validate_go_ffi_signatures` | Checks resolved GoML shapes; keep package loading outside this layer. |
| `gomlc/tast/env.gom`: `go_ffi_attribute_target`, `extern_callable` | Attribute target becomes a `GoForeign` callable. |
| `gomlc/artifact/tast_codec/callable.gom` | Encodes and decodes `GoForeign` path and symbol. Further interface/Core ownership and version audit remains outstanding. |
| `gomlc/cmd/gomlc/package_driver.gom`: `run_package_command` | Package compilation orchestration; external validation must precede successful artifact publication. |
| `gomlc/cmd/gomlc/package_driver.gom`: `run_link_command`, `run_test_link_command`, `entry_core`, `add_entry_wrapper` | Existing executable/test linking and entry construction. GoLibrary must use explicit roots without a synthetic main. |
| `gomlc/cmd/gomlc/artifact_package.gom`: `artifact_reachable_core_units` | Selects reachable package Core units before linking. |
| `gomlc/core/dce.gom`: `eliminate_unreachable_functions` | Seeds reachability from `Function.root`; export roots must already exist here. |
| `gomlc/mono/specialize.gom`: root scan | Seeds non-generic root functions. |
| `gomlc/mono/lower.gom`: `lower_checked` | Validates Core, specializes, lowers functions, materializes types and statics, then validates Mono. |
| `gomlc/go_backend/lower.gom`: `go_foreign_wrapper` | Ordinary calls; direct marker arguments read `.data`; a single marker result is wrapped; tuple results use typed multiple assignment; unit performs a call then returns internal unit. |
| `gomlc/go_backend/lower.gom`: `emit_statics`, `prune_functions` call sites | Static emission and final function pruning exist; library initialization and pruning roots still need a complete audit. |

## Existing contract coverage

- `gomlc/testdata/pipeline/244_go_ffi/main.gom`: string scalar, tuple (`strings.Cut`), and zero result (`runtime.Gosched`). Included in passing compiler baseline.
- `gomlc/testdata/module/project048_go_ffi/`: cross-package bindings, string/integer tuple, slice, fixed array, and channel. Included in passing compiler baseline.
- Dedicated duplicate-symbol and empty-marker contract coverage, all scalar widths, mutable slice and directional channel coverage still require a complete audit and additions where absent. A0 is not yet fully complete.

## Progress and next acceptance gates

- A1/A2 in progress: independent helper with strict protocol decoding, explicit build/caller contexts, structured bridge and referenced Go type graphs, bounded input/output, signal/deadline handling and cancellation of blocked input.
- Pinned `golang.org/x/tools v0.36.0`, `x/mod v0.27.0`, and `x/sync v0.16.0`; checked-in `go.sum`. The helper uses official `go/packages` and `go/types` through package loading, not the repository's experimental Go parser.
- Witnesses are generated as Go AST, formatted, and supplied through a Go package overlay in the actual caller directory. One load world includes all bindings. Equal binding requests share one witness while retaining individual declaration IDs. Official package loading checks internal restrictions and package-main imports. No source witness files are written into the caller directory.
- Referenced type graphs distinguish aliases and named types, preserve method signatures and recursive references, and are bounded. Display signatures are diagnostics only. Go world identity incorporates target/build context, selected source contents and package/module identities; no persistent Verified cache exists.
- `go test -race ./...` passed after implementing the loader and metadata graph. Coverage includes real `go build` differential tests, generic inference, fixed variadic arguments, named versus alias types, any/multiple/zero results, duplicate requests, independent per-binding failures, recursive metadata, missing packages/symbols/tools, package-main/internal restrictions, pre-cancellation, changed shim bodies/signatures, and Linux/Windows target signatures. Windows is checked/compiled, not run.
- The helper is built for the host by `tools/goml-go-meta/build.sh`, installed in stage2/stage3, copied by `just install`, and included in release packaging. CI now runs its race tests. Release smoke invokes the extracted helper in a separate temporary Go module with a standard-library binding.
- Added `gomlc/ffi/{model,protocol}.gom`: explicit check mode/build/caller/binding models, JSON request encoding, bounded response parsing, binding completeness/identity/status checks, referenced-type tag/ID checks, and verification results. Added `gomlc/tast/ffi.gom`: pure resolved-type conversion preserving char/int32, containers/directions, direct empty marker data, unit and flat tuple results; imported callable metadata cannot enable comptime/derive execution through this conversion.
- Added driver orchestration primitives in `gomlc/cmd/gomlc/ffi_driver.gom`: argument-array subprocess invocation, request/output size checks, 125-second outer timeout, UTF-8/protocol validation, and exit-status consistency checks. These primitives are now called by the internal artifact-check command; ordinary project-command scheduling remains pending.
- Formatted the affected `gomlc` module and ran `goml check`, then `just ci` (exit 0; `/tmp/goml-ffi-goml-ci.log`). Compiler tests: 754 passed (including 7 new wire/TAST tests); driver tests: 78 passed. Stage0 compilation, stage3 fixed point, helper race tests and release gates passed. Earlier development test failures from incorrect test API usage were fixed; a separate targeted run encountered an abandoned empty output lock, so final verification used CI's independent artifact directory.
- Added driver-side Go build-context resolution and strict context JSON decoding. Added collection of foreign bindings from artifact function/trait metadata without loading unrelated builtin resources. A live GoML driver test invokes the adjacent installed helper, observes a changed shim signature on the next request, checks required-tool failure and Off/Unverified behavior, and checks that module files/witness sources are not written. This test uses explicit artifact exports containing an unused binding; source visibility collection is not yet proven by it.
- The driver test also round-trips the binding through binary Core serialization and the resolved context through JSON before helper validation. Ran `just ci`, exit 0 (`/tmp/goml-ffi-context-ci.log`): 755 compiler tests and 78 driver tests passed, including this live integration test; helper race tests, stage0 compilation, fixed point and release gates passed. Full package-command and cache acceptance remain outstanding.
- Current integration limitations: ordinary project required/off behavior, context propagation and cache-hit validation are implemented below. LSP remains unchanged. Package-level metadata still needs complete source provenance mapping and directional/indexed diagnostics on the GoML side.
- Ran the updated helper `go test -race ./...`, `just ci` (exit 0; `/tmp/goml-ffi-helper-ci.log`), and the updated extracted-release smoke (exit 0; `/tmp/goml-ffi-helper-smoke.log`). The release smoke invokes the packaged helper, not the development helper. Build-tag changes, file deletion, all bridge representations and relocated source-world identity also pass automated tests.
- A2 acceptance is not complete: timeout/process-tree audit, broader collection-direction differential coverage, protocol error detail and remaining portability/determinism cases still require verification. The helper intentionally checks only package-level function objects. Legacy discarded return values follow the existing expression-statement wrapper behavior.
- A4 frontend validation and export artifacts are implemented below; A5–A9 roots, GoLibrary backend/initialization, CLI output ownership, full regression and final documentation remain unfinished. Some A9 distribution plumbing is in place but is not proof of complete A9 acceptance.
- B0–B4, C0–C2, and D0–D2 are not started. Do not advance stage0 or use unsupported new syntax in self-hosted sources as a shortcut.
- Ordinary `goml check/build/run/test` now invoke the helper; see the project integration section below. `export-go` remains unimplemented.

## Next integration work identified from current code

`goml/project/plan.gom::prepare_cached_command` returns `Fresh` without executing package compilation. Required-mode validation therefore needs an independent command or cache-hit verification step; a hook only inside `run_package_command` cannot meet A3/A8. `ArtifactCoreUnit.exports` retains full package function metadata. Interfaces now also retain a separate `foreign_bindings` verification field collected from full package exports before publishing a check result; their GoML public exports remain filtered. The next step is to consume this metadata during cache-hit validation and reconcile public callable descriptors with their verification metadata, while preserving explicit required/off policy.

The context resolver chooses a local installed toolchain and now honors persisted Go environment configuration. Both resolver and helper preserve GOENV while explicitly applying target, module, workspace and network policy. Tests cover saved GOFLAGS and saved GOPATH in module-off mode. Final-build context propagation is still pending; these tests do not yet prove end-to-end build equivalence.

## Interface verification metadata

Interface JSON format is now 24, binary interface value-section version is 7, and compiler-world metadata format is 19. Core payload layout remains unchanged. The new `foreign_bindings` field contains stable binding IDs, Go targets and structured bridge signatures, sorted by binding ID and included in the interface semantic hash. It is distinct from `exports` and `package_interface`, so private extern names do not become importable GoML API. No host paths or timestamps are introduced into this metadata.

Package compilation collects the field from full internal exports, with dependency trait information available for empty-marker checks. Binary/JSON decoding rejects duplicate IDs, unknown bridge tags, excessive type depth/count, nested marker data and invalid channel directions. Function artifact decoding now rejects a GoForeign callable marked comptime/derive, including when decoding Core exports through the shared exports codec.

New tests compile a real source package with a private unused extern, prove its verification metadata survives binary interface round-trip without entering the public name table, and show that changing its Go target changes the interface hash. An additional malformed-artifact test verifies recoverable rejection of an unknown bridge type. The first full compiler test run reported 753 passes and four expected fixed-hash failures from the schema change; these source-test assertions were updated after checking the serialized schema, with the base interface hash independently recomputed using SHA-256. No generated golden file was changed.

Source-span provenance, project CLI/cache integration, and broader provenance checks remain outstanding. This schema work does not yet make `goml check` invoke the helper.

Final verification for the interface schema change: `just make` passed; `just ci` passed with 757 compiler tests and 78 driver tests, helper race tests, stage0 compilation, fixed-point comparison and release smoke. Log: `/tmp/goml-ffi-interface-ci.log`. The four schema-dependent assertion failures are resolved.

## Artifact-check command and consistency

Added exact structural bridge/binding equality and identity-based merging. The interface consumer combines verification metadata with public callable descriptors, includes callable bindings even when metadata is absent, and rejects conflicting targets or signatures for the same stable ID. Distinct declaration IDs remain distinct even when they call the same Go symbol. Tests also exercise binary rejection of a forged comptime GoForeign function.

`gomlc __ffi-check-artifacts` is now available as an internal driver entry point. It accepts repeated `--interface-path`/`--core-path`, and requires explicit `--context`, `--caller-directory`, `--caller-import-path`, `--caller-package`, `--caller-mode package|files`, and `--ffi-check required|off`. It reads current artifacts on every invocation, combines dependency trait information, reconciles metadata/callables, and invokes only the helper adjacent to the compiler. Required validation consumes the supplied context JSON; Off emits an observable `ffi-unverified` warning. With no foreign bindings it exits successfully without reading the context or invoking the helper. This command has no persistent verification cache.

Formatted `gomlc` and ran `just make` and `just ci`. Final CI passed with 763 compiler tests and 78 driver tests, plus helper race tests, stage0 compilation, fixed-point comparison and release gates (`/tmp/goml-ffi-artifact-command-ci.log`). The live CLI test checks a real private-extern interface, missing Go symbol rejection, Off warning, and the no-bindings fast path. The earlier consistency-only CI also passed (`/tmp/goml-ffi-consistency-ci.log`).

The project driver does not yet schedule this command. Next work must propagate the explicit policy/build context into source compilation and schedule fresh artifact verification on cache hits. Successful artifact publication must obey the required-mode validation gate; a post-build check alone is insufficient for final A3 acceptance.

## Environment and project discovery

Fixed an observed compatibility failure caused by forcing GOENV=off: a module-off binding into a package under a saved GOPATH failed before the fix and passes afterward. The helper and GoML resolver now preserve persisted Go configuration while retaining explicit readonly/offline policy. `go test -race ./...` passes, including the regression.

Added `gomlc __ffi-build-context --go <program> --module-directory <directory> --output <file>`. It resolves the selected Go executable and target settings, obtains the module import path through `go list -mod=readonly -m -json`, and writes a context file with `module_import_path`. The live CLI test verifies persisted GOFLAGS, module identity and absence of go.sum writes.

Package-info protocol v1 now includes a `go_foreign` boolean, computed with the existing AST parser after a cheap text prefilter. Tests reject false positives in comments and string literals. The project graph preserves this flag across package/test merging and toolchain graph transformation, enabling the upcoming driver integration to avoid Go context resolution for checks with no FFI declarations. Existing stage0 drivers ignore the additional package-info field; the new driver diagnoses compilers that do not provide it.

Formatted both `gomlc` and `goml`. `just make` and `just ci` passed; final CI has 765 compiler tests and 78 driver tests, plus helper race tests, stage0 compilation, fixed point and release smoke (`/tmp/goml-ffi-environment-ci.log`). Ordinary project commands still do not schedule FFI verification.

Before wiring ordinary executable builds, audit helper caller loading against the existing Go file-list build mode: validation must not consume stale generated Go output or unrelated Go files in an artifact directory. Library/package callers and executable/file callers may need distinct explicit loading modes.

## Explicit caller loading mode

The helper protocol now requires `caller_context.load_mode`. Package mode loads the directory package; files mode loads only explicit overlay witnesses with the `command-line-arguments` identity. Both modes retain the actual caller directory for Go import legality. The GoML model, encoder, internal artifact-check CLI and release smoke request carry the field. The world identity includes the mode. A regression confirms that files mode ignores an invalid stale generated file without changing it, while package mode diagnoses that file.

Formatted `gomlc` and ran `just ci`: 765 compiler tests and 78 driver tests passed, along with helper race tests, stage0 compilation, fixed point and release smoke. Log: `/tmp/goml-ffi-caller-mode-ci.log`. Ordinary project commands remain to be integrated.

## Source package publication gate

Package `check`, `build`, `test-check` and `test-build` now accept the explicit validation options used by the artifact-check command. Parsing rejects partial or repeated validation options and invalid caller modes. The shared validator checks the newly compiled interface's full private/public binding metadata before entering the artifact output transaction. Required failures therefore prevent first publication and leave previous interface/Core files intact; Off emits the same unverified warning without reading the context file.

The stage0 compatibility path still accepts package commands without these options. Ordinary project commands must now supply them, and cache hits still need independent verification; this intermediate gate alone does not satisfy A3. The new integration test exercises all four package commands with an unused invalid private extern, a valid replacement, a failed rebuild over existing output, and explicit Off with a missing context file.

Verification: the initial CI run passed 766 compiler tests and exposed a binary-as-UTF-8 read in the new publication test. Corrected the test to compare bytes and require Core presence for build modes. The two focused tests then passed. Final `just ci` passed with 767 compiler tests and 78 driver tests, helper race tests, stage0 compilation, fixed point and release smoke (`/tmp/goml-ffi-publication-final-ci.log`). No generated golden files were changed.

## Ordinary project driver integration

`goml check/build/run/test` now parse `--ffi-check required|off` with Required as the default. A plan with foreign declarations captures one explicit Go build context; a no-FFI check does not resolve Go. Test-only source discovery is included. For each compiled package, the driver collects every actual linked caller directory; shared dependencies are checked for all their executable/test callers before source artifact publication. `--additional-callers` carries the additional file-mode directories as a JSON array.

Fresh GoML compilation cache entries undergo a separate artifact validation request before they are accepted. Link/test-link inputs are also checked in the final caller directory before executing or accepting a cached link. No persistent verification flag is consulted. GoBuild nodes with FFI always execute. Required final builds use the internal `gomlc __ffi-go-build` command, which consumes the same captured context and applies readonly/offline policy, selected toolchain, target, flags and tags. Off warns and avoids resolving Go for checks.

The focused project tests verify cached source rejection after a Go signature change, first-publication refusal for unused private bindings, build/run/test behavior, Go-body freshness, no-FFI checks without Go on PATH, and CLI defaults/errors. The initial test run exposed an incorrect expected artifact basename, corrected to the canonical package identity; all five initial focused tests then passed. Further tests cover Off without Go and multiple caller mapping. Complete CI evidence follows after verification. Source provenance diagnostics, remaining A2 audits, export-go and B/C/D remain outstanding.

Final verification for project integration: formatted both modules; `just make` passed; five initial focused project tests passed; final `just ci` passed with 767 compiler tests and 85 driver tests, helper race tests, stage0 compilation, fixed point and release smoke (`/tmp/goml-ffi-project-ci.log`). Ordinary project commands now exercise required FFI verification; this does not complete the export or later batch requirements.

## GoML source locations for binding diagnostics

Extern AST declarations retain their name token span. Source compilation and cache-hit artifact validation resolve stable binding IDs to readable source declarations and use the existing diagnostic renderer for file/line/column labels. Per-binding helper errors retain their code, identity and actual Go signature, with Go target, parameter-name mapping and return positions added to the source label. Cached Core validation can also use recorded source paths. Missing or unparseable source files fall back to the original binding diagnostic; no host paths were added to interface hashes or the helper request.

Tests cover exact declaration name spans, canonical package isolation, missing sources, protocol location attachment, and source labels on both first compilation and cached project failures. Full CI evidence follows after verification.

Verification: formatted both modules and ran `just ci`, exit 0: 768 compiler tests, 85 driver tests, helper race tests, stage0 compilation, fixed point and release smoke passed (`/tmp/goml-ffi-source-ci.log`). A manual invalid-private-binding check also displayed the exact `main.gom` name span with a caret and parameter/result mapping. No golden file was changed.

## Go export frontend

Started A4 by recognizing the existing generic attribute syntax as `go_export` declarations. AST lowering rejects the attribute outside a direct top-level ordinary function, including nested methods and field attributes that do not survive into HIR. TAST validates the exported ASCII name, duplicate attributes, public/non-generic/runtime/non-test function requirements, the scalar/fixed-array parameter boundary, and unit/flat-tuple return boundary. Type aliases normalize through the existing type environment. Duplicate Go names are checked across all files in one package.

Added positive and negative AST/TAST tests for placement, names, declaration capabilities, all numeric primitives, aliases, arrays and rejected text/container/function/tuple forms. Export metadata persistence, export roots, library emission and `export-go` remain outstanding; this frontend step does not create a callable Go API. The canonical guide documents this intermediate limitation explicitly.

Verification: fixed a test passing HIR lowering results instead of HIR files. The first executable focused run passed all four TAST groups and exposed an AST test expecting a placement diagnostic for syntax already rejected by the parser; the test now verifies that parser rejection separately. Final `just ci` passed: 773 compiler tests, 85 driver tests, helper race tests, stage0 compilation, fixed point and release smoke (`/tmp/goml-go-export-frontend-ci.log`). No generated golden files were changed.

## Go export artifact metadata

Artifact package exports now contain a separate `go_exports` collection, shared by interface and Core payloads. Each descriptor records canonical package/function identity, Go name, resolved function type, a source basename and declaration span. Plain public functions do not receive descriptors. Exported callable signatures are normalized to the checked signature, so transparent aliases have one consistent bridge type.

The interface hash includes export identity/name/signature but excludes debug file/position fields. Wire encoding orders descriptors by Go name. Decoders reject duplicate names/symbols, unsupported types, missing or inconsistent function targets, generic/comptime/non-function callables, invalid origins and mismatched owning packages. Interface exports must also occur in the public name table.

Interface JSON format is 25 and its binary value section is 8; Core JSON format is 24 and its binary value section is 4. Compiler-world metadata format is 20. Stage0 remains unchanged. Tests exercise alias normalization, interface/Core round-trips, name-sensitive hashes, position-insensitive hashes, duplicate metadata and forged signatures. The first focused run exposed the use of comptime-only TAST position fields; collection now uses the HIR declaration name span.

Verification: the full compiler run passed 772 tests and reported only two old schema-dependent hash assertions; both were updated after reviewing the new schema. The basic interface hash was independently recomputed with Python SHA-256 before the run. Final `just ci` passed with 774 compiler tests and 85 driver tests, helper race tests, stage0 compilation, fixed point and release smoke (`/tmp/goml-go-export-metadata-ci.log`). No generated golden files were changed.


## Internal Go library linking roots

The library linker reuses artifact dependency selection, validation, ordering and Core merging. It validates the selected package and export signatures, clears inherited executable roots, and installs only the selected package's export symbols before Core DCE and monomorphization. Dependency exports do not become roots automatically. Explicit root validation rejects missing, duplicate, ambiguous and non-concrete function signatures. Core generic parameters may be represented in signature types even when the explicit generic list is empty; the regression test exposed and fixed that case.

A binary-artifact roundtrip test links a package without main, retaining a cross-package private generic helper and a OnceCell static through ANF while removing unreachable ordinary functions and an unselected dependency export. The current static model uses package globals for OnceCell allocation and lazy get_or_init; it does not require calling main. Backend wrapper roots, Go package emission and CLI integration remain outstanding, so A5 is not yet fully accepted through the Go backend.

Verification: formatted `gomlc`; the focused library-root regression passed. Final `just ci` passed with 775 compiler tests and 85 driver tests, helper race tests, stage0 compilation, stage3 fixed point and release smoke (`/tmp/goml-go-library-roots-ci.log`). No generated golden files were changed.


## Go library backend and callable adapters

The existing backend now accepts an internal library target with explicit `(Core symbol, Go name)` declarations. It reuses representation lowering, runtime hooks, foreign wrappers and name allocation, emits no main function, and seeds Go AST pruning from its public wrappers. The printer supports an explicit package name while executable callers retain `package main`. Existing Go AST `Type::Multi` and `Stmt::ReturnMulti` represent real Go multiple results; tuple adapters unpack the internal struct once. Unit adapters discard the internal unit result. Scalars and fixed arrays retain their Go representations.

Generated top-level declarations and import aliases are checked for export-name collisions. Function, global and constant reachability now reaches a joint fixed point, retaining helper functions called by global initialization. This preserves the existing OnceCell package-global allocation; lazy initializers run only once across wrapper calls. No recover, goroutine switch or global FFI lock was introduced.

An external Go test package imports the generated library and calls scalar Add, a precisely typed multiple-result function, a fixed-array identity, a zero-result function, and persistent mutable static access. It also checks panic propagation. Focused backend tests cover import/type collisions and functions retained through global initializers. The initial two end-to-end tests passed. Additional focused tests and full CI evidence follow below.

Remaining A6 work includes isolating all generated internal Go names from the public package namespace and expanding import/collision audits. Public wrapper signatures already avoid internal tuple names. A7 CLI validation/output ownership and later batches remain unimplemented; the internal printer currently relies on its caller to validate the package name.

Verification: formatted `gomlc`; all four focused library tests passed. Final `just ci` passed with 778 compiler tests and 85 driver tests, helper race tests, stage0 compilation, stage3 fixed point and release smoke (`/tmp/goml-go-library-emission-ci.log`). Existing golden verification passed without regeneration.


## Go library public namespace isolation

After library pruning, an AST transformation assigns private generated identifiers to non-exported package declarations whose Go names would otherwise be public. It updates type references, expression references and matching local binders together, preserving shadowing. Field and method names and external import aliases remain unchanged. A first traversal reserves all existing identifiers before allocating private names, so a deliberately matching local name forces a distinct replacement. Executable and test targets do not run this transform.

The external Go test parses the generated file and checks its complete exported package namespace against the seven explicit API names. It also calls a Go math import through an exported GoML wrapper and verifies that an unused strings import is removed. A backend regression covers a local parameter shadowing an internal function, collision with the proposed private name, and a capitalized foreign import alias/selector. All five focused library tests passed. A7 command integration/output ownership, remaining import audits and later batches are still outstanding.

Verification: formatted `gomlc`; five focused library tests passed. Final `just ci` passed with 779 compiler tests and 85 driver tests, helper race tests, stage0 compilation, stage3 fixed point and release smoke (`/tmp/goml-go-library-private-ci.log`). No golden regeneration was needed.


## Internal library compiler command

`__link-go-library` exposes the existing artifact linker and library backend to the future project-driver command. The strict parser checks required/singleton options, duplicate inputs and legal non-main Go package names. It selects the dependency closure before collecting foreign bindings and validates them in the explicit generated-package caller context before writing output. A foreign closure without validation options is rejected; this command has no implicit compatibility fallback. The artifact FFI collector is shared with existing artifact-check commands.

CLI regressions exercise actual compiler dispatch, Core decoding, generated package/export names, invalid-name rejection without replacing previous output, rejection when foreign validation is omitted, and successful Required verification. Parser tests cover invalid and repeated arguments. `just make` initially encountered an executable-in-use installation error while formatting; after formatting finished, `just make` passed. Public `goml export-go`, output manifest/ownership, staging and final Go package verification remain outstanding.

Verification: formatted `gomlc`, rebuilt stage2, and passed all seven focused library tests. Final `just ci` passed with 781 compiler tests and 85 driver tests, helper race tests, stage0 compilation, stage3 fixed point and release smoke (`/tmp/goml-go-library-command-ci.log`). No golden regeneration was needed.


## Driver export output ownership and publication

The project package now contains an internal output publisher for the forthcoming `export-go` flow. It creates an exclusive `.goml-export-lock` directory, checks existing output ownership before invoking generation/validation, and rechecks both files immediately before publication. It only manages `goml_generated.go` and `goml_exports.json`; handwritten files are preserved. Existing symlinks and non-regular managed files are rejected. Lock contention fails immediately and does not remove another generator's lock.

The deterministic manifest records generator/version/protocol, package/import identity, API and prerequisite values supplied by the producer, the generated source digest and the manifest filename. A checksum covers the manifest payload; canonical encoding and the checksum detect modifications to the manifest itself. Unowned, incomplete or edited output is rejected. Staged files are renamed only after production succeeds; a reported failure publishing the second file restores the previous source or removes a newly created source. This does not claim crash-atomic publication of two files across process termination or power loss.

Four focused tests passed: deterministic regeneration and handwritten-file preservation; source/manifest edit rejection and validation failure preservation; changes made during validation; actual concurrent lock contention; and rollback after a missing staged manifest causes the second rename to fail. The initial test compilation required replacing Option debug assertions with boolean comparisons. Public CLI orchestration, module/path validation, compiler export API transfer and full staged Go-package checking remain outstanding. The producer callback is responsible for completing validation before returning its product.

Verification: formatted the affected `goml` module and passed four focused publication tests. Final `just ci` passed with 781 compiler tests and 89 driver tests, helper race tests, stage0 compilation, stage3 fixed point and release smoke (`/tmp/goml-export-output-ci.log`). No golden regeneration was needed.


## Driver export path and module validation

An internal destination resolver now selects a source directory from the current module graph, rejects main and out-of-module sources, and resolves the output through its nearest existing ancestor without creating it. Parent traversal components are rejected before lexical normalization. Symlinks are resolved before containment checks; dangling links and non-directory ancestors fail. An intervening go.mod rejects output in a nested Go module.

The GoML module root must contain a regular go.mod. `go mod edit -json` reads its module declaration with workspaces, network lookup and automatic toolchain selection disabled; the output import identity must equal that module path plus the canonical relative output path. The output basename supplies the ASCII Go package name and cannot be main, `_`, a keyword or an invalid identifier. Import path components are checked for allowed ASCII characters, trailing dots and Windows reserved/short names, following the locally available x/mod path rules. Validation leaves go.mod/go.sum and missing output directories untouched.

Tests cover matching and mismatching identities, absent go.mod, missing output creation, traversal, nested modules, source selection, main rejection, invalid components, symlink escapes and dangling links. These checks remain internal until the public CLI is connected. Full generated-package validation and compiler API-to-manifest transfer are still outstanding.

Verification: formatted `goml` and passed three focused destination tests. Final `just ci` passed with 781 compiler tests and 92 driver tests, helper race tests, stage0 compilation, stage3 fixed point and release smoke (`/tmp/goml-export-paths-ci.log`). No golden regeneration was needed. Saved GOFLAGS/module-file overrides still need an explicit audit when connecting this reader to the selected build context.


## Compiler API handoff and staged Go package validation

The internal emission command now optionally writes a sorted protocol-1 export API document, describing public names, stable GoML symbols and Go parameter/result bridge shapes. Unit, tuple multi-results and fixed arrays use the public ABI rather than internal representation names. The two outputs are staging artifacts for the driver publisher.

A new `__check-go-library` command checks the final module/import identity and runs Go build with an overlay replacing the generated source at its final package path. Existing handwritten files remain visible; old generated contents cannot mask candidate failures. Tests cover successful replacement and first generation, invalid candidate code despite a valid old output, handwritten symbol collisions, and direct/indirect Go import cycles. They verify that output source, go.mod and go.sum remain unchanged. The initial eight focused library tests passed, including actual CLI API output; additional API shape assertions participate in full CI.

Root go.mod parsing in the destination resolver now clears GOFLAGS and disables GOENV for that read only, preventing a saved module-file override from changing which module declaration is read. The eventual build still receives the explicitly selected Go build context and verifies its module identity. Public CLI orchestration and final publisher integration remain outstanding.

Verification: formatted both affected modules, rebuilt stage2 and passed eight focused library tests. Final `just ci` passed with 782 compiler tests and 92 driver tests, helper race tests, stage0 compilation, stage3 fixed point and release smoke (`/tmp/goml-library-overlay-ci.log`). No golden regeneration was needed.


## Public export-go command integration

The driver now exposes `export-go [PACKAGE_DIR] --import-path ... --out ...`, reusing module discovery, reachable-package compilation, cache validation and compiler-world resolution. It connects destination validation, an exclusive output lock, the shared artifact build lock, selected-package library linking/API output, staged Go checking and the ownership publisher. Dry-run prints the commands without creating the output. Export artifacts live in their own export-go subtree. Relative explicit paths follow the working directory; an omitted source selects the module root.

Intermediate artifact compilation and initial linking use explicit Required/Off file witnesses at the destination directory. Final validation checks every foreign declaration again in package mode with the candidate source included. The helper protocol has an optional generated_source field for this bounded in-memory overlay; it is only accepted in package mode and participates in source hashing. This allows handwritten Go code to refer to generated exports even during first generation, without using old generated definitions to validate the candidate.

Initial end-to-end tests passed: public CLI generation, external Go import/test including handwritten code calling generated functions, deterministic regeneration, preservation after source errors and refusal to overwrite edits. Additional Off/unused-binding tests and complete CI follow. This connects the public flow; full plan acceptance, cancellation/portability audits and B/C/D remain outstanding.

Verification: formatted both affected modules, rebuilt stage2, and passed four public CLI end-to-end tests. Final `just ci` passed with 782 compiler tests and 96 driver tests, helper race tests, stage0 compilation, stage3 fixed point and release smoke (`/tmp/goml-export-cli-ci.log`). The lock order acquires the output lock before the shared build lock, so simultaneous requests for one output are rejected while artifact writes remain serialized. No golden regeneration was needed.


## Complete fixture and extracted-release export smoke

The exact Add/AbsPlusOne/Pair fixture from plan section 4 now lives under `tools/release/testdata/go-export`, with Go function-type assertions and executable output `42 4 7 8`. A driver test copies it to an independent temporary module, runs required check and public export, removes the GoML source and manifest, then runs ordinary Go tests and the executable. The focused fixture test passed.

Release smoke now copies the same fixture beside the extracted toolchain, uses the extracted driver/compiler to check and export twice, verifies identical generated digests, removes GoML sources and runs Go tests/execution with no GoML tool directories on PATH. No module download or go.sum creation is expected. The cold-bootstrap cleanup recipe was inspected: its paths contain no tracked files and its toolchain roots are ordinary output directories. The explicitly requested just test, just bootstrap and just ci sequence is running; completion evidence follows below.

`docs/ffi/acceptance-a.md` now distinguishes current tests from missing matrix scope. Batch A is not declared fully accepted, and B/C/D remain open.

Verification: formatted the affected driver module and the new fixture module. The focused complete-fixture test passed. `just test` passed with 782 compiler tests and 97 driver tests (`/tmp/goml-interop-full-test.log`); `just bootstrap` passed its cold rebuild and fixed point (`/tmp/goml-interop-bootstrap.log`); final `just ci` passed with the same test counts, helper race checks, fixed point and expanded extracted-release export smoke (`/tmp/goml-interop-release-ci.log`). Release smoke verified matching source/manifest digests and ordinary Go tests/execution after removing GoML sources. Stage0 metadata is unchanged; no golden regeneration was needed.


## Export ABI boundaries and active-load cancellation

The public export regression now assigns exact Go function types for every integer width, bool, f32/f64, nested fixed arrays, unit and a mixed tuple result. It executes signed/unsigned limits, native int/uint limits, floating-point zero/subnormal/infinity/NaN bit patterns, array value semantics and multi-result values. A separate public CLI regression introduces direct and indirect import cycles after a successful export and verifies unchanged generated source/manifest and lock cleanup.

The helper cancellation regression waits until a wrapped Go executable starts `go list`, then either cancels the context or lets its deadline expire. Both paths must return a failed binding and an `ffi-canceled` diagnostic matching the context error within a bounded wait. This test uses a shell wrapper and skips Windows; it does not claim Windows execution coverage.

Verification: formatted the affected GoML module and Go test source. Both focused public CLI regressions and the active-load cancellation/deadline test passed. Full CI completion is recorded below.

Final `just ci` passed with 782 compiler tests and 99 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-interop-acceptance-ci.log`). `git diff --check` passed. No production compiler change or golden regeneration was needed for these regressions. Batch A still has the explicitly listed audit gaps; B/C/D remain open.


## Selected Go build contexts and public destination failures

The public export regression now reuses one output and artifact directory while changing build tags, deleting/restoring the selected Go source file, and switching Linux/Windows amd64 targets. The selected alternate function first has an incompatible return type, then is corrected; a Windows-only invalid source is also corrected before a successful Windows build. Each failed regeneration preserves the previous source/manifest and releases its lock. Success records the selected target/flags. These are cross-target builds, not cross-target execution.

Another CLI regression rejects parent traversal, mismatched import identity, a main output package, a nested Go module, unowned generated source and a handwritten Add collision. It checks the specific diagnostic as well as preserved user files and manifests, no generated publication, no leftover lock and no go.sum modification. Symlink, concurrent-lock and replacement-rollback evidence remains in the lower-level tests.

Verification: formatted the driver module. The focused environment-switch test passed all eight successive exports; the focused destination test passed before adding specific diagnostic assertions. Full CI validates those assertions and is recorded below.

Representation audit: `tast/ffi.gom` and `go_backend/types.gom` agree on scalar widths, char→int32/rune, arrays, both slice kinds and channel direction. `go_backend/lower.gom` extracts/reconstructs marker data only for direct parameters/results; tuple fields do not receive marker adaptation, matching the checked bridge restriction. Existing pipeline 244 exercises string, flat multi-results and unit; module project048 adds array, slice and channel calls. The helper tests cover the remaining raw Go shapes. Full end-to-end coverage of every original wrapper case remains an explicit acceptance audit item.

Final `just ci` passed with 782 compiler tests and 101 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-export-environment-ci.log`). The public destination diagnostic assertions passed in that run. `git diff --check` passed; no generated golden files changed. A-I01–03, A-I08, A-E08 and A-B04 retain their explicitly recorded audit scope before Batch A acceptance.


## Source/Core equivalence and cross-artifact binding origins

A new test reads the shared Add/AbsPlusOne/Pair fixture, emits one Go library directly from source-compiled Core and serializes the same Core for a separate compiler CLI invocation. It deletes the temporary GoML source before the CLI reload, compares the API JSON documents, and compiles one external Go test importing both libraries with exact Go function types and expected results. Interface inputs remain covered by existing dependency-library and binary interface tests.

A second test compiles equal foreign declarations in two different packages, roundtrips both interface and Core artifacts, and combines all four inputs. It expects two canonical declaration identities after deduplication and checks that each recovered source-location diagnostic names the corresponding source and Go target. The helper's duplicate-witness tests separately cover fanout from a shared Go call check to all binding results.

Verification setup: formatted `gomlc`. The first focused invocation encountered an old empty artifact lock; a fresh target directory bypassed it, then exposed the missing GOML_TEST_COMPILER_WORLD setting in that manual command. Full `just ci` supplies the repository's compiler-world setup and is the authoritative verification run below.

Final `just ci` passed with 784 compiler tests and 101 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-artifact-equivalence-ci.log`). Both new tests passed with the standard compiler-world setup. `git diff --check` passed; no generated golden files changed. Acceptance rows A-I08 and A-B04 now have direct cross-artifact and API/result evidence.


## Dependency static initialization and legacy import runtime matrix

The public dependency fixture records OnceCell initializer order across leaf, middle and exported library packages in a shared state package. Separate Go test processes exercise root-first and leaf-first access, repeated calls and shared dependency state. The module root has a panicking GoML main that is excluded from the selected library. The focused dependency test passed.

The scalar-width runtime test now calls a real generic Go identity function for bool, all integer widths and both float widths before returning through the exported Go API. A separate runtime fixture covers string and char identity, empty-marker object identity, read-only and mutable slice wrappers with shared mutation, and bidirectional/send/receive channel wrappers. Existing pipeline/module tests cover array, unit and flat tuple calls.

The marker fixture exposed a real regression: raw artifact function signatures retain an unqualified local marker name while trait metadata uses the canonical package name. The FFI metadata bridge now qualifies a local marker against the declaring function's package when that exact trait exists, then applies the unchanged strict-empty-trait check. This does not select a same-named trait from an unrelated package. A TAST assertion and the private-marker CLI runtime fixture cover the fix; the canonical language guide records the resolution behavior. After rebuilding stage2, the focused legacy runtime test passed.

Formatted both affected GoML modules. Full CI completion follows below.

Final `just ci` passed with 784 compiler tests and 103 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-legacy-statics-ci.log`). The qualified-marker TAST assertion and all integer/float roundtrip, legacy-wrapper and dependency-initialization runtime checks passed. The language comparison table no longer says library emission is unfinished. `git diff --check` passed; no generated golden files changed. All rows of the Batch A runtime matrix now have recorded evidence; the task-by-task completion audit remains the gate before starting B.


## Batch A acceptance gate closed; B0 contract opened

Rechecked plan A0–A9 against actual orchestration, codecs/hash views, helper protocol/host build, explicit roots/backend, output publisher, tests and distribution scripts. The task evidence table is in acceptance-a.md. Inspected cold-bootstrap cleanup paths again: generated directories contain no tracked files, and stage0 metadata was not changed.

Current-tree `just test` passed with 784 compiler and 103 driver tests (`/tmp/goml-a-final-test.log`). `just bootstrap` passed the cold stage0 rebuild and stage3 fixed point (`/tmp/goml-a-final-bootstrap.log`). Final `just ci` passed with the same test counts plus helper race checks, archive smoke and fixed point (`/tmp/goml-a-final-ci.log`). The combined process exited 0. No golden files were regenerated. Batch A is accepted; this does not complete plan.md.

B0 now has an explicit contract in external-types.md. It separates declared GoML spelling, normalized Go alias identity and build provenance; requires pre-typecheck metadata orchestration; preserves generic type arguments and method sets; and records the parser/typing/artifact/backend/bootstrap regression sequence. No B syntax or external types are implemented by that document. The next implementation step is the helper type-query protocol and its tests.


## B0 helper concrete type-query protocol

Added optional type_queries/type_results to the helper's v1 protocol, with combined request/response count limits and shared argument complexity limits. Type alias witnesses run in the same Go package load as function-call witnesses. Results distinguish declared alias/defined type from the unaliased concrete type graph and preserve generic arguments and methods. Query-local failures remain separate from valid queries and function bindings; cancellation produces failed type results and process status includes type failures.

Contract tests preceded implementation and initially failed because the query fields did not exist. Focused tests now pass for Duration identity across duplicate requests and an alias, primitive aliases, named interfaces, constrained generic instantiation with methods, invalid/missing/private/non-type targets, invalid constraints/arguments, mixed success, cancellation and file callers. Protocol tests cover roundtrip, duplicate IDs, unknown arguments and combined bounds. `go test -race ./...` passed for the helper. Full CI completion follows below.

This is an initial B0 implementation slice: named external type arguments, generic declaration queries, GoML wire consumers, source declarations, type identity through compiler IR/artifacts and final backend integration are outstanding. B0 is not complete.

Final `just ci` passed with 784 compiler tests and 103 driver tests, helper race checks including the new query tests, stage3 fixed point and extracted-release smoke (`/tmp/goml-type-query-ci.log`). `git diff --check` passed. No GoML source or golden files changed in this slice. B0 remains in progress.


## B0 named arguments and generic declaration queries

Helper bridge nodes now represent named Go types with import path, object name and recursive arguments. Witness AST rendering allocates deterministic import aliases and preserves named types through nested containers and generic applications. Function-call witnesses can use the same nodes, so a Duration parameter can be checked against time.Sleep without erasing it to int64.

Explicit query_mode=declaration loads and describes an exported type declaration without requiring generic instantiation. Its result provides declared_type and omits the concrete type field; instance mode remains the default and rejects uninstantiated generics. Declaration queries reject supplied arguments, and unknown modes fail protocol validation.

Tests cover Box[time.Duration], nested Container[[]Box[Duration]], method/type-argument graphs, named parameter success versus int64 rejection for Sleep, malformed named nodes, and declaration success versus uninstantiated-value failure. The generic declaration contract test was added first and failed before implementation. Helper race tests passed; final CI also includes protocol mode assertions. GoML wire consumers and the declaration/type pipeline remain the next B0 work.

Final `just ci` passed with 784 compiler tests and 103 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-named-query-ci.log`). `git diff --check` passed. B0 helper named arguments and declaration-mode queries are implemented; GoML consumption and the remaining compiler pipeline are still outstanding.


## B0 GoML declaration request and response codec

Added declaration-only query/request types and encoder in gomlc/ffi, reusing the existing context and caller encoding. Decoder results retain the Go world identity, declared alias/defined kind, root graph reference and structured node table. Checks include bounds, expected IDs/count, failed/inconsistent results, declaration kind/name/package agreement, duplicate graph fields/IDs, required graph references and dangling references in nested argument/member/constraint lists. Cycles are preserved as graph references.

Unit tests cover valid declaration metadata, malformed graph references/tags, missing required references, unexpected identity/kind, inconsistent results and duplicate query encoding. Raw node payloads still need kind-specific semantic conversion when integrating TAST; this slice does not claim resolved compiler identities or generic instance encoding. Full CI was started, then identity/encoder assertions were tightened; final verification must use the formatted latest source.

Added driver orchestration for declaration queries using the installed helper, bounded UTF-8/protocol output checks and exit-status consistency. Empty query lists avoid process launch. The existing live driver fixture now queries time.Duration and io.Reader through the real helper, checks graph/result consumption and helper-missing/no-query behavior, and verifies no witness file is written. All latest GoML files were formatted with the pinned repository stage0 toolchain. The earlier CI run passed, and a final CI run is rebuilding the latest identity checks and live integration.

Final latest-source `just ci` passed with 785 compiler tests and 103 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-type-codec-final-ci.log`). The graph decoder unit test and real-helper Duration/io.Reader driver test passed. `git diff --check` passed; no golden files changed. B0 still requires source syntax, pre-typecheck scheduling and semantic identity through TAST/IR/artifacts/backend.


## B0 external-type syntax and formatting

Parser/CST now distinguish extern type from extern fn, accept uppercase declaration names and square-bracket generic parameters, and require a semicolon. Tests cover attributes/visibility, generic declarations, malformed bodies/alias targets/missing or lowercase names and recovery to the following function. The formatter uses the semicolon-item path and preserves stable attribute/declaration formatting.

Until the AST/HIR and type identity pipeline is implemented, AST lowering emits an explicit external-type-checking-unavailable diagnostic. This guard prevents a parsed declaration from silently disappearing and producing a successful compilation. The language guide's prose, comparison table and informal grammar describe this temporary syntax-only status.

Formatted gomlc and passed four focused parser/formatter/AST guard tests (`/tmp/goml-extern-type-syntax-test.log`). Full CI completion follows below. B0 remains incomplete; the next step replaces the explicit guard with a real AST/HIR declaration and pre-typecheck metadata resolution.

The first full CI run passed 788 compiler tests and 103 driver tests but failed the existing 0577_extern_go_unsupported diagnostic snapshot: the parser now correctly says it expects fn or type after extern. Ran the required `just update-golden` recipe (`/tmp/goml-extern-type-golden.log`), which passed formatter/pipeline/compiler golden generation and changed exactly that one .out line. No generated snapshot was edited manually. Final CI after regeneration is recorded below.

Final `just ci` passed with 789 compiler tests and 103 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-extern-type-syntax-final-ci.log`). The generated diagnostic snapshot now passes. `git diff --check` passed. B0 remains in progress; only the syntax/CST/formatter part is available, with explicit AST rejection until semantic integration.


## B0 AST/HIR external declarations

Added explicit ExternType items to AST and HIR with attributes, visibility, names, generic parameters and source-name spans. HIR allocates DefId and qualifies the declaration name using the current package; it adds external type names to its nominal-name resolution input. AST/HIR printers preserve the declaration shape. Public item collection, test-attribute rejection and TAST declaration/parameter/attribute validation handle the new item. Builtin resource loading rejects external Go types so this intermediate feature cannot silently alter the stage0 runtime contract.

AST checks go_type placement, one required attribute, two string arguments, nonempty import path and an exported ASCII Go object name. The former AST unavailable guard is replaced with real lowering. TAST now emits a source-located metadata-not-connected diagnostic, preserving the boundary until pre-typecheck metadata scheduling and semantic types are implemented.

Tests cover AST fields and pretty-print reparse, invalid/misplaced/duplicate attributes, canonical HIR identity and TAST rejection without metadata. The initial stage0 build identified one missing exhaustive builtin-resource match; it was added and `just make` then passed. Formatted gomlc; focused tests and full CI results follow below. No new extern type syntax is used in compiler or driver source declarations.

Focused external-type tests initially passed six cases and failed the TAST guard assertion because the exact-message helper did not allow the declaration-name suffix. Changed that assertion to the existing substring helper and formatted gomlc again. Final latest-source `just ci` passed with 792 compiler tests and 103 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-extern-type-ast-ci.log`). `git diff --check` passed. No additional generated snapshots changed in this slice. B0 remains incomplete: metadata scheduling, semantic identity, generic substitution, artifacts and Go emission are still outstanding.

## B0 normalized Go type templates

Added a typed GoTypeIdentity representation in ffi for basic/named/parameter identities and pointer, array, slice, map and channel shapes. Named identity keys contain only canonical Go import path, object name and normalized arguments; aliases normalize to their targets. Basic byte/rune synonyms normalize to uint8/int32. Type graph node IDs, source declaration spellings and Go world provenance do not enter identity keys. Named types terminate structural traversal, preserving nominal identity and allowing recursive Go definitions.

Declaration resolution produces a positional generic template with retained constraint graph references, declaration kind and underlying kind. Substitution handles nested arguments and generic aliases that reorder or wrap parameters. This is substitution only; it does not validate Go constraints or claim a valid concrete Go instantiation. The original metadata graph remains necessary for method sets, constraints and provenance. Unsupported anonymous structural identities and malformed/cyclic/unbound metadata return errors; normalization has depth and expansion limits.

Unit tests cover nominal Duration versus int64, alias normalization, helper-node renumbering, basic synonyms, recursive named generics, positional generic aliases, argument count and malformed graphs. The real-helper driver test additionally resolves Duration, io.Reader, an alias of Duration, recursive Box[T] and Reverse[A,B] = Pair[B,*A], then checks normalized and substituted identities. Compiler/driver sources continue to avoid new extern type declaration syntax. Formatted gomlc and passed the stage0 build; full CI results follow below. TAST scheduling and semantic integration, IR/artifact propagation and emission remain outstanding.

The first CI run found unsupported unwrap() calls in the new test helpers; replaced them with existing unwrap_or_else and explicit test failures. A focused rerun encountered an empty output lock left by the failed build. Verified no compiler/test processes remained, removed only empty output-lock directories, and reran the same tests successfully: three identity tests passed (`/tmp/goml-type-identity-test.log`). The final latest-source full CI was then started.

Final latest-source `just ci` passed with 795 compiler tests and 103 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-type-identity-final-ci.log`). The real-helper generic alias/recursive declaration test and all three identity tests passed. `git diff --check` passed. No golden regeneration was needed for this slice. B0 is not complete: these templates are not yet consumed by TAST, and constraint instantiation, IR/artifact preservation and backend emission remain required.


## B0 package metadata scheduling

Added source-associated ExternalTypeRequest/ExternalTypeInfo and ExternalTypeEnvironment in TAST. Pure request collection retains canonical GoML identity, DefId, spans, source path, attributes and generics; metadata association resolves normalized templates and checks declared versus Go generic arity. Duplicate declaration requests and missing/count-mismatched metadata fail recoverably.

Package compilation now invokes the helper before its TAST check for every external type declaration, including unused ones, using the command's selected context and caller. Additional callers are validated too. Helper failures retain declaration source labels. Package discovery recognizes real extern type declarations as requiring Go context, while strings/comments remain excluded. Empty request lists do not load Go or start the helper. With ffi-check off, type resolution still needs Go metadata and resolves its context from the caller's enclosing Go module instead of trusting an absent or stale context file.

The source-associated environment is prepared and checked at this boundary, but is not yet installed into TAST equality/inference; the existing semantic-unavailable guard remains. This slice does not accept external values or complete B0. Tests cover source/canonical identity association, arity/duplicate/count checks and CLI unused/missing types plus wrong arity for standard and local generic Go types in required/off modes. Both changed GoML modules were formatted. The first make found a missing assignment semicolon, which was corrected; the subsequent stage0 make passed. CI was started before the local off-mode context refinement; final latest-source verification follows below.

The initial CI used a compiler built before the off-mode context fix while reading the newer local generic regression, and reproduced the local import failure. It also observed a fixed-point diff because the source changed during that run. The initial compiler suite passed 796 tests; the driver suite passed 103 and failed the new local off-mode case. The run is terminal. Started a new full CI against the formatted final source, including module discovery and nonfatal diagnostic source reads; its result is recorded below.

Final latest-source `just ci` passed with 796 compiler tests and 104 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-type-scheduling-final-ci.log`). The CLI regression confirms missing unused types receive source labels and standard/local generic arity is checked in both required and off modes, without publishing a package interface. The source-associated environment test passed. `git diff --check` passed. No generated golden files changed in this slice. Semantic TAST type integration and the remaining B0 pipeline are still outstanding.

## B0 external identity representation through the compiler

Added explicit External(import_path, name, args) variants to TAST, Core, Mono and repr. Equality and unification compare exact Go package/object identity and recursively process arguments. Updated occurs checks, parameter/inference/error/projection traversals, substitutions, Core/TAST conversion, monomorphization and Lift detachment. External types do not enter the GoML struct/enum constructor namespace. Type printers show Go identity, and backend mangling includes the package, object and argument structure.

The shared artifact type codec has a separate TExternal tag with required identity and argument fields. Core and interface roundtrips preserve external arguments. The Go backend lowers these types to their original package selectors with structured applied-type arguments, collects type-only imports, and traverses applied types during library renaming/reachability. External layouts remain unknown to enum/size optimizations. It does not emit new Go defined types for these identities.

Focused tests found two bugs: unresolved type variables were being defaulted to Error before the concrete-type predicate inspected them, and ordinary pretty-printer wrapping generated a Go type-argument newline without a trailing comma. The predicate now inspects pruned types, and Go applied types use the fixed delimiter printer. Twelve focused tests then passed (`/tmp/goml-external-variants-test3.log`), including a generic unique.Handle[T] Core program roundtripped through binary artifacts, specialized, lowered through Lift/ANF, emitted and run as actual Go; an added Go function verifies the original Handle.Value method remains available. A type-only import test covers nested unique.Handle[time.Time] through library pruning/renaming. Added an additional interface codec roundtrip/malformed-identity test for the full CI run.

The package metadata environment is still not installed into source TAST checking; source extern type declarations retain their explicit guard. Metadata kind/method/constraint artifact integration and actual Go instantiation checks remain outstanding. The internal type/IR path is implemented here, not the complete B0 language feature. Formatted gomlc; stage0 make passed before final test refinements, and full latest-source CI was started. Results follow below.

Final latest-source `just ci` passed with 801 compiler tests and 104 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-external-variants-ci.log`). Both Core and interface binary roundtrip tests passed, including malformed external identity rejection, real Go compilation/execution and method-set availability. `git diff --check` passed. No generated golden files changed in this slice. Source declaration installation, external binding bridge shapes, complete metadata/artifact provenance and Go generic constraint validation still prevent B0 completion.

## B0 named foreign binding bridge

Added Named(import_path, name, arguments) to BridgeType. TAST external parameter/result types now produce named bridge shapes recursively; they are not collapsed to the underlying scalar. Binding equality/merge, helper request JSON and persisted interface/Core foreign-binding metadata preserve the Go identity and arguments. Named type arguments use the existing nested bridge restrictions and artifact depth/node budget. The selectable bridge checks nonempty import paths and exported ASCII Go names, rejecting malformed names before emission; builtin error and inaccessible underlying alias targets still need their later dedicated handling.

Tests cover identity/argument differences, valid/invalid selectable names, nested TAST bridge conversion, unresolved/unsupported arguments and interface roundtrips with malformed metadata rejection. The real-helper driver fixture now verifies time.Duration returns/parameters and Box[time.Duration] through generic factory/echo functions. It rejects replacing Duration with int64, changing the returned Box argument and instantiating Numeric[T ~int64] with bool. These are binding-witness constraint checks; they do not replace the still-required validation of arbitrary source type instantiations.

Formatted gomlc. The first stage0 build caught character access used for byte validation; changed it to byte_get, then just make passed. Full latest-source CI was started. The source external declaration environment still has to be installed into TAST checking, with complete metadata provenance and alias spelling support; B0 remains incomplete.

Final latest-source `just ci` passed with 804 compiler tests and 104 driver tests, helper race checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-named-bridge-ci.log`). The real-helper named argument/result and constraint regressions, bridge conversion/equality tests and interface metadata roundtrip all passed. `git diff --check` passed. No golden regeneration was needed. The complete B0/source integration remains outstanding.


## B0 source checking with resolved external types

Added a pure TAST entry point that installs the source-associated external type environment before collecting and normalizing package signatures. Monomorphic external declarations become aliases to their normalized Go identity, preserving defined-type nominality while allowing different GoML names for the same Go type to unify. The existing checking entry points retain the missing-metadata diagnostic. Unsupported raw representations and generic declarations still fail explicitly at this boundary; generic instances must first receive actual Go constraint validation.

Source tests cover cross-file identity conversion, rejection of integer conversion/literal construction/field access/struct construction, and external function signature plus alias normalization. The production command continues resolving declarations and retaining its semantic guard: opening publication before recording every declaration in artifact metadata would let a type-only cached package skip Go revalidation. Artifact provenance, cache revalidation and generic instantiation remain required before exposing this path through the CLI. No user-visible language acceptance changed in this slice.

Formatted gomlc; stage0 make and the initial focused source test passed. Full latest-source CI includes the added signature assertions; results follow below.

Final latest-source `just ci` passed with 805 compiler tests and 104 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-external-source-ci.log`). The source checking test includes normalized external function signatures and aliases. `git diff --check` passed. No golden files changed in this slice. B0 remains incomplete pending production artifact metadata/revalidation, generic constraints and remaining alias/raw representations.


## B0 persistent external type metadata envelope

Added `ffi::ForeignTypeMetadata`, a versioned envelope containing declaration queries and the complete Go declaration graph, including build-world provenance. Encoding orders node IDs and declaration/query IDs for deterministic map traversal. Decoding applies byte/nesting/count limits, query identity validation, existing graph-reference validation and normalized template resolution. Malformed envelopes, incompatible versions and inconsistent query/result sets return recoverable errors. Build-world provenance remains separate from normalized Go source type identity.

The real helper test now serializes and restores its metadata before checking Duration, io.Reader, aliases, recursive generic Box and the parameter-reordering Reverse alias. A unit test covers deterministic round-trip, provenance preservation, malformed envelope fields, duplicate query rejection and the empty envelope. This supplies the checked serialization boundary for the next interface/Core integration; no artifact field or CLI acceptance is claimed in this slice. Formatted gomlc and started full latest-source CI; results follow below.

Final latest-source `just ci` passed with 806 compiler tests and 104 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-type-metadata-ci.log`). The real-helper metadata restoration and malformed-envelope unit tests passed. `git diff --check` passed. No golden files changed. Interface/Core field integration and current-world revalidation remain the next required B0 work.


## B0 external type metadata in interface and Core artifacts

Both artifact unit models now carry optional full external type metadata. The interface codec includes it in the interface hash and its binary payload; the Core codec carries it independently of exports and source paths. Both decode paths invoke the versioned metadata validator. The interface attachment API validates metadata and recomputes the hash. Toolchain-world Core construction preserves metadata from its interface. Interface JSON version is now 26 and binary value-section version 9; Core JSON version is 25 and binary value-section version 5. Old incompatible binary sections fail recoverably.

The real-Go metadata test now round-trips the complete declaration graph through both binary artifacts with empty public exports and source-free Core input, checks interface hash participation and verifies rejection of a damaged graph. Existing constructors initialize absent metadata explicitly. Production source publication remains guarded until current-world artifact validation is wired; no CLI acceptance or cache revalidation is claimed by this slice. Formatted gomlc and stage0 make passed. Full latest-source CI is running; results follow below.

The first CI run passed the new real-helper artifact checks and fixed point but exposed four unit assertions retaining old format hashes. Updated those explicit unit-test JSON/hash expectations for the new schema; no generated golden files were edited. Final latest-source `just ci` passed with 806 compiler tests and 104 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-metadata-artifacts-final-ci.log`). `git diff --check` passed. Current-world cache validation and production source publication are still outstanding.


## B0 current-world artifact type revalidation

The artifact-check command now reloads external declarations from both interface and Core metadata before considering function bindings. Type-only artifacts therefore take the same validation path even with no GoML sources or exported functions. Required mode uses the recorded command context; off mode resolves a current Go context and still validates type metadata. Additional caller directories are checked as well. Diagnostics include the GoML package and saved declaration identity.

Added cycle-aware graph comparison that renumbers reachable nodes deterministically and ignores helper node IDs and build-world provenance. It compares declaration kind and the reachable type graph, including underlying types, methods and constraints, with bounded recursion/node counts. A changed declaration requires rebuilding the GoML package from source; saved provenance is not treated as proof that Go declarations remain valid. The unit test checks renumbering/world changes and an underlying-type change. The live CLI regression loads type-only interface and source-free Core artifacts in required/off modes, then changes a private Go alias target and requires a diagnostic in all four cases. Production source acceptance remains guarded pending final source/publication integration and generic instance validation. Formatted gomlc and started full latest-source CI; results follow below.

Final latest-source `just ci` passed with 808 compiler tests and 104 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-type-revalidation-ci.log`). Both new revalidation tests passed, including all interface/Core and required/off combinations against changed real Go source. `git diff --check` passed. Go-library direct input paths in `go_library_check.gom` and `go_library_command.gom` still need the same type checks before source publication is enabled; source TAST/publication and generic instantiation integration remain outstanding.


## B0 monomorphic source publication and Go library type validation

Package compilation now installs its resolved external type environment into TAST checking and signature extraction, then attaches every declaration query and the complete metadata graph to the generated interface and Core artifacts. Imported interface metadata is revalidated before source checking. Go library emission and generated-package checking also validate Core type metadata even when there are no foreign function bindings. A shared artifact-type entry point requires a caller context only when actual type queries exist.

The remaining legacy source FFI signature predicate now delegates external types to the existing named bridge validator. Monomorphic external values can flow across GoML packages and Go calls without changing their Go defined-type identity. Generic source declarations still fail at the explicit instantiation-validation boundary, and unsupported raw aliases remain diagnostic. Updated the language guide's FFI prose, example, ABI table, comparison table and grammar to describe the accepted monomorphic scope and remaining limitations.

Added a driver regression with two GoML packages binding time.Duration, an executed foreign-call round-trip and a private unused Go alias; changing only that alias must fail cached checks in required/off modes. Initial focused testing caught an obsolete process API in the test, then exposed the legacy FFI predicate, both corrected. Formatted gomlc and goml; stage0 make passed. Latest focused and full-suite results follow below.

The focused driver regression passed (`/tmp/goml-external-publication-test3.log`). Final latest-source `just ci` passed with 808 compiler tests and 105 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-external-publication-ci.log`). `git diff --check` passed. No generated golden files changed. B0 remains incomplete: query/LSP metadata integration, generic Go instance validation and accessible spelling for aliases targeting private Go objects remain outstanding, along with later batches.


## B0 concrete Go type instance protocol

Added GoML request/response codecs for concrete Go type instances and a bounded compiler helper invocation. Queries carry the declaration identity and concrete bridge arguments. Responses retain declaration metadata and normalized instance identities. Decoding reuses declaration graph/status validation, requires an instance root and compares the returned normalized identity with substitution of the declaration template using the requested arguments. Missing roots, wrong instances, duplicate fields and invalid arity are recoverable errors. The Go helper remains responsible for checking actual Go generic constraints.

The real helper test now requests recursive Box[time.Duration], the reordered Reverse alias and Numeric[int64], and rejects Numeric[bool] through Go constraint checking. A unit test covers missing, unknown, duplicate and incorrect instance roots plus arity mismatch. Formatted gomlc and stage0 make passed. This is the concrete instance protocol boundary; source generic declarations remain guarded until concrete-use collection, validation scheduling and artifact instance replay are connected. Full latest-source CI is running; results follow below.

Final latest-source `just ci` passed with 809 compiler tests and 105 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-type-instance-ci.log`). The real-helper generic instance/constraint cases and malformed-instance unit test passed. `git diff --check` passed. No generated golden files changed. Source instance collection, scheduling and artifact replay are still required before enabling generic external declarations.


## B0 persisted concrete instance requests and replay

External type metadata envelope version 2 now records concrete instance queries alongside declaration metadata. Decoding bounds nested bridge arguments, rejects duplicate instance identities and unknown declarations, and checks argument arity against saved declaration templates. Requests survive interface/Core serialization and participate in the existing interface metadata hash. Source compilation currently writes an empty instance list until use collection is connected.

Current-world artifact validation replays saved instance queries through the Go helper for every caller, in required and off modes, after declaration graph validation. The live source-free artifact regression now includes Numeric[int64], verifies its interface/Core round-trip and successful replay, then installs Numeric[bool] with unchanged valid declaration metadata and requires a Go constraint failure in both modes. Unit coverage includes instance round-trip, missing declaration, wrong arity, duplicate identity and malformed instance fields. Formatted gomlc; stage0 make and latest-source CI results follow below. Generic source declarations remain guarded pending collection and scheduling.

Stage0 make and final latest-source `just ci` passed with 809 compiler tests and 105 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-instance-metadata-ci.log`). Extended artifact tests passed, including persisted valid Numeric[int64] requests and Go rejection of Numeric[bool] with unchanged declaration metadata in required/off modes. `git diff --check` passed. No generated golden files changed. Source generic-use collection and scheduling remain outstanding.


## B0 source alias application collection

External type alias records can now retain shared application collectors. TAST records the original arguments, source path and current span immediately before expanding an external alias; signature normalization preserves the collector. This keeps Go constraint obligations observable even when an alias erases or rearranges its parameters. These are checking-session records, not a new serialized semantic identity; ordinary and decoded artifact aliases have no collector until explicitly attached.

A pure conversion step resolves concrete recorded arguments into deduplicated instance requests keyed by the declaration and original bridge arguments. Symbolic applications return a specialization-required diagnostic rather than being silently omitted. The new TAST test expands Constrained[bool] through an ordinary local alias to i64 and verifies that the bool request and source association survive, with repeated normalization deduplicated. Generic source acceptance remains guarded pending imported-declaration collector attachment, scheduling and symbolic specialization support. Formatted gomlc; stage0 make passed. Full latest-source CI results follow below.

Final latest-source `just ci` passed with 810 compiler tests and 105 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-external-applications-ci.log`). The argument-erasure collection test passed. `git diff --check` passed. No generated golden files changed. Import collector attachment, instance scheduling and symbolic specialization remain required before generic source acceptance.


## B0 imported external alias collectors

Added a pure TAST attachment step for external aliases loaded from dependency artifacts. It matches canonical GoML alias names to saved declaration queries, checks generic arity and the normalized target against Go metadata, then installs fresh shared application collectors. Collected dependency uses convert to concrete instance requests through the existing deduplication path.

The new cross-package checking test loads a generic Box alias without a collector, attaches saved declaration metadata, checks a remote::Box[i64] parameter/result and verifies the resulting Go instance request. An inconsistent alias target is rejected before use. This implements the dependency-environment attachment primitive; compiler-driver scheduling and persistence of instances collected from imported aliases still need integration before generic source acceptance. Formatted gomlc and started latest-source CI; results follow below.

Final latest-source `just ci` passed with 811 compiler tests and 105 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-imported-applications-ci.log`). The imported generic alias collection and metadata mismatch regression passed. `git diff --check` passed. No generated golden files changed. Driver scheduling and persistence of imported application requests remain outstanding.


## B0 source concrete generic application scheduling

The compiler driver now attaches imported alias collectors before dependency environment construction and combines current-package uses, imported uses and inherited instance requests. Canonical declaration/instance IDs are deduplicated with conflict checks. The driver reloads the combined declarations in one Go world, checks them against pre-check metadata, validates all concrete instances for every caller and stores the complete request set in the generated interface/Core metadata. Generic declarations now accept concrete source applications; unresolved symbolic applications retain a specialization-required diagnostic.

Cross-package testing exposed signature extraction without dependency aliases. Added dependency-aware package signature extraction that normalizes only the current package's exported signature records using the full dependency environment. This lets imported generic external types reach the existing named FFI bridge correctly without exporting dependency functions as current-package items.

The focused driver test passed for cross-package Numeric[int64] execution and rejection of Erased[bool], whose alias expansion loses its parameter but whose original Go constraint must still apply (`/tmp/goml-generic-source-test2.log`). The final test also binds Numeric under a local GoML name and converts between local/imported instances. Updated language-guide prose and comparison/ABI tables for concrete generic applications and the remaining symbolic limitation. Formatted gomlc and goml; stage0 make passed. Full latest-source CI is running; results follow below.

Final latest-source `just ci` passed with 811 compiler tests and 106 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-generic-source-ci.log`). The concrete generic driver regression passed with local/imported identity conversion, execution, cache reuse and rejection of an erased invalid argument in required/off modes. `git diff --check` passed. No generated golden files changed. Symbolic specialization, query/LSP integration and inaccessible Go alias spelling remain outstanding B0 work; later batches remain required.


## B0 symbolic application specialization primitive

Inspection confirmed that existing Core/Mono TypeInstance records describe GoML layout materialization, not external constraint obligations. Added a separate pure specialization entry point with function-owned external application records. It reuses the real function-specialization work queue and bindings, resolves application arguments for each reachable function instance, and returns the specialized Core file plus concrete application records. Repeated calls reuse the existing instance deduplication. Missing owners, unknown type parameters, error types and residual symbolic arguments fail recoverably.

The test attaches an erased external alias obligation to a generic function with integer and boolean calls, including a repeated boolean call, and requires two concrete records. This is the specialization primitive; TAST owner association, symbolic obligation artifact transport and driver invocation still need integration. Existing source checking retains its specialization-required diagnostic. Formatted gomlc and stage0 make passed. Full latest-source CI results follow below.

The first CI run exposed the legacy single-file Core convention: generic parameters remain in parameter/return types while the explicit generic list is empty. Owner validation now recognizes those signature parameters as well as explicit generics, matching the existing specializer. The focused specialization test passed (`/tmp/goml-symbolic-applications-focused.log`); final full CI was restarted.

Final latest-source `just ci` passed with 812 compiler tests and 106 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-symbolic-applications-final-ci.log`). `git diff --check` passed. No generated golden files changed. TAST owner association, symbolic application artifact transport and driver invocation remain outstanding before symbolic source applications can be accepted.


## B0 TAST function ownership for symbolic applications

External application records now include an optional function owner. TAST scopes this owner around top-level function signature normalization and body checking, restores it afterward, and lets closures inherit the enclosing function. Method bodies remain unowned until their Core naming/trait identity is mapped explicitly. A pure partitioning API separates concrete requests from deduplicated symbolic records with owner, original declaration, arguments and source location; unknown inference variables, error types and ownerless symbolic applications fail recoverably.

The new test checks an erased external application in a generic function signature and in a local closure, plus a concrete top-level alias, and verifies that ownership does not leak across scopes. It also checks one concrete request and two distinct symbolic owners after deduplication. Production source checking still rejects symbolic applications while artifact transport and driver integration are unfinished. Formatted gomlc; initial stage0 make passed and full latest-source CI is running; results follow below.

The first CI attempt exposed insufficient stage0 inference for the symbolic-record vector before its first insertion. Added an explicit Vec[SymbolicExternalApplication] annotation and reformatted gomlc. Final latest-source `just ci` passed with 813 compiler tests and 106 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-application-owners-ci2.log`). `git diff --check` passed. Symbolic application artifact transport and driver integration remain outstanding; source acceptance is unchanged.


## B0 symbolic application Core artifact transport

Moved the shared external application record and owner/type-parameter validation into Core, so artifact decoding and Mono specialization use the same rules. Core artifacts now carry an external_applications vector independently of normalized runtime types. Each record preserves the function owner, canonical declaration query and symbolic Core arguments. The binary value section advances to version 6 and diagnostic JSON to version 26; clean bootstrap stages consume their own artifacts.

The decoder bounds record/argument counts, checks owner and parameter scope, requires matching saved Go metadata and checks declaration arity. Extended the live Go metadata artifact regression to round-trip Box[T] with an erased runtime signature and reject missing owners, unknown parameters, wrong arity, conflicting Go paths and unknown declarations. The existing Mono specialization test now uses the shared Core record. Production constructors still start with empty records pending source-driver integration; this does not yet enable symbolic source applications.

The initial stage0 build rejected a same-name cross-package type alias as cyclic. Removed that redundant alias and reference the Core record directly throughout Mono. Formatted gomlc; final validation results follow below.

Next integration must cover check as well as build: artifact_package currently lowers Core only when build is true. Merely storing obligations in emitted Core would leave generic calls unchecked in goml check. The driver needs an explicit checking path for symbolic obligations, and linker collection must preserve records alongside dependency Core before specialization.

Initial full CI passed 106 driver tests and fixed point but failed two compiler tests: the shared live metadata test assumed Box even for the Numeric fixture, and a Core JSON digest still represented the previous format. The test now selects an actual single-parameter declaration from its supplied metadata; updated the ordinary unit-test digest for Core version 26. No generated golden files were edited.

The corrected focused source-free metadata test passed (`/tmp/goml-symbolic-core-focused.log`). Final latest-source `just ci` passed with 813 compiler tests and 106 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-symbolic-core-final-ci.log`). `git diff --check` passed. Symbolic records now have checked Core artifact transport; production collection, check/build scheduling, linked specialization and source diagnostics still need integration before enabling symbolic source applications.


## B0 linked specialization and Go constraint validation

Mono now exposes a checked lowering entry point that returns concrete external applications from the same specialization pass that produces Mono. Ordinary lowering uses the same implementation with empty records. The linker validates record ownership before dead-code elimination, filters records to surviving owners, lowers once, constructs deduplicated concrete instance requests and checks them against each originating unit's saved/current Go metadata before output. Required and off modes both validate external constraints.

Go library emission uses this path. Executable and test linking also use it and accept the existing optional FFI caller/context arguments while preserving calls without those options for artifacts without obligations. The project driver's forwarding and cached-link handling remain to be connected before source symbolic applications can be enabled. This is not yet a claim that goml check handles symbolic calls.

Added a source-free cross-package regression: an exported Go library calls a generic helper carrying a Numeric[T] obligation, while an unreachable function calls the same helper with bool. Repeated valid i64 calls succeed and execute from Go; a reachable bool call fails in required and off modes and preserves the previous output. The expanded regression covers executable linking and execution too. The initial focused test compile needed an explicit BytesUtf8 trait import; the corrected Go library regression passed (`/tmp/goml-linked-specialization-focused2.log`). Stage0 builds passed; expanded and full validation results follow below.

The executable extension exposed a fixture root assumption: ordinary Core lowering marks source functions as roots, and executable linking preserves dependency root flags. Making a helper private does not clear those flags. The source-free helper fixture now explicitly uses without_roots, so the invalid unused call is actually unreachable while the entry still reaches erase through its call. This tests obligation pruning against the real root model without changing existing executable root semantics.

The corrected expanded regression passed for Go library and executable paths (`/tmp/goml-linked-specialization-focused5.log`). Project scheduling inspection confirmed that verify_foreign_cached_command runs both before fresh links and on cache hits, but currently invokes declaration/concrete-instance artifact checks without reconstructing symbolic specialization. The next driver integration must add specialization to cached validation as well as forward caller arguments to fresh link/test-link commands.

Final latest-source `just ci` passed with 814 compiler tests and 106 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-linked-specialization-ci.log`). Formatted gomlc and `git diff --check` passed. No generated golden files changed. Source collection/publication, check-mode specialization, driver forwarding/cache validation and original-source diagnostic provenance remain outstanding before symbolic source applications can be enabled.


## B0 production and cached link scheduling

The project driver now forwards the FFI caller/context arguments to fresh link and test-link commands. Cached links use new compiler validation-only entries, __ffi-check-link and __ffi-check-test-link, which share normal link preparation and specialization but return before Lift/ANF/Go emission and output writes. Artifacts without symbolic records skip specialization. Fresh links retain ordinary artifact verification without redundantly running the cached specialization entry first.

Expanded the source-free compiler regression to invoke both validation-only commands in required/off modes, accept concrete i64 instances, reject bool instances and assert that neither Go output nor the test manifest is created. That focused test passed (`/tmp/goml-cached-specialization-focused.log`). A driver regression traces real compiler invocations through a temporary proxy toolchain, checks context forwarding and confirms cached builds/tests use validation-only commands instead of emitting links again.

The proxy fixture needed an executable-relative lib link. It also needed a real _test.gom file: an attribute in the normal source alone did not cause the driver to plan an internal test group. With actual tests present, cached test compilation exposed a pre-existing duplicate-package interface rejection because it provides both production and test interfaces. FFI artifact validation now reads these snapshots individually, validates all saved type metadata and uses the existing binding merge to deduplicate or reject conflicting declarations. General package/interface loading still rejects duplicate packages. Compiler-side reads are reused within the validation request.

Both affected modules were formatted. Stage0 build and final regression/full CI results follow below. Source publication and goml check support for symbolic applications remain outstanding.

A scope re-audit against plan.md B0 is recorded in acceptance-b0.md. B0 explicitly permits recoverable rejection of unsupported shapes; it does not require unrestricted symbolic generic source applications before B1. Those applications remain documented as unsupported. After the current validation gate, implementation should proceed to the explicit B1 Ptr/method requirements rather than treating the broader generic-source extension as a mandatory B0 prerequisite. The full plan objective is unchanged.

The corrected driver regression passed (`/tmp/goml-cached-specialization-driver4.log`) for both fresh and cached build/test commands. Final latest-source `just ci` passed with 814 compiler tests and 107 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-cached-specialization-ci.log`). Both gomlc and goml were formatted; `git diff --check` passed. No generated golden files changed. B1 pointer and method binding is the next explicit plan batch.


## B1 pointer bridge protocol

Started the B1 contract in pointers-and-methods.md. Added Pointer(element) to the bridge model, equality, request encoding, interface binding codec and persisted instance argument decoding/identity. The Go helper accepts bounded pointer nodes and emits a real StarExpr type in witnesses. Nested marker values remain rejected and pointee nominality is retained.

Go helper tests passed for named/nested pointer parameters, nil pointer returns and wrong-value/wrong-pointee/wrong-depth rejection (`/tmp/goml-pointer-helper-test.log`). GoML tests cover nested pointer bridge identity and metadata argument round trips, interface binding round trips and rejection of pointer-to-any markers. The live instance test additionally resolves Box[*time.Duration] through the actual helper. Formatted gomlc; stage0 make passed (`/tmp/goml-pointer-bridge-make.log`). Full CI results follow below. Source Ptr, nil operations, method binding and resource fixtures remain B1 work.

Final latest-source `just ci` passed with 816 compiler tests and 107 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-pointer-bridge-ci.log`). This includes the actual helper instance round trip for Box[*time.Duration]. `git diff --check` passed; no generated golden files changed. The next B1 step is the distinct source/IR Ptr representation and explicit nil operations, followed by go_method receiver validation/emission and resource acceptance fixtures.


## B1 independent pointer type through the compiler

Added GoPointer(elem) to TAST, Core, Mono and repr. Type equality, unification/occurs checks, substitution, concrete/error/parameter walkers, projection normalization, interface/Core type encoding, printers, lowering, specialization and closure detachment preserve it independently of RefType. External Go pointer aliases normalize to this representation and typed foreign signatures use the pointer bridge. Go emission uses *T directly and recursively collects its pointee imports without generating Ref storage or deriving Ref equality/hash behavior.

Nullable Go pointers are excluded from the enum non-null pointer payload optimization, so Some(nil) remains distinct from None. The new source driver test passes a pointer alias across GoML packages and a generic function, captures it in a closure, mutates through an alias, checks pointer identity through Go, and distinguishes an optional nil pointer. It rejects Ref/integer conversions and layout construction. The first focused attempt used an unqualified expression constructor Some; corrected it to Option::Some. The focused driver test passed (`/tmp/goml-pointer-ir-driver2.log`).

Both affected modules were formatted and stage0 make passed (`/tmp/goml-pointer-ir-make2.log`). Updated docs/goml.md prose and comparison entry for supported pointer aliases and remaining direct Ptr/nil/method API work. Full latest-source CI results follow below. The public std::ffi::Ptr alias and builtin null/is_nil operations remain the next B1 integration step.

Initial full CI passed with 817 compiler tests and 108 driver tests (`/tmp/goml-pointer-ir-ci.log`). A subsequent name-generation audit found that the new pointer type-name prefix could collide with a user type in tuple names. Pointer type names now use the existing reserved hashed identifier mechanism, and a regression compares pointer-containing and user-type-containing tuple names. Final CI results follow below.

Final latest-source `just ci` passed with 818 compiler tests and 108 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-pointer-ir-final-ci.log`). Both affected modules were formatted and `git diff --check` passed. No generated golden files changed. Source Go pointer aliases now work end-to-end, including nullable enum payloads, cross-package identity, generic transport, closure capture and shared mutation. Public std::ffi::Ptr and builtin nil operations remain the next B1 step; go_method and resource acceptance fixtures are still required.

## B1 public pointer and nil operations

Added the hidden go_ptr language item and null/is_nil intrinsics, normalized to the distinct GoPointer representation. The packaged std::ffi module exposes transparent Ptr[T], null[T]() and is_nil[T](value), without adding them to the prelude. Direct calls lower to Go AST nil and equality; function values use typed callable bodies. The compiler's explicit standard package selector now includes ffi.

The driver regression passes actual Go *Cell values through Ptr[Cell] and the Go Handle alias, checks nil through both languages, calls is_nil as a function value, preserves Some(nil), and checks nested pointer nil values. Ref, integer and struct layout constructions are rejected. Focused test passed (`/tmp/goml-public-pointer-test2.log`). Added package selection and query navigation tests. Compiler/driver implementation sources do not use the new capability. Formatted gomlc, goml and lib/std; the driver reserves the builtin module name, so formatted changed builtin files through an isolated ordinary module with the same repository formatter.

Initial CI passed all 109 driver tests but rejected a new compiler test assertion because Vec[string] lacks Debug. The assertion is being changed to compare length and element separately. Full final CI evidence follows below. go_method and os.File resource acceptance remain required before B1 acceptance.

The second full CI passed 819 compiler tests and 109 driver tests. Pointer operation navigation and package selection passed. The remaining compiler failure was the existing explicit standard namespace list missing the new ffi child; updated that source assertion in catalog order. The final full run is `/tmp/goml-public-pointer-ci3.log`.

Final latest-source `just ci` passed: 820 compiler tests and 109 driver tests, Go helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-public-pointer-ci3.log`). `git diff --check` passed. No generated golden files changed in this step. Public Ptr and nil operations are now usable through packaged std::ffi, including query navigation. Next: go_method receiver representation, validation, artifact transport and emission, then the os.File acceptance fixtures.

## B1 Go method checker

The Go helper accepts additive call_mode=method requests with an empty package target and parameter zero as receiver. Witnesses use an explicit Go method expression, preserving the actual receiver method set without taking the address of a copy. Method requests enable Go type selection information; selected exported methods supply actual signatures and graph nodes. Ordinary package-function requests retain their existing validation path and load requirements. Duplicate method witnesses retain metadata for every binding ID.

Helper tests passed (`/tmp/goml-method-helper-test2.log`) for value and pointer receivers, pointer aliases, interfaces, promoted pointer methods, instantiated generic receivers, duplicate requests and missing/private/wrong-signature/wrong-pointer-depth rejection. A generated witness is executed with a nil receiver and confirms the method sees nil unchanged. Protocol tests reject missing receivers and unrelated package targets. Updated protocol-v1.md and the B1 contract. No GoML source changed in this step; go_method source declarations, IR/bridge/artifact transport and backend emission are still required. Full CI evidence follows below.

Final latest-source `just ci` passed with 820 compiler tests and 109 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-method-helper-ci.log`). `git diff --check` passed. No generated snapshots changed. Next is GoML method callable representation and its request/artifact/source/backend integration; B1 and the complete plan remain active.

## B1 GoML bridge and interface method transport

ForeignBinding now carries a typed ForeignCallMode. Request JSON preserves ordinary/method mode, and binding equality/merge conflict detection includes it. Interface metadata emits call_mode only for methods, retaining existing ordinary encoding; older ordinary records default to Ordinary when decoded. Decoding rejects unknown modes, empty symbols, missing ordinary package targets, absent method receivers and method package targets. Source diagnostic labels identify receiver methods without inventing a package target.

Added interface binary method round-trip and malformed receiver/target regressions, mode conflict coverage, and a real compiler CLI artifact check for time.Duration.Seconds plus a missing method. Focused CLI/interface tests passed (`/tmp/goml-method-transport-test.log`). Formatted gomlc and stage0 make passed (`/tmp/goml-method-transport-make.log`). Full CI evidence follows below. This transports binding metadata only; go_method source declarations, callable/Core representation and Go backend emission are still required.

Final latest-source `just ci` passed: 823 compiler tests and 109 driver tests, helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-method-transport-ci.log`). `git diff --check` passed. No generated snapshots changed. Next: GoMethod callable variants through TAST/Core/Mono/repr, callable artifact codecs and Go emission, followed by source attribute validation and full B1 resource fixtures.

## B1 source method callables and Go emission

Added GoMethod callables in TAST, Core, Mono and repr, preserving them through specialization, lifting, printing, queries and callable artifact codecs. The source go_method attribute binds the first parameter as receiver, rejects missing receivers, generics, invalid names and conflicting ABI attributes, and uses existing pure TAST bridge-shape checking. Imported callable artifacts reject invalid method symbols/package targets, missing receiver signatures and comptime capability; Mono validates receiver presence as well.

The backend emits a structured MethodExpression with an explicit receiver type, using a separate reserved wrapper name family. Go AST printing, privatization, reachability and DCE handle the new node. Calls retain the same argument/result wrapper behavior as ordinary FFI; nil receiver arguments are passed unchanged. External type imports are collected from the receiver signature.

The driver regression executes an imported public method through interface/Core artifacts, a generic identity function and a captured function value. Shared writes remain visible through aliases, pointer-to-value methods work, and nil-aware pointer methods see nil unchanged. A repeated run exercises cached artifacts. Negative declarations cover pointer-only methods on values, missing methods, wrong results, absent receivers, generics, mixed ABI attributes and comptime use. Focused test passed (`/tmp/goml-method-source-test.log`). Added malformed callable artifact regressions and updated docs/goml.md prose, example, comparison and informal grammar. Both source modules were formatted; stage0 make passed (`/tmp/goml-method-callable-make3.log`). Full CI evidence follows below.

B1 is not yet accepted: os.Open and File.Read/Close resource fixtures remain, with raw error/buffer foundations governed by B2/B3.

Final latest-source `just ci` passed with 824 compiler tests and 110 driver tests, Go helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-method-source-ci.log`). `git diff --check` passed and no generated snapshots changed in this step. Source go_method calls now work through package artifacts, specialization/lifting and Go emission. Remaining B1 resource acceptance requires raw Go error and buffer handling consistent with B2/B3; the complete plan remains active.

## B2 raw error and checked outcome foundations

Added the Go universe error named identity to bridge validation/witnesses, external alias normalization and artifacts. The hidden go_error language item normalizes to the existing external Go error type; nullary HIR type names now pass through nominal normalization. Std ffi exposes Error, nil_error and error_is_nil with direct Go nil/comparison emission. Generic error identities are rejected by protocol/artifact/Mono/repr validation.

Added NonNilError with checked construction and original-error extraction, and Outcome[T] preserving both value and error. The explicitly named into_result_discarding_value_on_error adapter returns Result[T, NonNilError]. An integration regression checks typed-nil versus nil, Go aliases of error, cross-package/generic/closure round trips, Error method dispatch, errors.Is chain matching, partial value/error preservation, Some(nil), Outcome conversion and invalid construction.

Testing exposed that standard package checking used complete environment exports, exposing private fields. User dependency checking now consumes each standard package's public interface; full layout metadata remains available to code generation. This closes direct NonNilError private-field construction. The final focused regression passed (`/tmp/goml-error-outcome-test3.log`). A nil Error cannot inhabit the NonNilError arm of Result. Formatted gomlc, goml and lib/std; changed builtin files were formatted through an isolated ordinary module because the driver reserves the builtin module name. Stage0 make passed (`/tmp/goml-error-outcome-make2.log`). Updated docs/goml.md and added errors.md. Full CI evidence follows below.

B1 resource fixtures and B2 public display/matching adapters, reverse conversion/boundary validation and actual Reader partial-read acceptance remain outstanding. This is not B2 acceptance.

Initial full CI passed all 111 driver tests and 808 compiler tests but all 16 pipeline shards detected the unused GoError placeholder struct in Go snapshots. A direct probe also showed that the hidden struct name could be constructed. Replaced the go_error placeholder with a language-item type alias, so it provides no constructible layout or generated struct. Normalization still produces the real Go error identity. Added rejection of GoError {} and unit-to-Error construction. Stage0 make and the final focused integration test passed (`/tmp/goml-error-alias-make.log`, `/tmp/goml-error-alias-test.log`). No snapshots were updated to accommodate the unwanted placeholder.

Final latest-source `just ci` passed: 824 compiler tests and 111 driver tests, Go helper checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-error-outcome-final-ci.log`). All golden checks passed without regenerating snapshots. `git diff --check` passed. Raw Error, checked NonNilError and explicit Outcome conversion are implemented; B2 and the full plan remain active pending the remaining adapters and resource fixtures.

## B2 reverse conversion and B1 resource acceptance

Added BoundaryError::NilError and Outcome::from_raw_result/from_result, with explicit caller-supplied failure values and Result-returning boundary checks. Err(nil) is rejected rather than becoming Go success; typed-nil errors remain failures. Outcome::into_tuple preserves both fields. The checked-wrapper adapter also passes through the raw boundary check. Failure values use normal eager argument evaluation, documented in the language guide.

The reverse conversion regression passes resulting tuples to actual Go functions and verifies success nilness, selected failure values, sentinel identity, typed-nil payload and the distinct Err(nil) contract error. Focused test passed (`/tmp/goml-error-reverse-test.log`). Added navigation coverage for the checked adapter.

The existing explicit MutSlice bridge supports B1 buffers without new implicit conversion. Added actual os.Open/File.Read/File.Close execution, alias invalidation after Close, nil File.Close behavior, shared byte storage and a Reader returning n > 0 together with io.EOF. Both the partial count/data and error survive; the named discard adapter retains the original EOF. The fixture runs twice through cached artifacts. Focused test passed (`/tmp/goml-file-resource-test.log`). Added the requirement-to-evidence B1 audit in acceptance-b1.md, pending final CI. Formatted gomlc, goml and lib/std; stage0 make passed (`/tmp/goml-error-reverse-make.log`). Full CI evidence follows below.

Final latest-source `just ci` passed: 825 compiler tests and 113 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-error-reverse-resource-ci.log`). The new adapter navigation test passed. `git diff --check` passed; no generated snapshots changed. B1 acceptance is finalized in acceptance-b1.md. B2 reverse Result conversion and real partial-reader coverage are complete; public message/matching adapters remain before B2 acceptance. B3/B4/C/D and the overall goal remain outstanding.

## B2 public matching and checked message APIs

Added error_matches and NonNilError::matches, implemented by the native Go errors.Is operation through the existing structured foreign wrapper machinery. Matching preserves error identity, chain traversal, custom Is behavior and nil semantics. Added NonNilError::message_bytes, which calls the original Error method and copies its bytes, plus message returning Result[string, utf8::Utf8Error]. Message decoding never substitutes invalid text and does not replace the underlying error. Std ffi explicitly depends on bytes and utf8; its package-selection regression was updated and navigation coverage added.

The integration test covers wrapped/custom errors, distinct objects with the same message, matching as a function value, nil/typed-nil behavior, valid Unicode, invalid UTF-8 and independent mutation of the copied message buffer. The first execution exposed an incorrect Vec storage name; emission now uses the lowered pointer element type. Focused test passed (`/tmp/goml-error-message-test2.log`). Both compiler/driver modules and lib/std were formatted; builtin intrinsic declarations were formatted in an isolated ordinary module. Stage0 make passed (`/tmp/goml-error-message-make2.log`). Updated docs/goml.md and added acceptance-b2.md with requirement-to-evidence mapping, pending final CI.

Initial CI passed all 114 driver tests and 825 compiler tests. The new navigation test stopped on its unused private helper diagnostic; made that fixture helper public so the test can validate API navigation without an unrelated warning. Final CI is /tmp/goml-error-message-final-ci.log.

Final latest-source `just ci` passed: 826 compiler tests and 114 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-error-message-final-ci.log`). Public message/matching navigation passed. `git diff --check` passed; no generated snapshots changed. B2 is accepted in acceptance-b2.md. Continue with B3 raw text/slice/map boundaries, then B4/C/D; the complete plan remains active.

## B3 raw Go string foundation

Added the hidden go_string alias language item and std::ffi::String, represented by a distinct external Go universe string identity. Go metadata basic string aliases normalize to this identity; bridge checking encodes it as the existing scalar string shape. Artifact, Mono and repr validation accept only nullary universe string/error identities. GoString remains separate from GoML Unicode string and the unit placeholder cannot be constructed as the raw type.

Added string_from_bytes (copy), string_from_text (immutable text conversion), string_bytes (independent byte copy), and string_text (Result with UTF-8 validation). Byte extraction shares structured backend emission with error message byte extraction. The integration regression executes invalid UTF-8 and NUL through a real Go alias/function, public child package, generic identity and captured closure, and repeats through cached artifacts. It checks copy isolation in both directions, valid text, empty strings and rejected implicit/direct construction. Focused test passed (/tmp/goml-raw-string-test.log); stage0 make passed (/tmp/goml-raw-string-make.log). Added query navigation and updated the language guide. Formatted compiler, driver, std and changed builtin sources. Full CI is /tmp/goml-raw-string-ci.log.

B3 remains unaccepted: raw slice/map and explicit copy/shared/trusted-readonly policies, rune validation, export text policies and the complete boundary matrix remain. B4/C/D and the overall plan remain active.

Final latest-source `just ci` passed: 827 compiler tests and 115 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-raw-string-ci.log`). Raw-string query navigation passed. `git diff --check` passed; no generated snapshots changed in this step. B3 remains in progress and the complete plan remains active.

## B3 rune and raw slice adapters

Added ffi::Rune = i32 with rune_from_char and rune_char returning Result[char, BoundaryError]. InvalidRune retains the exact invalid i32; negative values are rejected before unsigned conversion and the existing Unicode scalar validator rejects surrogates and values above U+10FFFF. The real Go rune alias/echo regression covers i32 extremes, invalid range boundaries, valid scalar boundaries, function values and cached execution.

Added ffi::RawSlice[T] = MutSlice[T], reusing the existing direct Go []T representation instead of introducing a second equivalent IR variant. Go slice aliases now normalize to MutSlice with recursively resolved element identity. Added nil/is_nil/capacity/append intrinsics and public adapters for explicit Vec sharing, shallow copies in both directions, and trusted read-only views. Append returns a new header; old headers remain unchanged. Nil is preserved by raw transport but empty copy adapters produce a non-nil empty allocation. No implicit Vec conversion, synchronization or recursive object copying is added.

The driver fixture tests Go alias identity, nil versus empty, capacity, shared writes, Go and GoML append, exhausted-capacity reallocation, Vec append retaining an old slice header, copy isolation, read-only views, and shared pointed-to objects with independently copied pointer slots. All four raw error/string/rune/slice focused regressions passed (/tmp/goml-rune-slice-test.log). Added additional reallocation and nil-copy assertions before full CI. Added query navigation and language guide API/policy tables and examples. Formatted compiler, driver, std and changed builtin sources. First slice build exposed an accidental expression-emitter branch in runtime collection; corrected that edit. Stage0 build then passed (/tmp/goml-slice-make2.log). Full CI is /tmp/goml-rune-slice-ci.log.

B3 still requires raw map support and explicit GoLibrary text export conversion policy before acceptance; B4/C/D remain outstanding. The overall plan remains active.

Final latest-source `just ci` passed: 828 compiler tests and 117 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-rune-slice-ci.log`). Rune/slice query navigation and the extended raw-slice regression passed. `git diff --check` passed. No generated snapshots changed in this step. Continue B3 with raw maps and export text policy; B3 is not yet accepted and the full goal remains active.

## B3 map bridge transport foundation

Added BridgeType::Map with separate key and element shapes to request encoding, equality, persisted generic instance arguments, and binary interface foreign-binding transport. Both child branches participate in depth/node accounting and nested marker rejection. The existing GoTypeIdentity::Map is retained for metadata normalization. The Go helper accepts the additive map shape with required key/element and rejects key metadata on other shapes. Witness generation emits ast.MapType and leaves key comparability to the Go checker.

Go helper tests pass (/tmp/goml-map-helper-test.log): legal map signatures, map JSON round trips, missing/extra key metadata, nested marker keys, missing values, key depth limits, and rejection of slice/map keys by real Go type checking. Stage0 make passed (/tmp/goml-map-bridge-make.log). Added compiler tests for key/value identity, map instance persistence and complexity budgets, malformed persisted arguments, and binary interface map round trips/marker rejection. An initial focused compiler run found a duplicated test helper introduced while adapting the pointer fixture; removed the duplicate before full CI. Compiler sources were formatted. Full CI is /tmp/goml-map-bridge-ci.log.

This is bridge infrastructure, not yet a public raw map implementation. Next add a distinct GoMap key/value IR type across typing, Core/Mono/repr, artifact codecs and Go lowering, then expose nullable/shared map operations and explicit shallow-copy adapters. GoML HashMap hashing/equality, storage and indexing must not be reused as the raw Go map ABI. Raw map operations, GoLibrary text policy and B3 acceptance remain outstanding, as do B4/C/D.

Final latest-source `just ci` passed: 830 compiler tests and 117 driver tests, Go helper tests, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-map-bridge-ci.log`). Both new map transport regressions passed. `git diff --check` passed and no generated snapshots changed in this step. Public raw map IR/APIs are the next B3 work; the full goal remains active.

## B3 independent raw map IR and nil transport

Added GoMap { key, value } to TAST, Core, Mono and repr. Structural equality/unification, occurs checks, substitution, type normalization, generic matching, public-name traversal, Core lowering/restoration, specialization, lifting/detachment, DCE type traversal, printers and validation preserve both children. TAST artifact encoding adds TGoMap with required key/value fields. Go lowering emits native map[K]V and uses a distinct hashed name family. Raw maps have pointer-sized layout but are excluded from the non-null enum payload optimization, preserving Some(nil).

The hidden go_map language item normalizes to GoMap; std::ffi exposes RawMap[K,V], map_nil and map_is_nil. Go map aliases normalize recursively to the raw map type and foreign bridges use the previously added Map protocol. Existing HashMap hashing, trait, indexing, allocation and runtime emission are not reused. No implicit container conversion or struct construction is accepted.

Stage0 build first found two missing layout branches; added the raw map layout handling and rebuilt successfully (/tmp/goml-map-ir-make2.log). The focused driver regression passed (/tmp/goml-map-ir-test.log), including a real Go map alias, nil/non-nil empty maps, Some(nil), public generic child package, generic identity, captured closure, native Go set/get with shared storage and repeated cached execution. Negative checks reject HashMap conversions, unit and direct struct construction. Added query navigation and language guide APIs/example/limitations. Compiler, driver and std sources were formatted; changed builtin files were formatted in an isolated ordinary module. Full CI is /tmp/goml-map-ir-ci.log.

Raw map entry/allocation/copy adapters and standalone key comparability checking remain before raw map acceptance. B3 also requires GoLibrary text policy, and B4/C/D remain. The full goal is active. Do not rerun /tmp/goml-add-map-ir.py: it is a one-time structural migration script and would duplicate branches.

Initial full CI passed all 831 compiler tests and 117 driver tests but failed the new hidden GoMap struct-construction negative case. The fresh compiler world exposed the placeholder struct constructor. Replaced the hidden generic struct with a go_map TypeAlias language item, matching the raw error/string placeholder strategy; generic normalization still produces independent GoMap IR. Revalidation follows.

The alias placeholder fix passed stage0 build/resource packing (/tmp/goml-map-ir-make3.log) and the repeated focused integration test including hidden/public construction rejection (/tmp/goml-map-ir-test2.log). Final full CI is /tmp/goml-map-ir-final-ci.log.

Final latest-source `just ci` passed: 831 compiler tests and 118 driver tests, Go helper checks, all goldens, stage3 fixed point and extracted-release smoke (`/tmp/goml-map-ir-final-ci.log`). The construction regression passes with the alias placeholder under a clean compiler world. `git diff --check` passed; no generated snapshots changed in this step. Raw map operations/copy adapters and key checking remain the next B3 work; the complete goal remains active.

## B3 raw map operations and shallow copying

Added map_new, map_len, map_get, map_set, map_delete and map_copy, backed by explicit go_map intrinsics and native Go map operations. Lookup's internal comma-ok result is wrapped as Option[V], preserving absent versus present zero/nil values. Insertion into nil retains Go's panic; lookup/delete/length retain Go nil behavior. Map copy preserves nil and allocates independent entry storage for non-nil inputs, while pointer values stay shared. Copy ranges over key/value pairs directly so NaN-keyed entries keep their values without equality-based lookup.

The integration fixture covers allocation, overwrite, length, missing/zero entries, captured mutation, actual Go reads, independent copied entries, shared pointees, Some(nil) pointer values, nil read/delete/copy, NaN entries and original nil-write panic. Repeated successful execution exercises cached artifacts. Initial test found missing declarations before MultiAssign in lookup emission; added typed item/present locals. Stage0 make passed (/tmp/goml-map-ops-make2.log) and focused test passed (/tmp/goml-map-ops-test2.log). Updated public API navigation coverage and language guide policy table. Compiler, driver, std and changed builtin sources were formatted. Full CI is /tmp/goml-map-ops-ci.log.

Standalone raw map key-constraint validation and related malformed-artifact cases still remain. B3 GoLibrary text policy, B4/C/D and the full goal remain outstanding.

Final latest-source `just ci` passed: 831 compiler tests and 119 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-map-ops-ci.log`). Public map operation navigation and nil/presence/shallow-copy/NaN/panic regression passed. `git diff --check` passed and no generated snapshots changed in this step. Continue raw map key constraints before B3 acceptance; the full goal remains active.

## B3 known map key comparability checks

Added TAST checks for definitively non-comparable raw map keys: Slice, MutSlice and GoMap, recursively through arrays, tuples and local struct fields with generic substitution. Signature checking rejects known bad keys and post-inference expression facts catch bad inferred generic applications. Go pointers to non-comparable pointees remain valid keys. Parameter/unknown external types are deferred rather than incorrectly rejecting valid Go keys.

TAST artifact decoding rejects directly non-comparable key shapes, and Mono/repr type validation independently rejects those structural cases. Added query regression cases for direct signatures, annotations, aliases, generic local structs, inferred calls and accepted pointer/array/scalar keys; added malformed artifact cases. The initial build used a nonexistent Mono resolve_type helper; Mono now checks its already concrete type directly. Stage0 build passed (/tmp/goml-map-key-make2.log); both focused tests passed (/tmp/goml-map-key-test.log). Compiler sources were formatted and language guide limitations updated. Full CI is /tmp/goml-map-key-ci.log.

This is not full key-constraint acceptance. External named keys used without foreign declarations, instantiated requirements originating only inside generic bodies, and named-shape revalidation still need coverage/implementation. B3 GoLibrary text policy and B4/C/D remain outstanding; the full goal remains active.

Final latest-source `just ci` passed: 833 compiler tests and 119 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-map-key-ci.log`). `git diff --check` passed and no generated snapshots changed in this step. Known-key source/artifact checks are verified; full external/deferred key constraints remain outstanding and the overall goal remains active.

## B3 external key comparability independent of generic arguments

Added bounded Go metadata comparability analysis over named/alias underlying types, arrays and struct fields, including private fields. Pointer/channel/interface/basic types are comparable; map/slice/signature types are not. Type parameters remain unknown; an independently non-comparable field dominates an unknown field. Missing nodes and cyclic value shapes produce recoverable errors. This reuses the existing Go metadata graph without exposing Go field layouts in public GoML APIs.

TypeAliasInfo carries a derived optional comparability result during checking. Local external aliases compute it from current Go metadata, imported aliases recompute it when their persisted/revalidated external metadata is attached, and ordinary aliases default to unknown. The derived flag is not trusted as independently serialized artifact metadata. TAST map-key checks match the original external type identity and reject definitively non-comparable named keys even without a foreign function declaration and with --ffi-check off. Generic declarations with an unconditionally bad field are also rejected.

Stage0 make passed (/tmp/goml-external-key-make2.log). The focused driver regression passed (/tmp/goml-external-key-test.log): imported private slice fields, named Go functions, zero-length arrays with slice elements, unconditionally invalid generic structs, accepted comparable structs/interfaces/pointers, repeated cached positive execution and required/off checking. Added metadata tests for known/unknown results, private fields, aliases and malformed/cyclic graphs. Compiler and driver modules were formatted and language guide limitations updated. Full CI is /tmp/goml-external-key-ci.log.

Comparability depending on concrete external generic arguments, transitive/deferred generic-body map requirements and complete named-shape artifact revalidation still remain. B3 text export policy and B4/C/D remain outstanding; the complete goal is active.

Final latest-source `just ci` passed: 834 compiler tests and 120 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-external-key-ci.log`). `git diff --check` passed; no generated snapshots changed in this step. External comparability independent of generic arguments is verified; dependent/deferred requirements remain and the full goal stays active.

## B3 parameter-dependent external key comparability

Replaced the checking-time optional comparability boolean with a GoComparability rule: Always, Never, Parameter(index), All(children), or Unknown. Metadata traversal preserves root type-parameter identities while following named/alias/struct/array nodes; pointer fields remain independent of pointee comparability. The existing known-result API remains as a reduction for tests and callers needing a definite boolean.

External map-key checking matches the normalized external alias template against the actual key type, obtains substitutions for its original generic parameter names, and evaluates the rule recursively against concrete arguments. This handles nested external types, aliases that reorder/erase parameters, and pointer-only parameter usage. Imported rules are still derived from revalidated persisted Go metadata instead of being trusted as a separately serialized flag.

Stage0 make passed (/tmp/goml-generic-key-make.log). The focused driver regression passed (/tmp/goml-generic-key-test.log), covering valid/invalid Box and nested Box, reordered/erased Go alias parameters, pointer-only generic fields, an external non-comparable argument, nested applications of the same constructor, required/off checks and cached cross-package execution. Added a metadata rule test checking parameter indices and pointer independence. Compiler and driver modules were formatted and the language guide updated. Full CI is /tmp/goml-generic-key-ci.log.

Deferred map-key requirements originating only inside generic GoML function bodies and their transitive artifact propagation remain before full key-constraint acceptance. B3 text export policy and B4/C/D remain; the full goal is active.

Final latest-source `just ci` passed: 835 compiler tests and 121 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-generic-key-ci.log`). `git diff --check` passed and no generated snapshots changed in this step. Parameter-dependent external key rules are verified for the covered source/cached paths; deferred generic-body obligations remain and the full goal stays active.

## B3 materialized struct key validation after generic specialization

Auditing the existing specialization path showed that concrete raw map keys already reach Mono validation, but the validator previously only checked nominal type existence. Added recursive key comparability checks against the materialized Mono struct fields, including hidden/nested fields, arrays and tuples. Pointer boundaries remain comparable and are not traversed as value fields.

The new regression instantiates a private generic function containing RawMap[Key[T], i64] through a public cross-package generic wrapper and a captured closure. Scalar, pointer-to-slice and Vec pointer-storage instances run successfully twice through cached artifacts. Slice, tuple-with-slice and array-of-slice arguments now fail at GoML Mono validation with the comparability diagnostic. This covers body-only requirements at build/link time without claiming that goml check already propagates those obligations.

Stage0 make passed (/tmp/goml-deferred-key-make.log); the focused driver test passed (/tmp/goml-deferred-key-test.log). Added an independent Mono validator regression for materialized nested/hidden fields and pointer boundaries. Compiler and driver modules were formatted; language guide limitations updated. Full CI is /tmp/goml-deferred-key-ci.log.

Full source-check propagation of deferred generic-body requirements, deferred external named-key validation, and the remaining B3 text export policy still require work. B4/C/D and the overall goal remain active.

Final latest-source `just ci` passed: 836 compiler tests and 122 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-deferred-key-ci.log`). `git diff --check` passed and no generated snapshots changed. Materialized struct key rejection is verified at build/link time; source-check propagation of deferred constraints remains, and the overall goal stays active.

## B3 explicit GoLibrary raw text policy

Enabled only the distinct nullary raw Go string identity (ffi::String) in Go export value validation and backend wrappers, recursively through fixed arrays and flat tuple returns. Ordinary GoML string/char exports remain rejected. ffi::Rune already uses the supported i32 ABI and can be explicitly checked with rune_char. The language guide now states the raw-byte/raw-integer policy, checked adapter failure-signature requirement, no-replacement rule, supported-type table and complete example before presenting the expanded export matrix.

The GoLibrary regression exports raw echo, a checked UTF-8 adapter returning (raw string, bool), a checked rune adapter returning (i32, bool), and a raw-string array echo. Native Go function assignments verify exact signatures. Go tests cover arbitrary invalid bytes, NUL, valid Unicode, invalid/valid rune boundaries, preserved failure values and array transport. Repeated generation is stable; attempts to export ordinary string/char are rejected without replacing prior owned output. Added TAST policy tests including malformed raw string identities. Stage0 make passed (/tmp/goml-export-text-make.log); focused export integration passed (/tmp/goml-export-text-test.log). Compiler and driver sources were formatted. Full CI is /tmp/goml-export-text-ci.log.

This advances the B3 text export requirement while deferred map-key source-check propagation and deferred external key validation remain unresolved. B3 acceptance is still pending, and B4/C/D plus the overall goal remain active.

Final latest-source `just ci` passed: 837 compiler tests and 123 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-export-text-ci.log`). `git diff --check` passed and no generated snapshots changed. Explicit raw-text GoLibrary export policy is implemented and verified; B3 remains pending on deferred map-key work, and the full goal stays active.

## B3 repeated local generic map key instantiations

Corrected source key comparability traversal to track concrete struct instantiations instead of nominal names. Nested Key[Key[RawSlice[i64]]] now reaches the inner non-comparable field. Pointer-only and phantom type parameters remain valid; array and tuple nesting retain their comparability requirements. The traversal rejects recursive struct declaration layouts before substitution can expand them and reports a recoverable diagnostic after 64 nested struct instantiations.

Added query regressions for nested invalid keys, accepted pointer/phantom/scalar cases, direct and expanding recursive declarations, and the depth bound. Compiler sources were formatted. Focused map_key tests passed (8 tests, /tmp/goml-nested-key-test.log). The initial stage0 build passed before the recursion guard; the subsequent build reached installation but collided with an already running compiler during the focused test (Text file busy). Full CI now rebuilds the current sources sequentially (/tmp/goml-nested-key-ci.log).

This closes a concrete source-check hole discovered while designing deferred requirements. Cross-package propagation of requirements originating only inside generic function bodies, deferred external key validation, B3 acceptance and B4/C/D remain outstanding. The full goal stays active.

Final latest-source `just ci` passed: 840 compiler tests and 123 driver tests, Go helper checks, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-nested-key-ci.log`). The sequential CI rebuild resolved the installation collision. `git diff --check` passed and no generated snapshots changed in this step. Nested concrete local map key checks are verified; deferred requirements and the overall goal remain active.

## B4 runnable file resource example

Added examples/ffi-file-read as a complete GoML module with a local Go shim and bundled three-byte input. It binds actual os.Open, io.ReadFull and File.Read/Close. ReadFull requests four bytes, giving a deterministic n=3 plus io.ErrUnexpectedEOF; Outcome retains the count and original error, and the shim verifies both after GoML sends them back. A pointer also passes through Go and back before explicit Close, and a subsequent read verifies that the original alias is closed. Shared buffer writes are copied over the returned range and UTF-8 is checked explicitly. Read and close errors remain separate. There is no adapter object registry or finalizer-driven cleanup.

The README explains execution, expected output, resource lifetime and the bounded-read limitation. The language guide links it. The driver regression copies the committed sources rather than duplicating the example and tests fresh/cached runs, complete reads, empty files and open failure. Both affected GoML modules were formatted and the Go shim passes gofmt. Standalone execution and the focused driver test passed (/tmp/goml-file-example-run.log, /tmp/goml-file-example-test.log). Initial full CI passed with 840 compiler and 124 driver tests (/tmp/goml-file-example-ci.log).

Extended the extracted-release smoke test to copy source-only example files into a fresh directory, check formatting and FFI validation, then execute twice with the extracted toolchain and offline dependency settings. This exercises the packaged raw string, pointer, error and slice APIs together. Final current-source CI is /tmp/goml-file-example-release-ci.log. B3 deferred checking work, remaining B4 audit and C/D still remain; the full goal is active.

Final latest-source `just ci` passed: 840 compiler tests and 124 driver tests, helper race tests, golden checks, stage3 fixed point and extracted-release smoke including the new file resource example (`/tmp/goml-file-example-release-ci.log`). `git diff --check` passed. No generated snapshots changed. B4 now has a runnable, automatically tested and extracted-toolchain-verified resource example; remaining acceptance work and B3/C/D are not marked complete.

## C0 function witness protocol foundation

Fixed the planned public spelling and conversion contract in callbacks.md: ffi::Func[F], func_nil, func_is_nil, func_from_closure and checked func_to_closure returning Option[F]. These APIs are not exposed yet. The contract requires preserving lambda lifting, typed adapters with strongly retained environments, explicit raw text/error policies, no registry/reflection/global lock and normal panic propagation. B3 deferred constraints and B4 audit remain tracked independently.

Added the helper's additive protocol-v1 function type, carrying required parameter/result arrays under signature. Validation enforces shape, nested type depth/node bounds and rejects marker payloads, including markers hidden inside generic arguments. Go witness construction uses ast.FuncType recursively, so real Go type checking verifies callback parameter/result signatures and imports nested named types. Existing protocol shapes remain compatible.

Added protocol round-trip/malformed/budget regressions and real Go witness cases for parameters, returned functions, unit, multiple results, nested callbacks and imported named types. Mismatched parameters, reordered/missing results and variadic-vs-slice functions are rejected. The initial fixture used a function name already present in the shared test module; renamed it to VariadicCallback. Helper tests passed (/tmp/goml-callback-helper-test2.log) and initial full CI passed (/tmp/goml-callback-helper-ci.log). Review then added the nested generic-marker rejection; repeated helper tests passed (/tmp/goml-callback-helper-test3.log). Final current-source CI is /tmp/goml-callback-helper-final-ci.log.

This implements protocol/type-checking groundwork only. The GoML BridgeType/identity and source type representation, persisted conversion requirements and bidirectional runtime adapters are still outstanding; C0 and the complete goal remain active.

Final latest-source `just ci` passed: 840 compiler tests and 124 driver tests, helper race tests including callback signature validation, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-callback-helper-final-ci.log`). `git diff --check` passed. No generated snapshots changed. This verifies the helper protocol foundation; source-level function types, conversion adapters and the remaining full plan are not complete.

## C0 GoML function bridge identity and persistence

Added Function variants to BridgeType and GoTypeIdentity with separate ordered parameter/result lists. GoML now emits the helper's function/signature protocol shape and preserves it through interface binary metadata and persisted instance arguments. Identity keys encode both lists separately to avoid parameter/result boundary collisions; generic substitution traverses both. Alias metadata containing non-variadic function signatures normalizes structurally, while generic function signatures and variadic shapes remain recoverable unsupported cases. Source-level external function values still reject unsupported raw representation pending the dedicated language/IR type.

Added function bridge identity/equality/round-trip/budget tests, metadata normalization and substitution tests, interface binary round-trip and marker rejection, and actual helper integration with a generic Go alias Callback[T] = func(T) (T, error). The integration verifies the GoML-emitted request and compares substituted declaration identity with the helper's concrete instance identity. Strict function signature decoding rejects extra fields and requires actual parameter/result arrays. Initial focused tests exposed legacy response_array accepting null as empty; function signatures now reject null explicitly.

Compiler sources were formatted. Stage0 make/resource packing passed (/tmp/goml-callback-bridge-make3.log). The repeated focused function_ tests passed, 25 tests including all four new cases (/tmp/goml-callback-bridge-test2.log). Full CI is /tmp/goml-callback-bridge-ci.log. No runtime callback conversion is claimed yet: raw source/IR function types and persisted typed adapter requirements still need implementation, along with remaining B3/B4/C/D work. The full goal remains active.

Final latest-source `just ci` passed: 844 compiler tests and 124 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (`/tmp/goml-callback-bridge-ci.log`). `git diff --check` passed and no generated snapshots changed. Function bridge/identity/artifact transport is verified against the real helper; source-level raw function values and runtime conversions are still outstanding, and the full goal remains active.

## C0 nullable raw function values across the compiler

Added a distinct GoFunction IR type carrying its signature through TAST/Core/Mono/repr, type equality/unification/substitution, artifact codecs, specialization, lambda lifting/detachment, ANF collection, layout and Go type emission. Go function results use native zero/multiple return values rather than the GoML unit/tuple storage ABI. Function identities and generated names are distinct from both Go pointers and ordinary GoML function types. Raw functions are non-comparable map keys and are excluded from non-null pointer enum niches.

Added the hidden GoFunc alias language item and go_func.nil/is_nil intrinsics, plus public ffi::Func[F], func_nil and func_is_nil. Supported Go aliases to non-variadic function types normalize to this representation. Source signature and post-inference checks reject concrete invalid signatures; artifact decoding and Mono validation independently reject malformed concrete function shapes and unsupported boundary types. Ordinary closures cannot implicitly become raw functions, and raw functions are not directly callable from GoML. Explicit closure adapters remain pending.

The driver integration obtains a Go closure with mutable captured state, transports it through a package generic and captured GoML closure, and invokes it in Go after transport. It verifies nil, Some(nil), shared capture state, unit and multiple results, cached execution and rejection of malformed signatures, implicit conversions, construction, direct calls and map keys. It passed (/tmp/goml-raw-function-test.log). Added API navigation, type artifact validation and backend signature/name regressions. Updated the canonical language guide and callback contract. All affected compiler/driver/std modules and changed builtin files were formatted. Stage0 make/resource packing passed (/tmp/goml-function-ir-make2.log); full CI is /tmp/goml-raw-function-ci.log.

The one-time migration scripts /tmp/goml-add-function-ir.py, /tmp/goml-function-ir-finish.py and /tmp/goml-function-surface.py must not be rerun on the changed sources. Typed callback adapter requirements, bidirectional conversions, C1/C2, B3 deferred checks/B4 audit and D remain outstanding. The full goal stays active.

Final latest-source `just ci` passed: 847 compiler tests and 125 driver tests, Go helper race tests, all golden checks, stage3 fixed point and extracted-release smoke (`/tmp/goml-raw-function-ci.log`). `git diff --check` passed and no generated snapshots changed in this step. Nullable raw Go function values and transport are implemented and verified. Explicit closure conversion adapters and the remaining plan are still incomplete; the full goal remains active.

## C0 explicit bidirectional callback adapters

Implemented ffi::func_from_closure and checked func_to_closure. Source/Core/Mono retain typed intrinsic requests; Lift detachment creates repr::Callable::CallbackAdapter carrying direction and concrete signature through Lift and ANF. Both verifiers reject mismatched adapter callable types and unsupported text policy. Backend requirement collection now groups runtime hooks, intrinsics, foreign witnesses and callback requests; callback emission generates signature-specific holder structs, Apply methods and factory functions returning bound method values. Holders retain the existing function/environment strongly, preserving lambda lifting without reflection, registries or global FFI locks.

The Apply method explicitly unpacks GoML tuple storage to Go multiple results in the forward direction and packs native multiple results in reverse. Unit maps to an omitted Go result in the forward direction and a GoML unit value in reverse. The public reverse operation returns None before constructing a closure for nil. Ordinary GoML string/char callback values, including exposed array/slice/channel/map elements, are rejected; raw ffi::String/Rune permit explicit checked handling. Source access to the internal unchecked intrinsic is rejected, verified with /tmp/goml-callback-nil-intrinsic.gom.

The cross-package driver regression covers uncaptured and captured closures, shared mutable cells, different closures with identical signatures, returned Go functions, repeated conversion, nil, unit, multiple results, preserved error identity, invalid UTF-8 raw-string transport, cached execution and text-policy failures. Initial emission used the wrong ReturnMulti field name, corrected before stage0 rebuild. The first integration run found that method emission printed an explicit void result; corrected it to omit the return type. Repeated focused integration passed (/tmp/goml-callback-adapter-test2.log). Stage0 make/resource packing passed (/tmp/goml-callback-adapter-make3.log). Added independent adapter metadata/direction/text-policy tests and expanded public API navigation. Compiler, driver, std and changed builtin sources were formatted. Full CI is /tmp/goml-callback-adapter-ci.log.

C0 conversion functionality is implemented but not yet formally accepted. C1 async/unregister/reentry/concurrency, C2 panic/provenance/DCE/release/performance acceptance, B3 deferred checks/B4 audit and D remain outstanding. Do not rerun /tmp/goml-callback-adapters.py on already modified sources. The full goal remains active.

Final latest-source `just ci` passed: 848 compiler tests and 126 driver tests, Go helper race tests, all golden verification, stage3 fixed point and extracted-release smoke (`/tmp/goml-callback-adapter-ci.log`). `git diff --check` passed; no generated snapshots changed. Basic bidirectional callback conversion is implemented and verified, while C0 audit and the remaining full plan stay active.

## C1 retained, concurrent and reentrant callback example

Added examples/ffi-callbacks with a typed Go subscription holding a converted GoML closure after its creating frame returns. A channel gate makes delayed invocation deterministic, including a GC before release. The first batch invokes the retained callback from 64 goroutines using an atomic counter and immutable capture. The example also verifies 32 levels of Go/GoML recursive reentry, callback self-unregistration and 128 balanced registration cycles. Registration accounting stores only a count; there is no global callback registry or finalizer dependency.

Unregister clears the subscription's function reference under its mutex. Workers release the mutex before invocation, allowing self-unregistration and reentry. A worker that already selected a callback may still execute it after Unregister; callers must join active batches before disposing of captured resources. Batches released after unregistration and subsequent batches dispatch nothing. The README and language guide specify this contract and ordinary shared-state synchronization responsibilities.

The driver regression copies the example sources, runs fresh and cached builds, compares generated source and runs the generated program under go run -race with CGO enabled. The focused regression passed (/tmp/goml-callback-lifetime-test.log). Full CI reached its final fixed-point comparisons without failure diagnostics, with 848 compiler and 127 driver tests passing (/tmp/goml-callback-lifetime-ci.log). The terminal tool result was lost during context compaction, so the final exit code of that invocation was not available. Added extracted-release smoke coverage for formatting, required FFI validation, fresh/cached execution and stable generated source. A new full CI run records an explicit exit status below. C2 panic/debug/performance acceptance, B3 deferred requirements, B4 audit and D remain outstanding; the complete goal stays active.

Final latest-source `just ci` passed with explicit exit status 0: 848 compiler tests and 127 driver tests, helper race tests, golden checks, stage3 fixed point and extracted-release smoke including the callback example (`/tmp/goml-callback-release-ci.log`, `/tmp/goml-callback-release-ci.status`). `git diff --check` passed. No generated snapshots were changed in this step. C0/C1 evidence and the precise unregister contract are collected in acceptance-c0-c1.md. Remaining B3/B4/C2/D work and the complete goal remain active.

## C2 panic propagation, callback pruning and measured allocation

Added callback_panics_preserve_identity_unwind_and_survive_pruning. Its explicit Go recovery adapters verify original panic object identity through Go/GoML calls and a Go function converted into a closure and back. Deferred Go work runs during unwinding. Unit and multi-result adapters propagate the panic without manufacturing normal return values, and a nil raw-map write inside a callback retains its native runtime panic. Separate process runs verify unhandled synchronous and asynchronous panic failure. Recovery is explicitly confined to its goroutine; adapters add none implicitly.

The retained closure captures a Ref and calls a private helper in another package. Both remain alive after normal compiler pruning, while an unrelated private helper's unique output string disappears from generated Go. Fresh/cached executions produce identical source. The focused driver regression passed (/tmp/goml-callback-panic-test.log). Driver sources were formatted before testing.

Added examples/ffi-callback-bench as a runnable measurement module with separate native/adapted call and escaping construction cases. GoML sources were formatted and the Go shim was gofmt'd. The final benchmark run passed (/tmp/goml-callback-benchmark2.log): on go1.25.8 linux/amd64, i7-13700K, invocation measured 0 allocations, while escaping capturing GoML callback construction measured 32 bytes and 2 allocations per operation. Its README records all timings, native baselines, included factory overhead and single-run limitations. No portable latency or general zero-allocation claim is made.

The language guide documents panic propagation and links the benchmark. Full current-source CI is /tmp/goml-callback-panic-ci.log. C2 source provenance and remaining acceptance, B3 deferred constraints, B4 audit and D remain outstanding; the complete goal stays active.

Final latest-source `just ci` passed with exit status 0: 848 compiler tests and 128 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (`/tmp/goml-callback-panic-ci.log`, `/tmp/goml-callback-panic-ci.status`). `git diff --check` passed. No generated snapshots changed in this step. C2 provenance and remaining acceptance plus B3/B4/D remain open; the full goal is active.

## C2 persistent Core source origins

Added ArtifactSourceOrigin records to Core units with canonical symbol, package-relative source file, byte range and one-based line/Unicode-column coordinates. The package compiler collects top-level function and extern declaration names from the checked source ASTs using the original parse input text and sorts by symbol. Origins do not affect interface semantic hashing. They are independent of source availability at link time. Core JSON format is now 27 and the binary value section is version 7; bootstrap stages continue to consume their own artifacts.

The decoder validates unique package-owned symbols, file-name-only paths and ordered positive source coordinates, returning recoverable errors for malformed metadata. Added source_origin_metadata_survives_missing_sources_and_validates_positions, covering CRLF source coordinates, extern/function records, binary/JSON transport after deleting the original source, malformed paths/positions/duplicates and actual package compilation. The initial focused test passed (/tmp/goml-source-origins-test.log). Added an interface-hash invariance assertion after moving declarations by a newline. Stage0 make passed after correcting the sort comparator to the library's three-way integer convention (/tmp/goml-source-origins-make2.log).

This completes the persistent metadata foundation only. Generated callback/specialization mappings and source-location output remain outstanding, as do B3 deferred constraints, B4 acceptance and D. The complete goal remains active.

The targeted Core serialization digest test detected the expected format change (/tmp/goml-source-origins-digest-test.log). Updated its ordinary unit-test digest for format 27 and the added empty source_origins field. No generated golden files were manually edited. Source collection now reuses parse input text instead of rereading files. Final current-source CI is /tmp/goml-source-origins-ci.log, with its exit status recorded separately.

Final latest-source `just ci` passed with exit status 0: 849 compiler tests and 128 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (`/tmp/goml-source-origins-ci.log`, `/tmp/goml-source-origins-ci.status`). This includes the extended source-origin regression and corrected Core digest test. `git diff --check` passed. No generated snapshots changed in this step. Generated-source mappings and the remaining full-plan work are not complete; the goal remains active.

## C2 generated Go origin index

Monomorphization now exposes explicit specialized-to-original function names in ExternalLowering. Lambda lifting has a checked entry point that records generated apply names against the enclosing Mono function; nested closures preserve the same declaration owner. These are debug associations rather than inferred name-prefix matches, and ordinary compilation APIs keep their existing behavior.

Added backend lowering that indexes the actual surviving Go functions and associates referenced typed callback factories/Apply methods and foreign wrappers with their enclosing function. Shared signature adapters retain multiple source associations. Executable module linking joins these names with persisted Core source origins and emits sorted, deduplicated version-1 JSON in the generated `_goml_source_origins` string constant after pruning. Embedding it in the existing source output keeps the cache and output update atomic without adding a second required driver-protocol output. The index labels its scope as enclosing-declaration; it does not claim expression-level positions or automatic panic stack rewriting.

Added project_generated_callback_origins_match_go_declarations_and_sources. It checks fresh/cached source identity, parses generated Go with its AST parser, unquotes/decodes the index, verifies every generated declaration, checks source name byte spans and line/Unicode-column positions, rejects dead-helper records, and verifies shared adapter and generic nested-closure mappings. Focused tests passed (/tmp/goml-generated-origins-test.log, /tmp/goml-generated-origins-test2.log); the existing retained callback example also ran successfully with its new index (/tmp/goml-generated-origins-example.log). Stage0 make passed (/tmp/goml-generated-origins-make.log). Compiler and driver modules were formatted before testing.

Full latest-source CI is /tmp/goml-generated-origins-ci.log. Direct extern-declaration associations, method origins, test/library linking and remaining C2 acceptance remain open, together with B3/B4/D work. The complete goal is still active.

Initial full CI passed compiler tests and fixed point but exposed a race in the existing asynchronous panic fixture (/tmp/goml-generated-origins-ci.log). Its goroutine deferred closing a completion channel while unwinding, which could release main before fatal panic processing completed. Changed the fixture to close that channel only after normal callback return; the unhandled panic then cannot race with successful main completion. This corrects the fixture's completion protocol rather than adding recovery. The focused panic regression passed (/tmp/goml-generated-origins-panic-test.log). Final CI is /tmp/goml-generated-origins-ci2.log.

Final latest-source `just ci` passed with exit status 0: 849 compiler tests and 129 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (`/tmp/goml-generated-origins-ci2.log`, `/tmp/goml-generated-origins-ci2.status`). `git diff --check` passed. No generated snapshots changed in this step. The executable origin index is verified at enclosing-declaration scope; the outstanding provenance coverage and full-plan items remain active.

## C2 test-runner source origin mapping

Refactored the backend's origin association into a shared helper and added lower_tests_with_source_names. Internal test linking now retains the Mono specialization and lifted closure associations and embeds the same versioned origin index as executable links. The generated source and test manifest continue through the existing output transaction; no extra output or driver protocol requirement is introduced.

Expanded project_generated_callback_origins_match_go_declarations_and_sources to generate and execute an internal *_test.gom fixture and a black-box tests package. It validates the program and both test runners with Go's AST parser, checks source spans and actual generated declarations, requires each test-specific callback's Apply mapping and compares fresh/cached output for both runners. The first attempt placed the internal test in the ordinary source file and ran zero tests because discovery requires *_test.gom files; moved it to main_test.gom and added the black-box fixture. The final focused regression passed (/tmp/goml-test-origins-test2.log). Stage0 make passed (/tmp/goml-test-origins-make.log), and both affected modules were formatted before testing.

Full current-source CI is /tmp/goml-test-origins-ci.log. Direct extern associations, method provenance, library linking and remaining C2 acceptance remain open, together with B3/B4/D. The entire goal remains active.

Final latest-source `just ci` passed with exit status 0: 849 compiler tests and 129 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (`/tmp/goml-test-origins-ci.log`, `/tmp/goml-test-origins-ci.status`). This includes AST/position checks and cached-output comparisons for both test runners. `git diff --check` passed and no generated snapshots changed in this step. Library mapping and the remaining C2/B3/B4/D work are still outstanding; the full goal remains active.

## C2 Go library source origin mapping

Added an explicit rename collector to library privatization and a backend entry point that uses those final, collision-resolved names when associating generated declarations with source symbols. Export wrapper names are indexed against their selected GoML declarations. Executable/test APIs retain their existing behavior through wrappers around the shared lowering implementation. The library command now retains Mono specialization and lifted closure ownership, joins them with persisted origins, and embeds the same unexported JSON constant in its generated source.

Expanded the generated-origin driver regression with a library export whose dependency invokes the captured/shared/generic nested callback fixtures. A normal Go host calls Calculate and verifies its result; Go AST inspection verifies all indexed declarations and the public wrapper's source origin. Repeated export must preserve identical source. The first fixture attempted to export the main package, correctly rejected by the CLI; moved the selected export into a library package. The focused regression passed (/tmp/goml-library-origins-test2.log). Extended the backend private-name test to check that a real collision's returned replacement matches the emitted name. Stage0 make passed (/tmp/goml-library-origins-make.log); both affected GoML modules were formatted.

Full latest-source CI is /tmp/goml-library-origins-ci.log. Direct extern-declaration associations, method origins and remaining C2 acceptance remain outstanding, together with B3/B4/D. The full goal remains active.

Final latest-source `just ci` passed with exit status 0: 849 compiler tests and 129 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (`/tmp/goml-library-origins-ci.log`, `/tmp/goml-library-origins-ci.status`). The library-origin regression and rename-collision assertions are included. `git diff --check` passed and no generated snapshots changed in this step. Direct extern/method provenance and the remaining full-plan work remain active.

## C2 explicit impl method source origins

TAST Function now retains name_span independently of comptime ranges, preserving it through comptime transformations. Explicit impl methods use a shared Core impl_method_name helper, so provenance uses the same receiver/trait-qualified identity as code lowering. Package artifacts collect positions from the original parse text for methods that actually exist in Core; synthetic methods lacking a declaration span are omitted. The decoder accepts method identities only when the unit contains a corresponding inherent or trait implementation function and still rejects forged method/foreign function identities. Generated JSON entries now include an additive source_package field so a method's defining package is independent of types and traits in its identity.

The regression exposed an additional trait-method rename during Mono type materialization. Added its final name to the explicit specialization/source table; this restores trait lifted-environment ownership without parsing encoded names. The generated-origin regression now includes three distinct make methods: generic inherent, trait implementation and another concrete inherent method. It checks their lifted apply origins, actual positions and owning package across executable, internal/black-box tests and library output, retaining shared adapter and cache checks. Shared adapters still report enclosing generated callers after inlining, not invented expression-level histories.

Stage0 make passed after fixing the decoder helper signature (/tmp/goml-method-origins-make2.log), and passed again after the Mono rename fix (/tmp/goml-method-origins-make3.log). The fixture needed an explicitly typed combination closure and a public alias for its public trait signature. The final focused regression passed (/tmp/goml-method-origins-test4.log). Both affected modules were formatted. Full latest-source CI is /tmp/goml-method-origins-ci.log. Direct extern associations and remaining C2/B3/B4/D acceptance are still outstanding; the goal remains active.

Final latest-source `just ci` passed with exit status 0: 849 compiler tests and 129 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (`/tmp/goml-method-origins-ci.log`, `/tmp/goml-method-origins-ci.status`). The same-named method regression and malformed-origin rejection checks are included. `git diff --check` passed and no generated snapshots changed in this step. Direct extern association and the remaining plan items are not complete; the full goal remains active.


## C2 direct extern binding origins

Foreign callables now retain their actual binding identity through TAST, Core, Mono and lifted representation, including go_method. Artifact callable bodies reuse the optional id field; legacy bodies without it retain an empty identity, while malformed non-string values are recoverable errors. Native wrapper naming and ABI are unchanged. Wrapper collection merges the used binding identities when target/signature sharing produces one declaration, and the source index adds those declarations alongside enclosing generated callers.

Expanded the origin regression with shared-target bindings in separate packages, unused aliases, ordinary versus raw string signatures and an external receiver method. Go AST inspection validates exact binding associations across program, internal/black-box tests and library output. The revised fixture changes inlining, so captured first/second apply ownership is checked independently from shared adapter caller ownership; no expression-level inlining history is inferred. Stage0 make and the final focused driver regression passed (/tmp/goml-extern-origins-make.log, /tmp/goml-extern-origins-test2.log). Added a compiler codec test for legacy and malformed IDs. Both affected GoML modules were formatted. Full CI evidence follows after completion. C2 acceptance, deferred B3 requirements, B4 audit and D remain outstanding; the full goal remains active.


Final latest-source `just ci` passed with explicit exit status 0: 850 compiler tests and 129 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (/tmp/goml-extern-origins-ci.log, /tmp/goml-extern-origins-ci.status). The initial focused codec test required replacing Option assert_eq with explicit Some/None checks because Option[string] lacks Debug; the corrected test passed in full CI. No generated snapshots changed in this step. [C2 acceptance](acceptance-c2.md) collects panic, pruning, precise declaration provenance, cache/export stability, packaged-toolchain and benchmark evidence with its declaration-level limits. Deferred B3 requirements, B4 audit and batch D remain outstanding; the full goal stays active.


## B4 resource and documentation audit

Audited the committed file-reading example against actual resource behavior and release coverage. Extended its driver regression with invalid UTF-8 bytes, requiring the original positive partial-read count/error and explicit close to survive decoding failure. Extracted-release smoke now compares generated-source digests between fresh and cached runs. The example README describes both additions. This state passed full CI with exit status 0, 850 compiler tests and 129 driver tests (/tmp/goml-file-read-audit-ci.log, /tmp/goml-file-read-audit-ci.status).

Added formats_raw_ffi_boundary_types_and_receiver_bindings to cover all B raw boundary type spellings, nested generic aliases and go_method formatting together. Compiler and driver modules were formatted. The B4 audit records the resource, diagnostic/artifact, text-policy and packaged stage0 compatibility evidence without accepting the separately deferred B3 constraints. Final latest-source CI is /tmp/goml-b4-final-ci.log; acceptance awaits its terminal result. Batch D also remains outstanding, and the complete goal is active.


Final latest-source `just ci` passed with explicit exit status 0: 851 compiler tests and 129 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (/tmp/goml-b4-final-ci.log, /tmp/goml-b4-final-ci.status). The new formatter fixture passed; file-example generated-source digests match. git diff --check passed, and no generated snapshots changed in this step. [B4 acceptance](acceptance-b4.md) is complete for its documented scope. B3 deferred map-key source-check propagation and deferred external named-key validation, plus batch D, remain; the full goal is active.


## B3 specialized external map key validation

Mono key validation now has an explicit metadata-aware entry point. Module linking derives normalized external identity templates and comparability rules from each unit's persisted Go metadata, then validates the pruned specialized program. Keys recurse through materialized local struct fields, arrays, tuples and parameter-dependent external types; pointer indirection retains native comparability. Rule matching binds original generic parameters against normalized concrete identities, covering reordered/erased aliases without inferring from source names. Missing, unknown or unresolved external metadata returns recoverable diagnostics. The ordinary Mono validation API remains available before a linked external environment exists.

Stage0 make passed (/tmp/goml-external-key-make.log). The focused driver regression passed (/tmp/goml-deferred-external-key-test.log), covering body-only map uses through a nested closure and a local generic key wrapper, private non-comparable Go fields, nested Box applications, reordered aliases, valid pointer-only fields, fresh/cached execution and --ffi-check off. Added compiler checks for missing/unresolved metadata and avoiding unrelated identity-only aliases. Compiler and driver modules were formatted, and the language guide now describes build/link enforcement. Full latest-source CI evidence follows. Source-check propagation of generic-body requirements and batch D remain outstanding; the full goal stays active.


Final latest-source `just ci` passed with explicit exit status 0: 852 compiler tests and 130 driver tests, helper race tests, golden verification, stage3 fixed point and extracted-release smoke (/tmp/goml-deferred-external-key-ci.log, /tmp/goml-deferred-external-key-ci.status). This includes the metadata failure checks and fresh/cached, nested generic, alias and disabled-validation driver cases. git diff --check passed and no generated snapshots changed in this step. Specialized external map-key build/link enforcement is implemented; generic-body requirement propagation during source checking remains before B3 acceptance. Batch D and the full goal remain active.


## B3 comparable predicate transport and checking

Introduced the internal TypePredicate::GoComparable in TAST and Core. It is substituted and qualified with the other predicates, participates in equality/deduplication, validates concrete keys against local structure and external comparability rules, and requires an explicit matching context obligation for unresolved parameters/projections. Pointer indirection is independent of pointee comparability. No source syntax or let-generalization is introduced. Interface/Core codecs preserve the predicate using a tagged type payload; malformed payloads remain recoverable. Core DCE retains referenced types and specialization rejects directly invalid concrete key shapes.

Stage0 make passed (/tmp/goml-comparable-predicate-make.log). Initial focused tests verified interface/Core round trips and inference-variable behavior, but found that the existing function-value instantiation helper dropped the new predicate (/tmp/goml-comparable-predicate-test.log). The helper now queues GoComparable requirements while preserving the same instantiated inference variables, so indirect calls can be checked after argument inference. Added malformed-payload regression coverage. This is the internal checking/transport foundation; function-body inference, same-package fixed-point propagation and attaching inferred obligations to exported signatures are not implemented yet. B3 and batch D remain incomplete, and the full goal stays active.


The first full CI attempt found a stage0 parsing ambiguity between a for expression and the following tuple return. A semicolon was not stable under the old formatter, so the helper now binds the returned tuple to a local result and returns that variable. The failed attempts are /tmp/goml-comparable-predicate-ci.log and /tmp/goml-comparable-predicate-ci2.log. The module was reformatted using the repository stage0 toolchain after stage2 was cleared by CI. Final CI is /tmp/goml-comparable-predicate-ci3.log.

The next source-inference step must collect normalized key obligations after inference, reduce private structural keys to the required type parameters/projections, propagate required predicates from generic callees, and iterate same-package signatures to a fixed point before exporting them. check_package_files_for_link_with_environments_options currently checks each file once; artifact_package_exports_with_contract currently obtains top-level function predicates from HIR-derived signatures. Both paths must consume the inferred obligations, including first-class function references and cached cross-package interfaces. Do not mark B3 accepted while those paths remain unchanged.


Final latest-source `just ci` passed with explicit exit status 0: 856 compiler tests and 130 driver tests, helper race checks, golden verification, stage3 fixed point and extracted-release smoke (/tmp/goml-comparable-predicate-ci3.log, /tmp/goml-comparable-predicate-ci3.status). All four new predicate tests passed, including the corrected function-value path. git diff --check passed and no generated snapshots changed in this step. The predicate foundation is verified; body inference, package fixed-point propagation/export and batch D remain outstanding. The full goal stays active.


## B3 generic-body inference and package propagation

Function checking now collects raw-map key requirements from inferred expression and signature types after field/coercion/numeric resolution. Comparable requirements are reduced through local key structure and external metadata to unresolved owner parameters or associated projections, then added to the function's internal predicates before required-predicate validation. Requirements introduced by generic callees and first-class function instantiation participate in the same step.

Single-file and package checking now iterate inferred declaration requirements to a fixed point, keyed by HIR declaration identity rather than bare names. Each pass creates its own checker and reports only its resulting diagnostics; a 256-pass bound returns a recoverable diagnostic. The table attaches requirements to ordinary function and method schemes. Top-level artifact export now includes the checked function's inferred predicates, so private key structure need not be exposed to downstream callers. Only functions with comparable predicates are scanned when updating the table, avoiding quadratic scans for ordinary packages with no such requirements.

Stage0 make passed (/tmp/goml-map-inference-make.log). Six focused map regressions passed (/tmp/goml-map-inference-test.log), including the two prior build-only deferred-key regressions now requiring goml check to reject invalid arguments, plus cross-file declaration ordering, function values, nested closures, a private generic key wrapper, two dependent packages and cached runs. Added a query regression for current-package generic functions and inherent methods. Compiler/driver modules were formatted. Full latest-source CI is /tmp/goml-map-inference-ci.log. Source-only query dependency environments still derive signatures from HIR and only typecheck selected comptime dependencies, so that path must retain inferred signatures before full B3 acceptance; trait/type-level coverage also needs audit. Batch D and the full goal remain active.


## Resource limits after interrupted CI

The previous map-inference CI was interrupted during compiler tests; its log stops at 856/857 and there is no successful terminal status. The user reported a process crash and explicitly requires systemd-run resource limits for subsequent builds/tests. The old execution handle is missing and no goml/gomlc/just process remains. Its empty orphaned _artifact/gomlc-test/.goml-build-lock was removed after checking for live holders. Do not record that interrupted CI as passed.

The new query regression passed under a transient user service with MemoryHigh=5G, MemoryMax=6G, MemorySwapMax=0, CPUQuota=200%, TasksMax=256, GOMAXPROCS=2, GOMEMLIMIT=1GiB and GOFLAGS=-p=2 (/tmp/goml-map-query-limited-2.log). The first bounded attempt stopped on the orphaned lock before testing. All subsequent heavy commands must continue to use systemd-run limits per the user's instruction.

Added optional GOML_CI_SEQUENTIAL=1 to tools/parallel-ci.sh and GOML_TEST_JOBS overrides to both CI test recipes. Full CI is running as goml-ci-limited-2.service with the same resource limits, a one-hour service deadline, GOML_BUILD_JOBS=2, GOML_TEST_JOBS=2 and sequential stages. Its log is /tmp/goml-map-inference-ci-limited-2.log. An initial launch used an unavailable /usr/bin/just and exited before CI; the active launch uses /home/li/.cargo/bin/just and passes the invoking PATH. Shell syntax validation passed. Await the live service's terminal result before claiming full verification.


The bounded full CI identified the runaway case precisely: the runner for query::rejects_recursive_struct_map_key_layouts_without_expansion reached about 5 GiB and triggered MemoryHigh throttling, with zero OOM kills in the service memory.events. The service was explicitly stopped before continuing. The new comparability requirement collector had proceeded after the older key checker diagnosed a recursive layout but returned false; Loop[(T, T)] then expanded tuple arguments exponentially. Added the declaration-level by_value_type_has_cycle guard before substituting fields in the collector. The existing regression covers both direct recursion and argument-expanding recursion.

After verifying no live build processes remained, the stopped test driver's empty orphan lock was removed. Formatting and focused tests now run through /tmp/goml-limited-run, a systemd-run helper retaining the user's 5G/6G high/max memory, zero swap, 200% CPU, 256 tasks, two build/test jobs, sequential CI and one-hour service deadline. The active focused service is goml-map-cycle-test.service and its log is /tmp/goml-map-cycle-test.log. Await its result and a fresh bounded full CI before claiming verification.


The bounded focused run passed all 10 map-key tests, including direct and argument-expanding recursive layouts, in 3m54s including test-runner compilation, with a 1.4G service memory peak and zero swap (/tmp/goml-map-cycle-test.log). This verifies the collector cycle fix without reproducing the earlier memory growth. Fresh full CI is now running as goml-ci-cycle-fixed.service through /tmp/goml-limited-run, with log /tmp/goml-map-inference-ci-cycle-fixed.log. No successful full map-inference CI has yet been recorded. Preserve the live service and await its terminal result; all future heavy work remains systemd-limited.

## Source-query map requirement transport audit

While the bounded cycle-fix CI remains live, source inspection confirms a separate query gap: query/analysis.gom::collect_dependency checks only packages containing comptime functions, and environments_with_comptime rebuilds signatures directly from HIR. Even those checked comptime packages retain only their CTIR module, not checked function predicates. tast/env.gom::external_function_scheme clears def_id, so attaching inferred requirements after externalizing signatures cannot reuse the declaration-identity matching that distinguishes methods reliably.

A candidate patch is staged outside the repository at /tmp/goml-query-map-inference-next.patch (git apply --check passed). It retains checked dependency TAST files in dependency traversal order, checks ordinary source dependencies, and attaches inferred requirements to the collected dependency checker before externalizing function/method schemes. It reuses record_file_map_predicates and attach_inferred_map_predicates rather than matching methods by bare name. The accompanying query regression covers reverse file order, first-class functions and closures, two dependency packages, inherent methods with a same-named method on another owner, valid/invalid arguments, and an unsaved dependency override removing the map requirement. These candidates have NOT been applied, formatted, compiled or tested; do not claim they close the query gap. The existing CI must finish before changing compiler/test inputs. Candidate files are also under /tmp/goml-query-map-inference-next/. The preparation script only builds the implementation portion; rerunning it would replace the combined patch and omit the subsequently appended test.

The latest live observation of goml-ci-cycle-fixed.service still reports active/running, with compiler tests progressing past 218/857 and memory below 1 GiB; this is a verified wait, not a passing CI result. Continue polling the same service/session rather than restarting. All subsequent builds, formatting and tests must retain the user's systemd-run resource limits.

The bounded cycle-fix CI has now completed all 857 compiler tests successfully (0 failed). The service is still active and has entered the helper race checks; stage3/driver/release completion is still pending, so this does not establish full CI success. Candidate query coverage was strengthened with a public harmless function applying the other owner's same-named method to a raw slice, which must remain accepted even when the map-bearing method acquires a key requirement. The combined candidate patch still passes git apply --check and remains unapplied.

The bounded full cycle-fix CI finished successfully with explicit session exit status 0 and systemd result success: 857 compiler tests and 131 driver tests, helper race checks, stage3 fixed point, extension build and extracted-release smoke. Log: /tmp/goml-map-inference-ci-cycle-fixed.log; service: goml-ci-cycle-fixed.service; elapsed 16m32s; final service memory peak 3.2G and swap peak 0B. This is the first successful full CI for the generic-body inference/package-export implementation and its recursive-layout fix. The full plan remains incomplete.

After that terminal success, applied /tmp/goml-query-map-inference-next.patch to the repository. Source dependency queries now retain checked TAST files and attach inferred declaration predicates before externalizing dependency environments. Added the cross-package/unsaved-override/method-owner regression. Formatting, rebuild and focused query validation of this new change are pending; the successful CI immediately above predates this patch.

The query patch was formatted successfully from gomlc using the repository stage2 formatter under systemd limits (/tmp/goml-query-map-format-2.log). An initial formatter invocation used the repository root with a module-relative executable path and exited 127 before formatting; the corrected invocation passed. git diff --check passed. Focused `goml test infers_map_key` is now running as goml-query-map-test.service, unified session 33259, log /tmp/goml-query-map-test.log, with the same 5G/6G memory, zero swap, 200% CPU and 256-task limits. It compiles current test sources using stage2 and covers the existing local query regression plus the new source-dependency regression. Do not launch another build against this artifact directory or claim test success before its terminal result.

The focused query map tests passed (2/2, explicit exit status 0) under goml-query-map-test.service, log /tmp/goml-query-map-test.log, elapsed 3m59s including runner compilation, peak memory 1.6G and zero swap. The complete query suite then ran under goml-query-map-suite.service (/tmp/goml-query-map-suite.log) and reported 65 passed / 1 failed. The failure was keeps_signature_names_for_canonical_cross_package_targets: its dependency fixture declared go_ffi("strings", "external_call"), which was previously skipped by source dependency checking. That lowercase, nonexistent Go symbol violates the existing exported-symbol rule. Updated only the Go target to the real strings.ToUpper while retaining the GoML external_call name, parameter name and all original signature-help assertions. Reformatted gomlc under limits and git diff --check passed. Full latest-source CI is required; the previous 857/131 success predates the query patch.

Fresh full CI for the source-query patch and corrected existing fixture is running as goml-query-map-ci.service, unified session 34488, log /tmp/goml-query-map-ci.log. It uses /tmp/goml-limited-run with the persistent resource limits and sequential CI. Preserve this service and await its terminal result before changing compiler or test inputs; do not confuse its result with the earlier cycle-fix CI.

The first full source-query CI terminated with explicit status 1: 857 compiler tests passed and keeps_signature_names_for_canonical_cross_package_targets failed. Log /tmp/goml-query-map-ci.log; elapsed 9m16s; memory peak 2.3G; swap peak 0B. After correcting the invalid Go target, this test exposed an existing signature-help bug: signature_parameter_names found the correct canonical GoML extern symbol but searched its AST using the native Go symbol ToUpper, dropping extern_value. Changed the resolved-symbol branch to use symbol.name when reading parameter names, retaining the existing assertion and native Go binding target. Unresolved lookup behavior remains unchanged. Formatting and renewed focused query validation follow; this failed CI is not a passing source-query acceptance result.

The signature-name fix was formatted successfully under goml-query-signature-format.service. The complete 66-test query suite is now running as goml-query-signature-suite.service, unified session 27390, log /tmp/goml-query-signature-suite.log, with the persistent resource limits. Await this service before starting further builds against the same artifact directory. After it passes, run the three prepared /tmp/goml-map-type-audit modules to establish actual nominal/opaque-key behavior before the next full CI; the type-level acceptance risks currently derive from source inspection rather than executed fixtures.

The signature-name fix passed the complete 66-test query suite with explicit exit status 0 (/tmp/goml-query-signature-suite.log), elapsed 4m7s including runner linking, memory peak 1.9G and zero swap. No full CI has yet passed with the complete source-query/signature fix.

Ran the three prepared type audit modules under systemd limits after that suite terminated. All modules were formatted. /tmp/goml-map-type-audit.log records fresh and cached check status 0 for nominal_invalid, opaque_invalid and opaque_valid. The follow-up /tmp/goml-map-type-build-audit.log records build status 1, 1 and 0 respectively. The two invalid cases failed recoverably in Mono: nominal_invalid reports the Table[RawSlice[i64]] storage field; opaque_invalid reports a map whose Key contains a private RawSlice. The pointer-to-slice private field case builds successfully. Individual command logs are inside each /tmp/goml-map-type-audit case. These executed results confirm source/artifact checking gaps rather than hypothetical risks. Next implementation must preserve nominal contained-map obligations and opaque key comparability across public interfaces without exposing private fields or rejecting valid pointer-indirected keys. Add corresponding repository driver/source-query regressions, then implement and run a new bounded full CI. No heavy service remains live from these focused checks/audits.

Added repository driver regressions project_opaque_map_keys_preserve_private_field_requirements and project_nominal_map_storage_requires_valid_type_arguments in goml/cmd/goml/ffi_test.gom. Each alternates valid, invalid and valid input, checks fresh/cached behavior, and builds or runs the valid pointer case. The opaque test changes the dependency's private field type without changing its public root use; the nominal test specializes Option[Table[T]] without constructing the hidden map directly. Formatted the driver under systemd limits. The bounded map_ test run reproduced both expected gaps: 6 existing tests passed, 2 new tests failed because check unexpectedly succeeded (/tmp/goml-map-type-regressions.log, explicit exit status 1, 33s, zero swap). These are intentionally unsatisfied regressions pending the implementation, not a passing test result. No service from this turn remains live.

Implementation inspection: tast::StructInfo and tast::StructDef currently carry name/generics/fields/public_fields/has_hidden_fields only. artifact_struct_def filters private fields, and artifact_dependency_struct reconstructs the reduced StructInfo. The type contract needs both a comparable-key summary and contained-map obligations; infer while full private structure is available, serialize it with the public type, and substitute nominal arguments when checking uses. Existing ffi::GoComparability (Always/Never/Parameter/All/Unknown) is a candidate representation for the key summary; it does not itself represent contained-map obligations. Avoid treating has_hidden_fields as proof of either comparability or non-comparability, and retain bounded cycle handling. The source-query/signature changes have passed 66 focused query tests but still await a successful complete CI after these open regressions are implemented.

## B3 opaque struct key comparability transport

Added optional go_comparable summaries to TAST StructInfo/StructDef. Checked structs derive a rule while full private structure is available; public artifact extraction retains that rule while continuing to omit private fields. Dependency environment reconstruction and source/checker copies preserve it. Map-key checking applies the rule with nominal argument substitution and cannot prove comparability from hidden fields lacking a rule. Core-to-TAST reconstruction leaves the optional summary absent because Core still carries full structural fields for the existing specialization validator. This step does not implement contained-map obligations for nominal types.

The interface codec serializes tagged Always/Never/Parameter/All/Unknown rules with depth/node and parameter-range validation. Interface JSON version is now 27 and binary value-section version 10; Core schema is unchanged by this step. Added two compiler codec tests for rule round trips, null/unknown handling, malformed values/tags, parameter bounds and excessive nesting. Summary construction now discards only diagnostics produced during its own metadata probe, preserving prior checking diagnostics, so inspecting an unused type does not itself produce a map-key-use diagnostic. The language guide documents opaque comparability summaries without exposing private field names/types.

Bounded stage0 `just make` passed (/tmp/goml-struct-key-make.log), 1m31s, peak1.5G and zero swap. The driver regression project_opaque_map_keys_preserve_private_field_requirements passed after the initial implementation (/tmp/goml-opaque-key-test.log), including dependency private-field changes, cached checks, valid pointer builds/runs and invalid slice rejection. Subsequent metadata-probe diagnostic isolation and compiler codec test sources were formatted but are not covered by that driver result. The nominal contained-map regression remains unsatisfied; do not mark B3 complete.

Focused compiler `goml test struct_comparability` is now running under goml-struct-comparability-codec.service, unified session 15875, log /tmp/goml-struct-comparability-codec.log, using the persistent systemd resource limits. Await its terminal result. No full CI has yet passed with the source-query/signature and new opaque-summary changes. The one-time edit script /tmp/goml-struct-comparability.py must not be rerun. Two formatter launches used the repository root and failed before formatting; corrected gomlc-directory services succeeded (/tmp/goml-struct-key-format-2.log and /tmp/goml-struct-key-test-format-2.log).

The two struct comparability codec tests passed with explicit exit status 0 (/tmp/goml-struct-comparability-codec.log), elapsed4m1s including runner linking, memory peak1.5G and zero swap. This also compiles the metadata-probe diagnostic isolation added after the earlier driver regression. No heavy service remains live. Current full CI is still pending and the nominal-map-storage driver regression remains expected to fail until the next implementation.

Nominal contained-map implementation design: store a separate list of map-key requirements on TAST StructInfo/StructDef and EnumInfo/EnumDef; opaque key comparability alone cannot encode the validity of a map stored inside a non-comparable struct. Derive requirements from fields and enum payloads using a bounded fixed point over nominal declarations, consulting existing summaries and substituting their generic parameters at applications. Reduce structural key obligations to leaf parameters/projections so recursive applications such as Node[(T,T)] do not require expanding the actual type layout indefinitely. Traverse direct type arguments/container children as well. Export/import the requirement list with the type and apply it when collecting function parameter/return/expression obligations; known invalid nominal applications also need source-signature validation. Reuse the existing internal GoComparable predicate machinery and public-field filtering. Include aliases, enum payloads and private storage; do not solve only the single Table fixture by syntactically scanning function bodies. Check interaction with source-only dependency environments and preserve the 256-pass recoverable bound. Type summary collection must not erase previously diagnosed source errors or trigger unbounded recursive expansion.

## B3 nominal contained-map requirements

Added map_requirements to TAST StructInfo/StructDef and EnumInfo/EnumDef, preserving the list through checker copies, public export filtering, interface codecs and dependency environments. Interface JSON version is now28 and binary value-section version11. Core-to-TAST reconstruction leaves the list empty because full Core layout still supports downstream structural validation; no Core schema change in this step.

New tast/nominal_map.gom collects map-key obligations from direct type children and previously inferred nominal declaration requirements. Structural keys reduce through the existing comparable collector to leaf requirements; an unsatisfiable key uses an internal definitely-uncomparable type marker, avoiding export of private structural names. A bounded256-pass fixed point derives requirements from struct fields and enum payloads without expanding recursive nominal layouts. Metadata probes retain earlier diagnostics. Function signature/expression inference consumes substituted requirements, and nominal signature validation diagnoses known invalid substitutions. Alias normalization and source dependency environments share the same checker path.

Bounded stage0 just make passed (/tmp/goml-nominal-map-make.log). All8 map_ driver regressions passed (/tmp/goml-nominal-map-driver.log), including both previously failing opaque/nominal cases plus external metadata, nested private wrappers, function values and cached propagation. Expanded the nominal regression to private aliases, enum payloads and mutually recursive enum/struct declarations; that expanded test also passed (/tmp/goml-nominal-expanded-driver.log). Compiler and driver modules were formatted independently. Updated the language guide. No full CI has yet passed these changes.

Added query::validates_nominal_map_storage_without_expanding_recursive_applications, including Table[(T,T)] recursion behind a pointer with raw-map storage and valid/invalid key types. The focused run is active as goml-nominal-recursive-query.service, unified session45172, log /tmp/goml-nominal-recursive-query.log, with persistent systemd resource limits. Await its terminal result, then run bounded full CI. The one-time field-edit script /tmp/goml-nominal-map-fields.py must not be rerun. Broader trait/associated-type propagation and batch D remain outstanding; do not mark the overall goal complete.

The nominal recursive query regression passed with explicit exit status0 (/tmp/goml-nominal-recursive-query.log), elapsed4m25s including linking, memory peak1.5G and zero swap. It covers struct/enum storage and parameter-expanding Table[(T,T)] recursion behind a pointer with valid and invalid keys.

Executed five standalone trait audits under systemd limits (/tmp/goml-trait-map-audit.log; fixtures and individual logs under /tmp/goml-trait-map-audit). Blanket generic trait implementations whose method body adds GoComparable are rejected by the existing exact method-bound comparison in validate_trait_method_signature. Generic consumers with associated-type map keys accept the scalar implementation and reject the raw-slice implementation. A default trait method constructing RawMap[Self,i64] remains a confirmed gap: default_valid (private pointer field receiver) passes check/build, but default_invalid (private raw-slice field receiver) passes check and fails only Mono validation during build. Build audit log: /tmp/goml-trait-map-build-audit.log. Preserve the valid default-method case when carrying inferred default-body requirements into trait method use/implementation validation. These are executed audit fixtures, not repository regressions yet. B3 remains incomplete on this path.

Full current-source CI is now running as goml-nominal-map-ci.service, unified session77681, log /tmp/goml-nominal-map-ci.log. It includes source-query/signature fixes, opaque struct key summaries, nominal contained-map requirements and expanded regressions. Use the same persistent systemd limits and sequential CI; await its terminal result before changing compiler/test inputs. The known default-trait-method audit gap is not fixed by this CI and prevents B3 acceptance even if the current suite passes.

Default-method source audit while the nominal CI runs: validate_trait_impl synthesizes default_trait_method only when an implementation does not provide that method. The synthesizer builds an Expr::Call to default_name directly, bypassing ordinary call instantiation/required-predicate checking. The source default function is a private generated generic function (hir/derive.gom::trait_default_function) and can acquire inferred GoComparable predicates, but TraitMethodInfo carries only declaration predicates plus default_name. ArtifactTraitDef likewise persists declaration constraints and the default callable name, not a separate default-body requirement list. Therefore checking only a locally found default function would not fix cached cross-package implementations.

The next fix should retain default-body map requirements separately from declared method predicates, attach them from the checked generated default function, export/import them with the trait, and validate their substituted keys only when synthesizing an actually selected default implementation. Explicit overrides must not inherit restrictions from the unused default body. Preserve concrete pointer-bearing receivers and associated-type substitution; reject strengthened requirements that cannot be proved from an implementation's owner/method context. Include same-package and cross-package fresh/cached tests, explicit override controls, and default-body edits. Do not simply add inferred default requirements to the trait method's public declaration bounds: that would incorrectly restrict explicit overrides.

## B3 default-body requirement transport and canonical implementation identity

Default trait method map requirements now have a separate TAST and interface representation. Checked generated default functions contribute their inferred GoComparable keys; exported/imported traits retain those keys independently of declaration predicates. Only synthesis of an omitted method validates the substituted default-body requirements. Explicit overrides therefore do not inherit bounds from unused default bodies. Interface JSON is now version 29 and the binary value section is version 12; Core remains JSON 27 / binary value section 7. The decoder rejects duplicate requirement entries and references to methods without default bodies. Compiler and driver modules were formatted and stage0 make passed (/tmp/goml-default-map-make-2.log).

The new cross-package fresh/cached driver regression initially failed for a valid pointer-field receiver during specialization (/tmp/goml-default-map-driver.log). Read-only decoding of its checksummed Core artifacts showed that the generated default required ffi_demo::behavior::Size while the implementation witness retained the short name Size. Canonicalizing the checked implementation reference fixed that valid default scenario, but the explicit-override scenario then failed ANF validation because its method environment still emitted the old short-name function target (/tmp/goml-default-trait-name-driver.log). Stage0 make for the first identity fix passed (/tmp/goml-default-trait-name-make.log, 1m24s, peak 1.7G, no swap). The follow-up also canonicalizes resolved method callables, method references and implementation environments; its rebuild is in progress. These are functional failures, not permission or resource blockers. B3 remains unaccepted pending the complete regression and full CI.

The preceding nominal-map CI terminated with 857 passing compiler tests and four stale artifact digest expectations (/tmp/goml-nominal-map-ci.log). Its format-28 ordinary interface digest was independently reproduced from the exact expected JSON. Version 29 changes that same digest from a6047d38ccc83993292dcacfa5cbe6dce985aec75745eaa7e8c9081a7b890477 to 33809db1847b4c235d4408049f1e16e0743ed9a8c7e1e4218d601ca917b02aca when only format_version changes. The source assertions still need the final version-29 update and execution; no generated golden files were edited.

The method-environment identity fix passed stage0 make (/tmp/goml-default-trait-env-make.log) and the complete original default-method driver regression (/tmp/goml-default-trait-env-driver.log). Added an associated-type variant using qualified behavior::Size: scalar and pointer-to-slice associated keys check and run, a raw-slice associated key fails check, and an explicit override with that raw-slice associated type checks and runs. Both driver tests pass with fresh/cached repetitions (/tmp/goml-default-associated-driver.log, 2 passed, 17.254s, no swap). This covers both unqualified imported trait names and qualified trait implementation references. Updated the language guide with the separate default-body requirement rule. Added source-query dependency/unsaved override coverage and malformed default requirement metadata tests; their compiler test run is in progress as goml-default-map-compiler-tests.service, session75300, log /tmp/goml-default-map-compiler-tests.log. The simple interface version-29 digest and exact JSON expectations have been updated with the independently reproduced value; the trait-interface/Core digest expectations remain pending actual verification. No full latest-source CI success yet.

Default compiler/query/codec tests terminated successfully (/tmp/goml-default-map-compiler-tests.log): four selected tests passed, 4m28.596s, peak 1.3G, no swap. A sequential direct-runner digest audit used that exact newly built runner (/tmp/goml-default-map-digest-tests.log): both simple interface tests passed; the trait-interface and Core tests failed only their old digest assertions, producing 96519be5a441d3f46fb35fc0c74f58f3738de72e1cb13864452af8dbe656ec00 and b756b1b0e3e4a2311a003a083e16bee0fdae1587ef8f7ef5c0e5a92de3d3078a. The audit launcher reports each child exit code; its own zero status does not mean all four child tests passed. Updated those ordinary test-source digest expectations without removing the remaining structural/roundtrip assertions. Compiler formatting and git diff --check passed. Full latest-source CI is now running as goml-default-map-ci.service, session26545, log /tmp/goml-default-map-ci.log. Await this exact service/session's terminal result before changing compiler or test inputs. All build/test/format execution continues through /tmp/goml-limited-run; the full plan and B3 acceptance remain open.

## D0/D1 shared interface metadata audit

While the default-map CI remains live, read-only inspection identifies the next common prerequisite. tools/goml-go-meta/internal/validate/types.go currently serializes only NumExplicitMethods/ExplicitMethod and embedded interface references. It does not serialize Go's completed method set or IsMethodSet result. The type instance witness in witness.go is a type alias declaration, so its successful type query alone does not establish that a constraint-only interface is usable as a runtime value. D0/D1 must not infer either property from the existing declaration/instance status.

The next implementation should preserve existing explicit-method metadata and add a completed interface method list plus an explicit runtime-interface flag derived from go/types. Preserve method package identity, including unexported methods, so D1 can reject interfaces whose methods cannot be implemented in the generated caller package. D0 should consume the same completed shape to require a complete method mapping, preserve native interface identity and distinguish a wrapper's raw interface from GoML dyn Trait. Missing runtime-interface evidence must not count as an affirmative runtime classification; constraint-only interfaces may still be described as generic constraints, but must be rejected as runtime values. Test embedded io.Reader/io.Closer, duplicate inherited methods, generic instantiated signatures, sealed interfaces and constraint-only interfaces using the existing metadata helper and artifact transport. This is an implementation prerequisite, not completed D0/D1 adapter support. No compiler/helper/test inputs were changed during the live CI.

D interface metadata changes are drafted in /tmp/goml-interface-meta-work while the preceding CI continues to use unchanged compiler/helper/test sources. The helper draft preserves explicit methods, adds a mandatory-on-interface completed method list and an explicit IsMethodSet-derived runtime flag, and adds an actual variable type witness for instance queries so aliases of constraint-only interfaces no longer establish runtime usability. Draft tests cover io.ReadCloser inherited methods/results, diamond deduplication, instantiated generic method signatures, private method package identity, empty interfaces, declaration-only constraints and rejected runtime constraints. The GoML draft validates links to complete method signatures, resolves a runtime interface shape while retaining native identity, and includes metadata roundtrip/malformed/constraint tests. Files are not formatted, built or tested yet. Before copying the selected draft files into the repository, await goml-default-map-ci.service terminal state and verify /tmp/goml-interface-meta-work/base.sha256 against the live source files. Do not copy the entire helper tree over the repository. The current CI has passed all 863 compiler tests and helper race checks; its fixed-point stage is still active.

The complete B3 CI terminated with explicit exit status 0 (/tmp/goml-default-map-ci.log, session26545, service inactive): 863 compiler tests and 135 driver tests passed, together with helper race checks, stage2/stage3 fixed point, extension compilation and extracted-release/LSP smoke. Runtime 16m6.241s, memory peak 2.2G, swap 0B. The final dispatch review verified that selected defaults are checked with receiver/associated substitutions before package publication and that dyn conversion requires a satisfying implementation and matching associated bindings. Updated acceptance-b3.md to record B3 acceptance at this verified state; the full plan remains open on D0/D1/D2.

After CI termination, sha256sum -c /tmp/goml-interface-meta-work/base.sha256 verified all four base source files unchanged. Copied only the three modified helper files, new helper test, modified GoML link decoder and two new GoML interface-shape files into the repository. Go/helper and compiler formatting completed under systemd limits. The complete helper race suite is now running as goml-interface-helper-tests.service, session76007, log /tmp/goml-interface-helper-tests.log. The newly copied D metadata changes have not yet passed compiler build/tests or full CI. The language guide now states the runtime-instance variable-check rule; no adapter syntax or implicit Go-interface/dyn conversion is claimed.

D shared metadata helper race tests initially failed because the new fixture's type Empty collided with an existing fixture declaration (/tmp/goml-interface-helper-tests.log). Renamed it to EmptyInterface; the entire helper race suite then passed (/tmp/goml-interface-helper-tests-2.log, 16.980s, no swap). Stage0 make passed for the compiler metadata implementation (/tmp/goml-interface-metadata-make.log, 1m31.515s, peak 1.4G, no swap).

Added project_external_runtime_interfaces_reject_constraints. It checks and runs an empty native Go interface through a GoML identity function, proving nil versus typed-nil preservation, and rejects a constraint-only interface used in a GoML function signature. Both required/off modes and fresh/cached repetitions are covered. The driver test passed (/tmp/goml-runtime-interface-driver.log, 16.176s, no swap). GoML interface-shape codec tests are now running as goml-interface-shape-tests.service, session30801, log /tmp/goml-interface-shape-tests.log. The D0/D1 adapters and D2 binding generator are still unimplemented; these changes establish shared metadata and runtime type validation only. The prior successful full CI covers B3 and predates these D changes.

GoML interface metadata tests passed (/tmp/goml-interface-shape-tests.log, session30801 terminal0): three selected tests, 4m9.098s, peak 488.6M, no swap. Selected the D0 declaration contract documented in docs/ffi/interface-adapters.md: #[go_interface(RawType, WrapperName, trait_method = "GoMethod")] on a trait, with explicit from_go/into_go raw-value conversion methods. Added the AST specification parser, a native Go identity-to-bridge conversion helper, and a TAST checked-method planner. The planner requires complete one-to-one mappings, exported unambiguous Go methods, exact native parameter/result shape, and explicit raw text/slice boundaries; it rejects result dropping. Added AST and TAST unit tests for these rules. These functions are not wired into wrapper expansion/project compilation yet and the contract document explicitly marks the syntax unavailable. Compiler formatting passed; stage0 make is running as goml-interface-plan-make.service, session89830, log /tmp/goml-interface-plan-make.log. Await terminal build evidence and run the new tests before treating this planner as verified. The full objective remains active.

The D0 specification/planner stage0 make passed (/tmp/goml-interface-plan-make.log, 1m31.744s, no swap). New specification/planner tests are now running as goml-interface-plan-tests.service, session36628, log /tmp/goml-interface-plan-tests.log. Do not change compiler/test input files while this run is active. The next implementation step is syntax expansion of the explicit wrapper and its raw forwarding helpers, followed by mandatory typed-plan validation before package artifact publication. Required follow-up covers imported raw type paths, name collisions, source spans, nil/typed-nil and real io.Reader partial-result/error behavior. No D0 adapter is published by the current pipeline yet.

All four D0 specification/planner tests passed (/tmp/goml-interface-plan-tests.log, session36628 terminal0, service inactive): AST explicit mapping/invalid mapping tests and TAST raw buffer/partial-result/error plus implicit-conversion/result-loss tests. Runtime 4m23.367s, memory peak 1.4G, swap 0B. These tests and stage0 make verify the current parser/planner only, not project-level generated adapters. No heavy services remain from this work.

Expansion integration notes from source inspection: hir/derive.gom already expands trait defaults into ordinary AST functions and can host syntax-only adapter expansion without introducing type-directed decisions into HIR. Generated wrapper/forwarding AST should use the go_interface attribute's source span, preserve original non-receiver parameter/return AST types and visibility, and create deterministic private go_method extern helpers plus a private raw field and explicit from_go/into_go methods. TAST remains responsible for the checked method plan. compile_artifact_package currently computes public_names before derive expansion; generated public wrapper names must be included before exporting artifacts. Its hir_inputs retain expanded ASTs, which can also supply generated binding source origins. Mandatory plan validation belongs after external metadata resolution and normalized package signatures, before artifact publication, and must run in required and off modes. Imported raw type paths should resolve through the checked wrapper field's native type and dependency foreign metadata, preserving aliases/canonical identity. Unverified syntactic expansion must never publish an implementation. D0/D1/D2 remain incomplete; the full goal remains active.

D0 module compilation now expands deterministic raw-interface wrappers before public-name collection and validates the complete TAST method plan before artifact publication, including in off mode. Imported raw types resolve through checked wrapper fields and dependency metadata. Generated forwarding functions preserve the declaration source span. Stage0 builds passed in /tmp/goml-interface-expansion-make.log and /tmp/goml-adapter-diagnostics-make.log. Cross-package io.Reader tests now pass for partial results/errors, shared buffers, receiver state, explicit dyn conversion, nil/typed-nil round trips and generated source origins. Invalid signatures, missing or incomplete mappings, readonly slices, declaration/member collisions and non-interface targets fail with source diagnostics in required and off modes. Both driver tests passed in /tmp/goml-interface-driver-recheck-0906.log (39.686s, terminal success, no swap). The initial negative test incorrectly expected old artifacts to survive GoML source changes; the driver intentionally removes stale outputs before recompilation. The corrected assertion verifies that invalid declarations publish no interface or Core output and does not change cache behavior. Attribute placement and formatter idempotence tests have been added and formatted; the six selected compiler tests are running in /tmp/goml-interface-compiler-recheck-0906.log. D0 GoLibrary coverage and full CI remain pending; D1 and D2 remain unimplemented. All heavy commands continue under systemd memory/CPU/task limits.

The first six-test compiler run completed with four passes and two test-expectation failures (/tmp/goml-interface-compiler-recheck-0906.log, 4m27.654s, peak 1.3G, no swap). The formatter intentionally preserves attribute interiors verbatim; the new expected output now follows that policy. Direct bounded AST probes confirmed that attributes on trait/impl methods are rejected by the parser before placement checking, whereas function/struct/field/extern-type attachments receive the explicit go_interface placement diagnostic. Tests now distinguish those paths. Added a GoLibrary regression that calls an exported scalar/tuple API from ordinary Go, internally using an imported generated wrapper through dyn Reader; it checks partial results, errors, shared buffer writes, repeated generation and portable source origins. All three driver regressions pass (/tmp/goml-interface-library-tests-0906.log, 55.456s, terminal0/inactive, no swap). Both affected modules were formatted and git diff --check passed. Full just ci is the next verification gate, including the corrected compiler tests; D0 acceptance remains pending and D1/D2 remain incomplete.

The D0 full CI is live as goml-interface-d0-ci-0906.service, process session84302, log /tmp/goml-interface-d0-ci-0906.log. At the latest inspection it was linking the compiler test runner, with peak memory about 1.7GiB under the 6GiB hard limit; it has not reported a terminal result. Do not restart this run or modify its source/test inputs until terminal state is established. During the run, source inspection established the D1 closure-backed bridge approach and docs/ffi/interface-adapters.md now specifies the explicit from_trait conversion, typed method fields, static native interface assertion, structured plan transport, imported-plan checks and asynchronous/reentrant acceptance cases. This is a design contract only; no D1 public method or bridge implementation exists yet.

Full D0 CI completed successfully: /tmp/goml-interface-d0-ci-0906.log, session84302 terminal exit 0, goml-interface-d0-ci-0906.service inactive. All 871 compiler tests and 139 driver tests passed, together with Go helper race checks, stage3 fixed-point artifact comparisons, extension compilation and extracted-release/LSP smoke. Runtime 17m24.657s, memory peak 2.2GiB, swap 0B. This verifies the corrected attribute-placement and formatter expectations as well as the new interface metadata, D0 wrapper expansion, strict mapping validation and GoLibrary regression. The requirement-to-evidence audit is in docs/ffi/acceptance-d0.md; D0 is accepted for its documented module compilation path. D1 from_trait remains a design contract only; no reverse bridge or D2 bind-go generator is implemented. The full plan goal remains active. Next work is the structured D1 bridge construction plan and its validation/codec transport, followed by generated Go methods and a static native interface-satisfaction assertion. No heavy task remains from this CI.

Started D1 implementation with representation-level GoInterfaceBridge/GoInterfaceBridgeMethod and a structural construction-signature validator in repr/interface_bridge.gom. It enforces exported unique method names, matching closure factory inputs and native target identity, explicit native value boundaries, flat result handling and bounded recursive type inspection. This does not replace current Go metadata method-set verification, which remains a required integration step. go_backend/interface_bridge.gom emits typed closure fields, forwarding Go methods, a static interface-satisfaction assertion and a deterministic factory as Go AST nodes. Added two representation tests and a backend test that compiles and runs the generated io.ReadCloser with ordinary Go, preserving partial-result/error/buffer and captured state behavior. These components are not yet connected to a callable variant, source from_trait expansion, artifact codecs or the full D1 runtime path. Compiler formatting and stage0 make passed (/tmp/goml-interface-bridge-fmt-0906.log and /tmp/goml-interface-bridge-make-0906.log, make 1m13.415s, terminal0/inactive, no swap). Three selected tests are running as goml-interface-bridge-tests-0906.service, session28026, log /tmp/goml-interface-bridge-tests-0906.log. Do not modify these test/compiler inputs until terminal completion. Last full CI success remains the D0 run and predates these D1 components.

All three D1 representation/emission tests passed (/tmp/goml-interface-bridge-tests-0906.log, session28026 terminal0, service inactive), runtime 4m28.223s, memory peak 1.7GiB, swap 0B. The backend test used the real Go compiler and executed the generated io.ReadCloser methods, checking original io.EOF identity together with n=3 and buffer writes, repeated calls sharing captured state, a non-nil interface and Close state. This validates the isolated bridge emitter and structural validator only, not a GoML trait-to-interface conversion. The remaining integration must introduce a structured callable plan through TAST/Core/Mono and artifact codecs, attach it to generated from_trait code only after complete current Go metadata validation, preserve runtime-only restrictions and source origins, detach into the new repr bridge and collect/import/emit it through ANF/GoLibrary reachability. Unit-return, multiple-method ordering and malformed imported-plan tests remain required as the path is integrated. Last full CI is still the D0 success and predates these D1 components.

D1 transport implementation now adds GoInterfaceBridge { binding, methods } to TAST/Core/Mono callables. The callable function type supplies the native target and ordered closure signatures; Lift detaches that combined typed plan into repr::GoInterfaceBridge. Added substitution, printing, DCE, query names/signatures, ANF/Lift bridge validation, backend collection/deduplication/emission and generated method/factory source-origin mapping. Callable artifacts encode the ordered method array and originating binding; decoding rejects malformed/duplicate/non-exported names and validates constructor signatures in interface/Core expressions. Interface schemes reject comptime bridge callables. Interface format is now JSON30 / binary value13; Core is JSON28 / binary value8. Existing main_test.gom exact artifact hashes and format-29 JSON expectations still require version-30/28 verification and updates; they have not been silently changed. Initial builds identified and fixed missing TAST substitution/query match cases and an accidental decoder field insertion. Final stage0 make passed (/tmp/goml-interface-transport-make-query-0906.log, 1m26.773s, terminal0/inactive, no swap), and module formatting plus git diff --check passed. Five focused tests are running as goml-interface-transport-tests-0906.service, session71681, log /tmp/goml-interface-transport-tests-0906.log. The public from_trait syntax expansion and complete current Go metadata revalidation of source/imported bridge plans remain unimplemented, so D1 is not accepted. Do not change compiler/test inputs during this run. The full goal remains active; last full CI success is the earlier D0 run.

The first five transport tests passed (/tmp/goml-interface-transport-tests-0906.log, 4m32.585s, peak 1.3GiB, swap0). Source review then found that Go-level pruning would discard the generated blank-name static interface assertion. prune_functions_with_roots now seeds references from blank-name globals and retains those declarations; the native Go reader test now prunes the emitted file before compiling/executing it and explicitly verifies the assertion remains. The four exact artifact digest tests were run against the newly built runner and failed only the old expected values (/tmp/goml-interface-transport-digest-audit-0906.log). Updated the source-test expectations to interface JSON30: simple interface 561c6ff5b689c168f38de73d87b941dbad18dcf0bc3ce1d49710110742ab6bf1, trait interface c8e523ec6dc5d7d0afc4b45fae3b71650a0a721a26dd13369b33e4107a0c8b4b; Core JSON28 c101c6bb0e887fa9ffef4ccdb40d4ccdd75b204b97f3b062d5a2149d3accd3b0. No generated golden files were edited. Formatting and the pruning stage0 make passed (/tmp/goml-interface-pruning-make-0906.log, 1m9.642s). All five final focused tests passed (/tmp/goml-interface-pruning-tests-0906.log, session34351 terminal0/inactive, 4m23.229s, peak1.3GiB, swap0); all four exact interface/Core shape, hash and binary round-trip tests then passed using that same fresh runner (/tmp/goml-interface-digest-verified-0906.log). git diff --check passed. D1 from_trait expansion and complete source/imported bridge method-plan validation against current Go metadata still remain; full latest-source CI has not yet been rerun. No heavy task remains active.

D1 source integration now generates `Wrapper::from_trait(value: dyn Trait)` with typed capturing closures and a compiler-owned bridge extern. Source attributes cannot impersonate that extern, and the extern permission gate recognizes generated bridges without applying legacy FFI closure restrictions. Complete current interface metadata validates source exports before publication and imported interface/Core bridge uses before linking, including when FFI checks are explicitly off. Core traversal checks nested callable uses independently of export tables. Initial driver regressions exposed a missing generated-extern permission and then an uncanonicalized local dyn trait in exported signatures; both were fixed. Signature alias expansion now traverses dyn bounds, resolves their trait identity and expands nested type arguments/associated bindings.

The final stage0 build passed (/tmp/goml-dyn-signature-make-0906.log, 1m24.846s, peak1.7GiB, swap0, terminal0/inactive). All four interface driver regressions passed (/tmp/goml-dyn-signature-driver-0906.log, 1m19.181s, terminal0/inactive), including the new reverse reader path in required/off modes and fresh/cached runs: Go retains the trait-backed reader after its creation function returns, forces GC, calls twice, preserves shared captured state, partial count, original EOF and buffer writes, then explicitly releases it. Added pure checks for complete method sets, target identity, method names/result shape and missing verified metadata. Eight focused bridge compiler tests are running as goml-dyn-bridge-compiler-tests-0907.service, session87921, log /tmp/goml-dyn-bridge-compiler-tests-0907.log. Formatting and git diff --check passed before this run. D1 asynchronous/reentry/panic and reverse GoLibrary acceptance, broader imported-plan adversarial coverage and latest-source full CI remain pending. D2 remains unimplemented; the full goal stays active. All heavy commands run sequentially under systemd resource limits (MemoryHigh5GiB, MemoryMax6GiB, no swap, CPUQuota200%, TasksMax256).

All eight focused bridge compiler tests passed (/tmp/goml-dyn-bridge-compiler-tests-0907.log, 4m24.983s, peak1.5GiB, swap0, session87921 terminal0 and service inactive). This includes the compiler-owned source-attribute rejection, callable codec checks, missing-metadata rejection, complete native method-shape checks, representation checks and real Go emission execution. git diff --check passed. No heavy task remains active. These focused results do not replace the outstanding D1 acceptance cases or latest-source full CI.

D1 reverse GoLibrary coverage now exports InstallReader(), which registers a trait-backed native interface for the Go host to take and unregister explicitly. It uses the existing go_export unit-return API: returning native interfaces directly remains unsupported and is not silently added by this test. The host interface embeds real io.Reader and adds Reset() with zero native results; the GoML trait declares methods in a different order than the native completed method set. The test verifies deterministic portable output/static assertions across required/off and repeated export, retained captures after GC, asynchronous goroutine reads, shared Read/Reset state, independent concurrently used instances under go test -race, Go→GoML→Go→GoML reentry and exact panic-object identity. It passed (/tmp/goml-interface-library-register-test-0907.log, 25.602s, session98572 terminal0, swap0). The initial fixture failure (/tmp/goml-interface-library-test-0907.log) exposed incorrect test syntax and an unsupported export result, corrected by to_u8() and explicit registration.

Added current-metadata regressions that first cache a valid native interface, then alter its method set without modifying GoML source and require a recoverable Go declaration changed diagnostic; a separately rebuilt metadata case checks unexported native methods. The first run stopped at an overly generic expected diagnostic; the actual cache rejection was correct. The corrected test is running as goml-interface-stale-expect-test-0907.service, session64502, log /tmp/goml-interface-stale-expect-test-0907.log. Do not rerun /tmp/goml-add-interface-library-test.py: subsequent tests were appended to its generated test file and would be overwritten. D1 invalid receiver/associated-type, imported-plan adversarial checks and latest-source full CI remain pending; D2 remains unimplemented.

The corrected stale/private method-set regression passed in required/off modes (/tmp/goml-interface-stale-expect-test-0907.log, 12.059s, session64502 terminal0/service inactive, swap0). Native method changes invalidate cached declarations; rebuilding against an interface with hidden() rejects that unexported method. Both new driver tests have terminal success and no heavy task remains active. Driver formatting and git diff --check passed. These are focused D1 results; full CI and the remaining acceptance items are still outstanding.

D1 unsupported-trait driver coverage passed (/tmp/goml-interface-invalid-test-0907.log, 9.543s, session54134 terminal0, swap0): associated types, generic traits, associated functions without a receiver and generic methods all produce source-associated recoverable errors in required/off modes. Added an imported bridge artifact test using actual official-Go io.Reader metadata. It serializes interface and Core units and invokes __ffi-check-artifacts in both modes, requiring a valid plan to pass, a forged Write mapping to fail against the current Read method set, and stored metadata with an empty query list to fail as unverified. Its Core unit deliberately has no exported helper and nests the bridge callable in a non-root private function block. This tests Core traversal independently of the exported interface scheme. Compiler/driver formatting and git diff --check passed. The imported test is running as goml-interface-import-test-0907.service, session24117, log /tmp/goml-interface-import-test-0907.log; do not edit test/compiler inputs while it runs. Latest-source full CI is still pending and D2 remains unimplemented.

The first imported bridge run stopped before artifact validation because its fixture caller directory was relative (/tmp/goml-interface-import-test-0907.log, 3m44.484s, session24117 terminal1/service failed, peak1.3GiB, swap0). The official metadata protocol correctly requires absolute directories. Fixed exercise_imported_bridge_validation to resolve its directory with path::absolute_structured before creating the fixture; formatting passed. The corrected test is running as goml-interface-import-path-test-0907.service, session24664, log /tmp/goml-interface-import-path-test-0907.log. This failure is a test setup error, not evidence that bridge-plan validation passes or fails. Wait for this exact live service before modifying compiler/test inputs; full CI follows after the focused test passes.

The absolute-path imported test reached the real validation cases: the valid plan passed and the forged Write mapping was rejected for both interface/Core inputs and required/off. It then failed while constructing the no-query fixture because the metadata encoder rejects nonempty declaration results with zero queries (/tmp/goml-interface-import-path-test-0907.log, 3m43.666s, session24664 terminal1/service failed, peak1.3GiB, swap0). Preserved that structural encoder rejection as an explicit assertion and changed the imported missing-verification case to a well-formed empty TypeDeclarations/queries payload. Its bridge callable still references io.Reader, so the test now reaches the intended missing-verified-metadata gate rather than failing fixture serialization. Formatting and git diff --check passed.

Started complete latest-source CI, including the corrected imported test, under goml-interface-d1-ci-0907.service, session27003, log /tmp/goml-interface-d1-ci-0907.log. This avoids separately rebuilding the whole compiler test runner before the required full gate. CI is sequential under the existing 6GiB/2CPU/no-swap limits. Do not modify compiler, test or bootstrap inputs while this exact job is live; inspect its terminal result before any new heavy task. D1 full acceptance remains unproven until this gate and the requirement audit pass. D2 remains unimplemented.

During the live D1 full CI, source audit identified an additional representation edge to verify before declaring D1 accepted: repr::resolve_type preserves Tuple { items: [] }, interface_bridge_signature currently accepts it as zero native results, and interface_bridge_forward always declares a result local for any tuple but uses no fields when the tuple is empty. lower_go_function_type maps that signature to Multi { items: [] }. This may emit an unused result local for a structurally accepted imported bridge. Do not modify the live CI inputs; after CI terminates, add a real-emission regression for this case and either emit an effect-preserving discarded call or reject a noncanonical empty tuple at the appropriate boundary. This is a pending audit finding, not a demonstrated runtime failure. The active CI is still goml-interface-d1-ci-0907.service/session27003 and D2 remains unimplemented.

Live D1 CI has now completed all 880 compiler tests successfully (/tmp/goml-interface-d1-ci-0907.log). go_interface_bridge_imports_require_current_complete_metadata passed, including valid interface/Core plans, wrong method mapping, malformed query/result metadata encoding and structurally valid missing-verification metadata, across required/off. The service goml-interface-d1-ci-0907.service/session27003 remains active in subsequent CI work; this is not a complete CI result. Further read-only audit confirms the empty-tuple finding is not automatically handled by DCE: eliminate_dead_vars is applied in compile_fn, while emit_interface_bridge appends generated methods independently afterward. After this CI terminates, use a real sync.Locker bridge with empty-tuple-returning closures to verify zero native results, side effects and compilation, then fix emission if reproduced. Do not mutate live CI inputs.

Complete D1 CI passed: /tmp/goml-interface-d1-ci-0907.log, session27003 terminal0/service inactive, 17m37.698s, peak2.3GiB, swap0. It includes 880 compiler tests, 143 driver tests, Go helper/script checks, stage3 fixed-point artifact comparisons, extension compilation and extracted-release/LSP/FFI smoke. This is the latest full CI success but predates the following audit fix.

After CI ended, updated interface_bridge_forward so an empty-tuple return emits the closure call as an expression followed by return, preserving effects without declaring an unused result local. Added go_interface_bridge_empty_tuple_results_preserve_effects: it emits/prunes a real sync.Locker bridge whose two stored closures return the empty GoML tuple, checks that the static assertion remains and no result local is introduced, and compiles/runs Go Lock/Unlock calls to verify effects. Compiler formatting and git diff --check passed. Ten bridge-focused compiler tests are running as goml-interface-empty-test-0907.service, session64062, log /tmp/goml-interface-empty-test-0907.log. Do not modify compiler/test inputs during this job. The preceding full CI does not cover this follow-up change. D2 is still unimplemented; continue the full plan after confirming this edge regression.

All ten focused bridge tests passed after the empty-tuple emission fix (/tmp/goml-interface-empty-test-0907.log, 3m47.540s, session64062 terminal0/service inactive, peak1.3GiB, swap0). The real sync.Locker test proves Go compilation and Lock/Unlock effects without an unused tuple result. The earlier full CI success predates this small follow-up; retain that distinction in final acceptance.

Started D2 with explicit function metadata queries in the existing helper protocol. query_mode=function reuses TypeQuery arguments, Go AST witness construction, official package/type checking, current build context/world identity and the existing referenced type graph. It queries only an allowlisted exported package-level function, preserves generic declaration and concrete instantiated signatures, and rejects uninstantiated generics, failed constraints, private/missing symbols and function variables. Go checker resolves finite generic arguments via a variable witness; loading does not execute package init. No public bind-go command or file generator is implemented yet. Updated protocol-v1.md with the mode and function result contract.

All Go helper tests passed with race detection (/tmp/goml-function-query-test-0907.log, 19.390s, session23825 terminal0, swap0), including new tests for strings.Count, constrained/unconstrained finite generic instances, variadic metadata, repeated stable world identities and an init that would panic if executed. Existing function/type/interface query tests also passed. Go formatting ran under systemd limits. D2 next needs GoML query decoding/checked type conversion, explicit allowlist configuration, a reviewable source writer with overwrite protection, CLI integration, finite generic binding generation and examples/acceptance tests. Latest-source full CI remains required after subsequent D2 changes. No heavy task is active.

D2 now has GoML FunctionQuery/QueriedFunction/FunctionQueries types, request encoding and response decoding in ffi/function_queries.gom. It reuses bounded type graph/link validation and native Go identity normalization. Decoding verifies request/result IDs, status/kind, world identity, concrete parameter counts and no receiver; substitutes the explicitly requested arguments into the declared signature and compares the predicted and returned concrete signatures. It keeps variadic state separate and requires a final slice for variadic functions. External type declaration decoding remains distinct and unchanged. Added malformed/forged signature and variadic decoding tests.

The stage0 make after this codec addition passed (/tmp/goml-function-codec-make-0907.log, 1m30.822s, session48278 terminal0/service inactive, swap0); it also rebuilt the helper with D2 function query mode. Added run_foreign_function_queries in the compiler command layer, retaining existing helper timeout/output limits and process-status checks, plus a real helper integration test for strings.Count and MakeNumeric[int64], wrong generic constraints and duplicate query requests. Formatting and git diff --check passed. Three selected tests are running as goml-function-codec-test-0907.service, session84972, log /tmp/goml-function-codec-test-0907.log. Do not modify compiler/test inputs until this exact job terminates. Public bind-go configuration/CLI/source generation, overwrite protection, explicit generic binding emission and full D2 acceptance remain unfinished. Last full CI remains the earlier D1 run and predates the empty-tuple follow-up and D2 work.

All three D2 function-query codec/integration tests passed (/tmp/goml-function-codec-test-0907.log, 4m31.330s, session84972 terminal0/service inactive, peak1.4GiB, swap0). The real helper's constrained MakeNumeric[int64] declaration/instance signatures round-trip through GoML normalization and substitution; an invalid string argument is rejected.

D2 explicit native generic calls cannot rely solely on Go call inference, particularly for result-only type parameters. Added structured Go AST Expr::Instantiate { expr, args: Vec[Type] } and Expr::CallSpread { func, args, spread } instead of embedding native syntax into symbol strings. Integrated printing, private-name rewriting, type/name reachability and DCE effects. Spread append retains its destination assignment when its value would otherwise be discarded; other spread calls preserve effects. Added generic_call_nodes_preserve_types_spreads_and_effects, which prunes and privatizes a type referenced only as a generic type argument, emits zero[Type]() and sum[int64](values...), and uses real Go compilation/execution to check side effects and append/sum behavior.

Compiler formatting, git diff --check and stage0 make passed (/tmp/goml-generic-ast-make-0907.log, 1m10.108s, session22664 terminal0/service inactive, swap0). The new Go AST regression is running as goml-generic-ast-test-0907.service, session5652, log /tmp/goml-generic-ast-test-0907.log. Do not modify compiler/test inputs while it runs. D2 allowlist configuration, source/native forwarding generation, ownership-aware file publication, CLI and acceptance examples remain unimplemented. Last full CI remains the prior D1 checkpoint and predates these changes.

The first generic-call Go AST test stopped at its source assertion (/tmp/goml-generic-ast-test-0907.log, 4m22.496s, session5652 terminal1/service failed, peak1.4GiB, swap0). Its fixture incorrectly used append as an expression statement and expected DCE to convert that already-invalid Go statement into an assignment. Corrected the fixture to explicitly assign the append result to the slice; source assertion failures now include the emitted source. The regression is now named bind_go_generic_call_nodes_preserve_types_spreads_and_effects so it can run with configuration tests. No emitter behavior was changed for this fixture correction.

Added pure bind_go configuration models/parser and conversion to the existing function/type-instance query models. Version1 JSON explicitly names the GoML package/output and Go package/output/import path, with function/type allowlists and protocol-shaped finite type arguments. Parsing rejects unknown/duplicate fields, duplicate binding names across both lists, reserved aliases/prefixes, invalid identifiers, invalid output extensions, empty allowlists and bounded-input violations. It reuses lexer identifier recognition and the FFI bridge argument decoder through bounded public wrappers. Added configuration tests and docs/ffi/bind-go.md, explicitly marking CLI/generation/publication unimplemented. Formatting and git diff --check passed. Stage0 make is running as goml-bind-config-make-0907.service, session3858, log /tmp/goml-bind-config-make-0907.log. Do not modify compiler inputs until terminal completion; then run the bind_go_ filter to cover the parser and corrected native Go AST regression.

The repository stage0 make completed successfully (/tmp/goml-bind-config-make-0907.log, 1m29.791s, session3858 terminal0/service inactive, swap0). The new bind_go package is not yet connected to compiler command reachability; its dedicated tests provide the next direct compilation check. Three bind_go_ tests are running as goml-bind-config-test-0907.service, session82076, log /tmp/goml-bind-config-test-0907.log. This run includes configuration query/output preservation, malformed/ambiguous input rejection and the corrected real Go generic/spread AST test. Wait for this service before further source edits. Public command and generators remain pending.

Both bind_go configuration tests passed in /tmp/goml-bind-config-test-0907.log. The Go AST test still failed an incorrect fixture assumption: prune_functions_with_roots intentionally retains struct definitions, so asserting removal of an unused struct did not test the new Instantiate handling (session82076 terminal1/service failed, 4m30.523s, peak1.4GiB, swap0). Replaced that assertion/fixture with imports: native_io is referenced only through a type argument and must survive; discarded_fmt is unused and must be pruned. The test also retains local type privatization, explicit append assignment and native execution, now expecting two effectful generic zero calls. No existing struct-pruning behavior was changed.

Added bind_go/types.gom for native Go AST type construction and explicit GoML raw type rendering from shared GoTypeIdentity values. It preserves named generic types/import aliases, raw text, slices/maps/pointers, function signatures/multiple results and channel direction; requires explicit allowlist names for native named types; rejects unresolved parameters, ambiguous/missing mappings, unsupported scalars and invalid array/channel shapes. Type conversion tests check nested boundaries, raw function source syntax and native Go type output. These are pure type conversion components, not a completed source generator. Formatting and git diff --check passed. Five bind_go_ tests are running as goml-bind-types-test-0907.service, session14753, log /tmp/goml-bind-types-test-0907.log, including the revised real Go AST test. Do not modify compiler/test inputs until this job terminates. The module is still not connected to compiler CLI reachability, so subsequent command integration must also verify the stage0 build path. D2 full source generation/publication/CLI/examples and latest-source full CI remain pending.

The five-test bind-go type run finished with four passes and one fixture failure (/tmp/goml-bind-types-test-0907.log, session14753 terminal1, 3m47.208s, peak1.3GiB, swap0). Both configuration and both raw type conversion tests passed. The native Go AST fixture emitted its import after a struct; moved ImportGroup before the struct, preserving the existing compiler behavior.

Added the pure bind_go source generator and compiler-side generate_go_bindings orchestration through the existing function/type instance queries. It sorts bindings/imports, renders explicit raw GoML signatures and finite generic type aliases, emits native forwarders with explicit generic arguments and variadic expansion, and rejects missing or inconsistent metadata. The stage0 build passed (/tmp/goml-bind-generator-make-0907.log, session57338 terminal0, 1m31.592s, peak1.6GiB, swap0). Added deterministic generation coverage and an integration fixture querying strings.Count and Numeric[int64], generating both files, running native Go tests and goml check. Compiler formatting passed. Seven bind_go_ tests are running in goml-bind-generator-test-0907.service, session70849, /tmp/goml-bind-generator-test-0907.log; do not modify compiler/test inputs until terminal. CLI, protected publication, complete example and latest-source full CI remain pending. All heavy work continues through /tmp/goml-limited-run.

The seven generator tests completed: six passed, one failed (/tmp/goml-bind-generator-test-0907.log, session70849 terminal1, 4m32.845s, peak1.4GiB, swap0). The real Go generic/spread AST regression now passes, as does deterministic finite variadic generation. The metadata-to-source integration failed formatter parsing because internal generic extern type names began with underscores, whereas GoML type declarations require uppercase initials. A bounded standalone formatter probe reproduced the precise diagnostic. Changed the reserved helper prefix to GomlBindType_, added configuration validation/tests for type/function/package naming categories and reserved helper collisions, and formatted the compiler module. Seven focused tests are now running again as goml-bind-generator-name-test-0907.service, session99111, log /tmp/goml-bind-generator-name-test-0907.log. No claim of end-to-end success yet.

All seven focused bind-go tests passed after the helper naming fix (/tmp/goml-bind-generator-name-test-0907.log, session99111 terminal0/service inactive, 3m42.038s, peak1.3GiB, swap0). Evidence includes real Go generic/variadic AST execution, repeated deterministic source generation, metadata-query generation for strings.Count and Numeric[int64], native execution of the generated forwarders, and goml check consuming the generated public concrete alias. No heavy task remains active. Latest stage0 make predates only the subsequent name-validation/helper-prefix corrections and added integration test; latest-source full CI is still required. Next implement the public bind-go command and protected two-output publication, using compiler-side config/query/generation and a thin goml CLI forwarding layer to avoid duplicate config semantics, then add the complete real standard-library example and finish plan-wide acceptance. Existing goml/project/export_output.gom and export_paths.gom provide reference ownership/path behavior, but bind-go must protect both .gom and .go outputs and handle partial publication failure.

Implemented bind_go/output_paths.gom and output.gom: resolve config-relative output paths under a canonical root; reject traversal, symbolic links, nested Go/GoML modules, path collisions and the reserved recovery directory; protect two source files using an exact canonical ownership manifest with SHA-256 hashes; serialize module generators; preserve identical output timestamps; detect modifications during generation; stage/commit both files and manifest with source rollback and retained backups if rollback fails. Public CLI is not connected yet. Added four tests covering ownership/edits/unchanged timestamps, unowned/concurrent modifications and failed generation, rollback after both source commits, and path/nested-module/symlink rejection. Initial test compilation failed because assert_eq requires Debug<Option[string]>; fixed assertions to compare equality directly. Formatting passed after corrections. Eleven bind_go_ tests are running as goml-bind-output-verified-test-0907.service, session87362, /tmp/goml-bind-output-verified-test-0907.log. Do not change compiler/test inputs during this job. Latest-source stage0 make and full CI remain pending, along with command orchestration, module/import identity validation, complete example and plan-wide acceptance.

All eleven bind-go tests passed (/tmp/goml-bind-output-verified-test-0907.log, session87362 terminal0/service inactive, 3m50.040s, peak1.3GiB, swap0). A subsequent stage0 make exposed test artifact placement: publication fixtures left .gom files under gomlc/_artifact, and the stage0 build driver discovered them as source (goml-bind-output-make-0907.service terminal1, /tmp/goml-bind-output-make-0907.log). Moved these test roots to ../_artifact outside the compiler module and removed only the five directories created by these fixtures. Formatted the compiler module again. Stage0 make is now running as goml-bind-output-fixture-make-0907.service, session73474, /tmp/goml-bind-output-fixture-make-0907.log. Repeat the focused tests after it succeeds; the compiler/driver plan remains active.

Stage0 make passed after moving the publication fixtures outside the compiler module (/tmp/goml-bind-output-fixture-make-0907.log, session73474 terminal0/service inactive, 1m10.259s, swap0). The new stage2 toolchain then passed all eleven bind-go tests again (/tmp/goml-bind-output-final-test-0907.log, session69839 terminal0/service inactive, 4m39.948s, peak1.4GiB, swap0). Confirmed no bind-go-output-* fixture directories remain under gomlc/_artifact. No heavy task is active. Protected publication is tested as a library API; no public CLI invokes it yet. Next connect gomlc/goml bind-go commands with existing module discovery, read-only Go module/import identity validation and metadata generation, add CLI failure tests and the complete standard-library example, then run latest-source just ci and complete the plan-wide acceptance audit.

Connected public gomlc bind-go and goml bind-go commands, help, optional driver compiler selection and dry-run. Compiler-side command discovery requires existing GoML/Go modules, reads the Go module identity without modification, checks the configured native import path against its directory, and calls the protected publisher. Official metadata queries execute from a temporary package under the publication lock, avoiding stale generated wrappers and never executing initializers. Added a public query module-root helper and a driver compiler-resolution wrapper rather than duplicating configuration/query logic in the driver. Added examples/ffi-bind-go with strings.Count/strconv.Atoi, explicit raw text/error boundaries and real generator-produced sources/ownership manifest; canonical docs and contract updated.

Stage0 make passed (/tmp/goml-bind-cli-make-0907.log, session87825 terminal0, 1m34.094s, swap0). Formatted compiler and driver modules; the example module was formatted after its bindings were generated. Actual example generation/check/run passed (/tmp/goml-bind-cli-example-{generate,check,run}-0907.log), output 3/42/true. All four driver bind_go_cli_ tests passed (/tmp/goml-bind-cli-generic-test-0907.log, session21114 terminal0, 11.572s, peak346.6MiB, swap0), including constrained Numeric[int64] check/run and rejected string instantiation preserving both previous files, stale-wrapper regeneration with panic init, dry-run/no-output errors and handwritten-edit refusal. Added extracted-release smoke coverage that generates the example from source config, repeats/compares hashes, checks/runs using only packaged tools and verifies edit protection. This smoke addition and latest complete CI still require execution; no goal completion claim.

Latest full CI is running as goml-bind-d2-ci-0907.service, session44446, log /tmp/goml-bind-d2-ci-0907.log. Revalidated live during stage2 compiler linking/build: ActiveState=active, memory about 0.9GiB, MemoryMax=6GiB, CPU quota=200%. It uses the serial systemd wrapper and includes the new archive bind-go smoke. Do not change compiler, driver, example or test inputs until the job terminates. Poll the same session/service; observation timeouts are not failures. After CI, inspect every gate and perform the full plan completion audit; D1/D2 acceptance documents and any remaining audit corrections must reflect actual latest evidence. The goal remains active.

Plan-wide audit found the D2 query caller location did not preserve deep internal-import permissions: queries ran under the module root lock, while native output could live under owner/native and validly import owner/internal/secret. Deliberately stopped CI goml-bind-d2-ci-0907.service before completion to fix this confirmed gap; session44446 terminal130/service failed due requested stop, 4m28.211s, peak1.5GiB, swap0. This is not a test-suite pass or an independently observed test failure. The bounded /tmp/goml-bind-internal-audit-before-0907.log reproduces the forbidden-internal diagnostic for that otherwise permitted output location.

Moved query packages to <native-output-directory>/.goml-bind-go-query, using exclusive directory creation, preserving existing directories, cleaning after either query outcome, and reserving that directory name in output paths. Added CLI regressions for allowed deep internal imports (including actual check/run), disallowed outside callers, query cleanup and preexisting query-directory preservation. Both modules formatted. Stage0 make passed (/tmp/goml-bind-internal-make-0907.log, session2077 terminal0, 1m15.722s, swap0). All six CLI tests passed (/tmp/goml-bind-internal-cli-test-0907.log, session63574 terminal0/service inactive, 19.815s, swap0). No heavy job is active. The added compiler-side reserved-path assertion still needs its next focused/full suite.

A second D2 audit defect is reproduced and remains outstanding: configuring the original Go API and generated forwarding file in the same native package makes the generator emit an import of that package itself. Generation succeeds but goml check reports an import cycle (/tmp/goml-bind-self-before-0907.log and /tmp/goml-bind-self-check-before-0907.log; probe /tmp/goml-bind-self-audit). Native type/function emission needs same-package unqualified references and no self import. Also verify regeneration after the same-package API signature changes: the temporary query package imports the original native package, so stale owned generated wrappers may need an explicit official-Go overlay during metadata loading. Do not treat rejecting same-package generation as completion by narrowing the configured output contract. Consider final generated-source validation through the shared official helper, preserving handwritten sources and caller/import rules. Next add these regressions and fix generation/metadata handling before restarting latest full CI. D1/D2 acceptance documents and the complete plan audit remain pending.

Implemented same-package unqualified function/type rendering and omission of self imports. Added an optional bounded protocol source_overlay (one absolute module-contained .go file, no reserved witness names) to official Go package loading, with no disk writes and existing overlay-aware world hashing. GoML query runners retain their previous entry points and add overlay variants; bind-go queries omit only their owned generated file via a package placeholder, then validate the complete candidate through native wrapper queries before publication. Type-only bindings also validate the candidate native package. Added helper overlay/path/conflict tests and three CLI regressions for same-package generic types plus stale wrapper regeneration, candidate symbol conflicts retaining old outputs, and type-only package conflicts. Compiler/driver and Go helper files formatted. Entire helper go test -race ./... passed (/tmp/goml-bind-overlay-helper-test-0907.log, session6403 terminal0, 19.521s, swap0). Stage0 make is running as goml-bind-overlay-make-0907.service, session68802, /tmp/goml-bind-overlay-make-0907.log; do not change compiler/test inputs during it. Nine CLI tests and latest full CI remain to run.

Stage0 make with source overlays passed (/tmp/goml-bind-overlay-make-0907.log, session68802 terminal0, 1m37.476s, swap0). All nine CLI tests passed (/tmp/goml-bind-overlay-cli-test-0907.log, session87019 terminal0, 28.331s, swap0): this includes same-package generic type/function output, regeneration after Make gains a parameter, candidate symbol conflicts, and type-only candidate package conflicts. The original /tmp/goml-bind-self-audit failure directory now regenerates in place and passes check/run with output 42 (/tmp/goml-bind-self-{after,check-after,run-after}-0907.log). Added two further CLI regressions covering a candidate-induced native↔shim import cycle preserving old output and explicit variadic generic instantiation through candidate validation and real execution. Driver module formatted. Eleven CLI tests are running as goml-bind-overlay-final-cli-test-0907.service, session51262, /tmp/goml-bind-overlay-final-cli-test-0907.log; wait before changing source/test inputs. Latest full CI remains required after this audit correction.

All eleven CLI regressions passed (/tmp/goml-bind-overlay-final-cli-test-0907.log, session51262 terminal0/service inactive, 26.074s, swap0), including candidate import cycles and explicit generic variadic calls. Restarted full CI after the confirmed audit corrections: goml-bind-overlay-ci-0907.service, session91103, /tmp/goml-bind-overlay-ci-0907.log. Revalidated this specific service/session live during stage2 compilation. It uses the serial systemd wrapper with MemoryMax=6GiB, swap disabled, two CPU equivalents and 256 tasks. No compiler/driver/helper/example/test input edits while CI runs. Poll this same handle; do not restart on observation timeouts. After its actual terminal result, inspect the entire log/gates, complete D1/D2 acceptance documentation and the full plan-wide audit. The goal is not complete.

Latest CI goml-bind-overlay-ci-0907.service ended with exit1 before the compiler suite started: /tmp/goml-bind-overlay-ci-0907.log reports timeout on _artifact/gomlc-test/.goml-build-lock (session91103 terminal1, 1m43.837s, peak1.5GiB). Verified that no goml/gomlc build process or build service was running and that the lock was empty; it was left by the deliberately interrupted earlier CI. Removed only that empty directory with rmdir. Retrying complete CI as goml-bind-overlay-ci-retry-0907.service, session51160, /tmp/goml-bind-overlay-ci-retry-0907.log; the compiler suite now proceeds beyond lock acquisition. Do not edit build/test inputs while this job runs. Added D1 and D2 requirement/evidence audit documents, both explicitly pending current-tree full-suite acceptance. The stage0 pin is unchanged, and the TAST interface/env files contain no process or filesystem orchestration. The FFI implementation does not depend on the separate experimental gomlgo parser.

## Final plan-wide acceptance, 2026-09-07

`goml-bind-overlay-ci-retry-0907.service` completed successfully, process exit 0 and service inactive. Log: `/tmp/goml-bind-overlay-ci-retry-0907.log`. The run passed 895 compiler tests and 154 driver tests, the complete helper race suite, stage3/stage3-fixed artifact comparisons, script/resource/version checks, extension compilation and extracted-release/LSP smoke. The new packaged bind-go path generated the standard-library example, preserved repeated digests, checked and ran the expected output, and refused user-modified generated source. Runtime was 20m24.813s, peak memory 2.3GiB and swap 0B.

The final requirement audit found incomplete differential coverage in A2: argument count, private symbols and directional parameter/result mismatches had helper assertions but were missing from the direct native-build comparison. After CI terminated, TestDifferentialGoBuild was expanded to 24 cases covering those failures, missing/main packages, any assignment direction, defined parameter/result identity, aliases, inference, variadic arguments, tuples, unit and preserved discarded returns. This changes only tests. The entire helper race suite passed afterward in `/tmp/goml-plan-final-audit-checks-0907.log`, exit 0 and service inactive, 24.837s. The same bounded sequential run passed goml fmt --check for gomlc, goml, lib/std and examples/ffi-bind-go and checked goml/gomlc bind-go help. No implementation source changed after full CI.

Current contract documentation no longer describes completed external types, callback conversions, project validation, reverse interface bridges or bind-go as unimplemented. The language comparison table now matches from_trait behavior. The separate historical records remain chronological; [plan-audit.md](plan-audit.md) maps the frozen constraints, A0–A9, B0–B4, C0–C2 and D0–D2 to their implementations, tests and named deliverables, with explicit remaining language boundaries. All requested plan gates are satisfied; no stage0 pin change, release or commit was performed. All heavy work remained under the user's systemd limits. git diff --check passes.
